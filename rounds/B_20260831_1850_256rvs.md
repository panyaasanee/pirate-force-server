# LANE-B round `256rvs` (COMBAT)

Opened 2026-08-31T18:50+07:00. Branches: `claude/beautiful-carson-256rvs`
(pirate-force-server), `claude/modest-wright-256rvs` (pf_bridge) -- fixed
branch names assigned for this scheduled routine, not the usual random
per-round token.

## ผู้เล่นจะเห็นอะไรต่างจากเมื่อวาน

**ไม่มี.** โมดูลใหม่รอบนี้ (`mob_ai_scheduler.py`) ไม่มี importer ใน `src/` (พิสูจน์ด้วยเทส
`test_the_scheduler_has_no_importer_yet`, AST sweep) -- ไม่ถูกเรียกจาก `runtime.py` (ของ
chief) และไม่ compose เฟรมใด ๆ บนไวร์ไม่ว่าจะถูกเรียกวันไหนก็ตาม รายละเอียดทั้งหมดอยู่ใน
`notes_to_chief/20260831_1850_LANE-B-STATUS-ka1b-three-defects-verified-mob-ai-scheduler-built-unwired.md`

## เครื่องมือที่มีจริงในรอบนี้

ไม่มี `mcp__github__*` tool (function list ที่ได้รับมีแค่ Read/Grep/Glob/Bash/Edit/Write) ไม่มี
Agent/Task tool ให้เรียก subagent `pf-adversary` -- ใช้ `curl -H "Authorization: Bearer
$GITHUB_TOKEN"` ยิง GitHub REST API ตรงแทน (ยืนยัน `GET /user` คืน `panyaasanee`, HTTP 200)
และทำ self-review เชิง adversarial ด้วยตัวเองแทน `pf-adversary` (บันทึกไว้ในจดหมายคู่รอบนี้)

## Protocol A -- round-lock check (live API)

`GET /repos/panyaasanee/{pirate-force-server,pf_bridge}/pulls?state=open` ตอนเริ่มรอบ
(~18:36+07:00): มีแค่ `[LANE-GM]` (`pirate-force-server#404`) ไม่ใช่ล็อกของสายนี้ ไม่แตะ --
ไม่มี `[LANE-B]` เปิดค้าง ตรวจ PR `[LANE-B]` ที่ปิดล่าสุดทั้งสอง repo:
`pirate-force-server#399` (`merged=true`), `pf_bridge#614` (`merged=true`) -- งานรอบก่อน
(`fz9mhb`) อยู่บน `main` แล้ว ไม่ต้อง cherry-pick อะไร

## Protocol B -- mailbox triage

`grep "ADDRESSEE: LANE-B"` ทุกไฟล์ `notes_to_chief/*.md` ที่ยังไม่มี `.CONSUMED.txt`: **1 ใบ
ใหม่จริง** -- `20260831_1700_KA1B-TO-LANE-B-three-code-defects-from-owner-audit-verify-then-ticket.md`
(ใบ `20260831_0147_LANE-B-STATUS-addendum-2355-*` ที่ grep เจอด้วยไม่ใช่ใบเข้า เป็นจดหมายที่
LANE-B เขียนถึง chief เองตั้งแต่รอบก่อน ไม่ต้องมี stub) ตอบครบทั้งสามข้อในจดหมายรอบนี้ สร้าง
stub + ย้าย consumed แล้ว `_BRIDGE_HEARTBEAT.txt` ล่าสุด `2026-08-31T18:24:04+07:00` เทียบกับ
ตอนเริ่มรอบ (`18:36+07:00`) ต่างกัน 12 นาที ไม่เกิน 60 นาทีตามกฎ

## KA1B สามข้อ -- ตรวจกับ HEAD จริงทีละข้อ

รายละเอียดเต็มอยู่ในจดหมาย ที่นี่สรุปผล:

1. **โปรไฟล์ผู้โจมตีตายตัว (level 7/STR 132)** -- ยืนยันจริง (`mob_combat.py:1759-1760`,
   `runtime.py:268-277`) แต่บันทึกไว้แล้วในคอมเมนต์ของโค้ดเอง ("[PROPOSED, not measured]")
   ไม่เปิดใบใหม่รอบนี้ -- แก้จริงต้องมีแหล่งสเตตัสผู้เล่นที่ไม่มีในโปรเจกต์ (M cost ตาม KA1B เอง)
2. **`tick_step` ไม่มีใครเรียก production** -- ยืนยันจริง (`grep -n "mob_ai_control\."
   runtime.py` = 9 hit, ไม่มี `tick_step` สักจุด) -- **สร้าง `mob_ai_scheduler.py` รอบนี้**
   (ดูหัวข้อถัดไป)
3. **Training Iron Man (916) อาจได้ AI/loot/respawn ที่ไม่ควรมี** -- ตรวจละเอียดแล้ว **ปิดเป็น
   non-issue**: `mob_ai_control.profile_of()` บังคับ `offensive=False` เมื่อ `ai_combat=None`
   (916 เข้าเงื่อนไขนี้เสมอ), `mob_loot._set_rows` ข้าม `set_id==0` ทุกชุด (916 มีทั้งสามชุด=0),
   death ได้รับอนุญาต**โดยตั้งใจ**ผ่าน `WIDENING_RULINGS` ที่ตั้งชื่อ 916 ตรง ๆ, respawn ไม่มี
   กลไกอยู่จริงในโค้ดเลยไม่ว่ามอนตัวไหน (`grep -rn respawn src/pirateforce_foundation/*.py` =
   0 hit ของกลไก spawn-again) -- ไม่แก้โค้ด ไม่เปิดใบ

## ของจริงที่สร้างรอบนี้ -- `mob_ai_scheduler.py`, ผู้เรียกที่ `mob_ai_control.tick_step` ขาด

`src/pirateforce_foundation/mob_ai_scheduler.py` (ใหม่): `tick_session(ai_register,
combat_ledger, player_identity, player_position, player_alive)` วนทุกแถวในรีจิสเตอร์ของ
**เซสชันเดียว** (สถาปัตยกรรมที่มีอยู่แล้ว: `self.mob_ai_register` เปิดต่อ session ตาม
คอมเมนต์ของ `runtime.py:1231-1247` เอง) ด้วยผู้เล่นเดียวที่ session นั้นเห็นได้จริง ดึง HP จริง
จาก `combat_ledger.balance_of()` ไม่ประดิษฐ์อะไร ใช้ตำแหน่ง placement เป็นตำแหน่งมอน (มอนยัง
ไม่เคยขยับในโปรเจกต์นี้ ข้อความนี้จริงจนกว่าจะมีมอนขยับจริง) เรียก
`mob_ai_control.tick_step`/`commit_step` เรียงตาม `ai_register.identities()` (ลำดับ ascending
ที่รีจิสเตอร์เก็บอยู่แล้ว) ให้แถวถัดไปเห็น state ที่ commit แล้วของแถวก่อนหน้า **ไม่ compose
เฟรมเลย** (`mob_aggro.ATTACK_INTENT_DELIVERABLE` ยังเป็น `False` -- Door B ยังปิดเหมือนเดิม)

ทดสอบด้วยมอนจริงจากตาราง Bg0002 (placement 92, Orc Chief, `ai_wander=11` ซึ่งมี
`n_OFFESIVE=1` ตามตาราง `field_mob_ai_tables.AI_WANDER_ROWS`) ไม่ได้ประดิษฐ์แถวไหน --
`tests/test_mob_ai_scheduler.py` (ใหม่ 15 ใบ) พิสูจน์ว่ามอนตัวนี้เข้าโหมด AGGRO ได้เองผ่าน
`tick_session` โดยไม่มีผู้เล่นคนไหนตีมันมาก่อนเลย (ต่างจาก `damage_step`/`death_step` ที่ต้องมี
`HitOutcome` มาก่อนถึงจะทำงาน) และมอนหุ่นฝึก (bg0001) ยัง**ไม่มีทางเริ่มอะไรเอง**ผ่าน scheduler
ตัวใหม่นี้เช่นกัน (regression pin สำหรับข้อ 3 ด้านบน)

**containment guard ที่ต้องอัปเดตด้วย** (ไม่ใช่ของแถม เป็นสิ่งที่โปรเจกต์ตั้งใจให้ทดสอบ): โมดูลใหม่
เป็น importer ตัวที่สองของทั้ง `mob_aggro` (เดิมมีแค่ `mob_ai_control`) และ `mob_ai_control` (เดิมมี
แค่ `runtime.py`) -- แก้ไข `tests/test_mob_aggro.py::ContainmentTests` และ
`tests/test_mob_ai_control.py::ContainmentTests` ให้รับ importer ตัวที่สองนี้พร้อมคำอธิบายว่า
มันไม่ใช่ controller ตัวที่สอง (ไม่ owns AI state mutation, แค่เรียก `tick_step`/`commit_step` ที่
มีอยู่แล้ว) และแก้ `tests/test_mob_stat_fabrication_guard.py::LANE_B_MODULES` ให้รู้จักไฟล์ใหม่

## CORE-REQUEST (ยังไม่ใช่คำขอ wire ทันที)

`MOB_AI_SCHEDULER_WIRING` ในโมดูลเองเขียน call site แบบ "ยังไม่ระบุบรรทัด" โดยตั้งใจ (ยังไม่รู้ว่า
dispatch point ไหนใน `runtime.py` วิ่งถี่พอโดยไม่อ่านโค้ดเพิ่ม) รายละเอียดเต็มอยู่ในจดหมายพร้อม
ASK-COO ว่าจะเดินหน้า "Door B" (compose เฟรมให้มอนขยับ/ตีกลับได้จริงบนจอ) เป็นงานถัดไปของ COMBAT
หรือรอ BUILD-006 ปิดก่อน

## ตัวเลขที่วัดได้

```
tests/test_mob_ai_scheduler.py : ใหม่ 15 ใบ ผ่านทั้งหมด
src/pirateforce_foundation/mob_ai_scheduler.py : ใหม่ 1 ไฟล์
สวีตเต็ม pirate-force-server (pytest tests -q):
  ก่อนแก้ 3 containment/inventory guard: 4 failed (คาดไว้ -- โมดูลใหม่ชนกับ tripwire ที่ตั้งใจ
    ให้ชน), 5855 passed, 323 skipped, 11148 subtests passed (218.52s)
  หลังแก้ทั้งสามจุด (containment x2 + LANE_B_MODULES tuple) และแก้วลีไทยที่หลุดเข้า docstring:
    5859 passed, 323 skipped, 11148 subtests passed, 0 failed (214.27s) -- +4 passed ตรงกับ
    4 ใบที่เคย fail (ยืนยันว่าแก้ครบ ไม่ใช่ผลจาก count drift แบบที่รอบ fz9mhb เคยบันทึกไว้)
git diff --check: silent
ไฟล์ที่แตะรอบนี้ -- pirate-force-server: 6 (mob_ai_scheduler.py [ใหม่],
  tests/test_mob_ai_scheduler.py [ใหม่], tests/test_mob_aggro.py, tests/test_mob_ai_control.py,
  tests/test_mob_stat_fabrication_guard.py, rounds/B_20260831_1850_256rvs.md [ไฟล์นี้])
```

`current/pf_login_game_server_v141.py`: ไม่แตะเลย. ไม่แตะ canonical DB. ไม่แตะ capture corpus.

## ยังไม่ได้พิสูจน์

- BUILD-006 การ wire สุดท้าย ยังรอ `GT-146` (attended, human-at-client เท่านั้น) -- ไม่เปลี่ยน
- `mob_ai_scheduler.tick_session` ยังไม่เคยถูกเรียกด้วยข้อมูล session จริง (แค่ roster/ledger
  ที่สร้างขึ้นตรงในเทส) -- production_allowed = True ตามกฎ (ไม่มี flag ใด ๆ) แต่ "ไม่เคยถูกเรียก
  จริง" ต่างจาก "ถูกเรียกแล้วพัง"
- ASK-COO เรื่อง Door B ยังไม่มีคำตอบ (ส่งรอบนี้)

## CORE-REQUEST

`MOB_AI_SCHEDULER_WIRING` -- ดูหัวข้อด้านบน ไม่ใช่คำขอเร่งด่วน รอ ASK-COO ตอบก่อนว่าจะเดินไป
ทิศไหน (wire เฉยๆไม่มีผลบนจอ vs เดินหน้า Door B เต็มรูปแบบ)

## เปิดใบให้สาย C

ไม่มี -- คำถามจริงของข้อ ① (per-player battle stat บนไวร์) ยังไม่เปิดใบเพราะเป็นของใหญ่กว่า
งานรอบนี้ รอสัญญาณจากเจ้าของ/COO ก่อนตามที่เขียนไว้ในจดหมาย

## nonclaim

ไม่แตะ `runtime.py`/`app.py`/`current/pf_login_game_server_v141.py`/`scenarios/world_*.json`/
`scenarios/combat_*.json` เลยรอบนี้ ไม่อ้าง milestone เกมเพลย์ใหม่ -- โมดูลใหม่ไม่มี importer ใน
`src/` เลย (พิสูจน์ด้วยเทส) ไม่แตะเขตสาย A

## pf-adversary (self-review, tool ไม่มีในรอบนี้)

(1) `tick_session` mutate register ที่ได้รับไหม -- ไม่ (dataclass frozen + เทส
`test_a_call_is_pure_on_the_register_it_was_given` ยืนยัน) (2) mismatch ระหว่าง register/ledger
เงียบไหม -- ไม่ (`mob_combat.MobCombatContractError` โผล่ตรง ๆ มีเทสปักไว้) (3) docstring อ้างสิ่งที่
โค้ดไม่ได้ทำ (compose เฟรม) ไหม -- ไม่พบ อ่านซ้ำทุกประโยคใน "WHAT THIS MODULE IS NOT" เทียบกับ
โค้ดจริงแล้ว (4) มี Thai/non-ASCII หลุดเข้า `src/`/`tests/` ไหม -- เจอ 1 จุด (วลีไทยในดอกสตริงตัวเอง
ตอนร่างแรก) แก้แล้วก่อน commit ยืนยันด้วยสคริปต์กวาด byte>127 ทั้งสองไฟล์ = 0 (5) containment
guard สามตัวที่ import ใหม่ทำแตก -- พบและแก้ครบทั้งสามก่อน commit (ดูหัวข้อด้านบน)

-- LANE-B (COMBAT) round `256rvs`
