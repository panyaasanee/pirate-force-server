# LANE-A round 6p22bu

2026-08-30T14:52+07:00 - 2026-08-30T15:52+07:00 (approx).

## What this round did

Carried out `COO-DECISION 2026-08-30T14:41+07:00`
(`pf_bridge/notes_to_chief/20260830_1441_COO-DECISION-scene4-slave-market-first-door.md`),
which approved this lane's own recommendation from round `12lyda`: start the CLINE->MOBS
native-actor crosswalk for scene 4 (Bg0004, Slave Market Island, 116 native placements -
the highest of the ten still-shut doors surveyed), same pattern as BUILD-001's bg0001
crosswalk (`world_port_royal_identity.py`) and M3's bg0015 crosswalk
(`world_bg0015_identity.py` / `world_population_bg0015.py`).

Read the join by hand directly off the bridge clone's own committed tables (not copied
from any sibling module's citation of the same tables): `CONSTDATA_TH__SCENE_NAME.tsv`
(scene 4 -> `n_CLINE_TYPE=4`, `n_SCENE_LV=45`), `CONSTDATA_TH__CLINE.tsv` (type 4's 61
rows), `CONSTDATA_TH__MOBS.tsv`, `TEXTDATA_TH__MOBS_TIP.tsv`,
`CONSTDATA_TH__STANDARD_MOB.tsv`, and `gamedata/scene/bg0004/bg0004.placements.tsv` (116
rows). Digests re-derived and matched what sibling crosswalk modules already pin for the
four shared tables; the scene's own placements TSV got its own fresh digest.

Measured: 55 distinct Mob-Set numbers used across the 116 placements, all 55 present in
CLINE type 4's block (which also carries six unused keys, 109-114, all leader 0). 48 of
the 55 resolve to a real body; 7 do not (1 has no CONSTDATA MOBS row at all - the
recurring cross-scene "Port transportation" boat prop; 6 have a MOBS row with no
`s_OUTFIT` avatar template). 109 of the 116 placements ship; 7 are dropped, each with its
placement index and reason on the console. One resolved leader (917, outfit `INVISIBLE`)
has no MOBS_TIP row and ships with an empty name - the exact same shape
`world_port_royal_identity.py` already ships for the same leader id at Bg0001 - and this
one leader alone accounts for 25 of the 109 shippable placements (Mob-Set 107, instances
01-25). Nine sets carry a `;`-separated multi-variant outfit; shipped first-variant only,
matching every sibling module's own [LANE-A ASSUMPTION].

Two placement rows (82, 83) disagree with themselves in the raw source: the free-text
`name` column says "Mob_Set_34 08/09" while the machine-parsed `template_ids` column says
45/46. Followed `template_ids` (the machine-parsed, authoritative column, same choice
`field_mob_tables_bg0002.py` already makes for its own scene) - recorded rather than
silently picked. One placement (83) also carries a second, unbuilt spawn triple in its raw
row; not shipped as a 117th actor this round (would silently move the round's own 116
target), recorded in `EXTRA_TRIPLE_NOT_SHIPPED`.

## What shipped in `src/`

- `src/pirateforce_foundation/world_bg0004_identity.py` (new) - the crosswalk table:
  `SceneIdentity`, `Bg0004Placement`, `IDENTITIES`/`UNRESOLVED`, `shippable_placements()`,
  `unshippable_placements()`, `_self_check()`. `production_allowed = True` (convention
  marker, matching bg0002/bg0015's own value - "shippable, no scenario flag", not a claim
  that a player has seen it).
- `src/pirateforce_foundation/world_population_bg0004.py` (new) - the census composer,
  reusing the exact frozen encoder (`legacy.make_npc_attr` / `make_remote_movement_attr` /
  `make_remote_actor_entry` / `make_runtime_remote_actors`) every sibling composer already
  uses; wire header constants and `INITIAL_REAPPLY_MS` imported from `world_population`,
  not redefined. Verified end to end against the real frozen `v141` encoder this round:
  builds a 109-actor collection, wire count agrees with the header, all 109 bodies
  byte-intact, every console line cp874-encodable.
- `tests/test_world_bg0004_identity.py` (new, 15 tests, 64 subtests) and
  `tests/test_world_population_bg0004.py` (new, 14 tests, 355 subtests) - table-shape
  controls, ASCII/cp874 checks, GT-078-regression check on the actual wire bytes (real
  `n_ID` in the NPCAttr template field, never the raw Mob-Set number), refusal tests,
  console-line shortfall tests, and an explicit "nothing under `src/` imports this module
  yet" AST-walk pin (the opposite assertion from bg0015's own now-wired version of that
  test, on purpose - see below).

Not registered anywhere a player can reach: `world_scene_travel.CENSUS_SOURCES`,
`world_population_handoff.ROSTER_COMPOSERS` and `lane_hooks/lane_a_scene_census.py`'s
console-reader table are all untouched this round, and scene 4's `login_entry_allowed`
stays `false`, exactly as the COO decision instructs ("do not flip it until the population
component is actually ready"). Bg0015's own history shows the identity+census pair landing
several rounds before its wiring did (`w0pu2i` built it, `ga91m5-r2` wired it) - this round
does the same first half for scene 4, deliberately, so the diff can be verified on its own
before the next round's wiring touches three more shared plumbing tables and their tests.

## Fallout from adding a new actor-entry-building module (fixed this round)

Adding `world_population_bg0004.py` moved three numbers `tools/pf_runtimeres_actor_entry_static.py`
pins from `src/`'s own content (`SRC_ACTOR_ENTRY_SITES` 17->18, `SRC_ACTOR_STREAM_SITES`
26->27, `SRC_MODULES_WITH_ACTOR_ENTRY` 16->17, new name inserted alphabetically). This is
outside the four write-zone paths named in the round brief, but it is the established,
repeated maintenance duty this exact situation has required of every prior lane that added
an actor-entry call site (rounds `w0pu2i`, `y9s0xo`, `7ptoku` and others all did the same
three-place re-pin for their own new module) - `tests/test_static_verifier_pins_cloud.py`
exists specifically to catch a lane that adds the call site and skips this step. Re-pinned
in the same commit: `tools/pf_runtimeres_actor_entry_static.py` (the guard + name tuple),
`tests/test_runtimeres_actor_entry_static.py` (the bridge-only test module's own copy of
the same three pins), and `reports/PF_RUNTIMERES_ACTOR_ENTRY001_STATIC_20260819.md` (the
`RUNTIMERES_COUNTS` JSON block re-pinned, plus a new append-only NOTE section - no
existing prose rewritten, following that file's own erratum convention).

## Player-visible claim

**None.** Scene 4's door (`login_entry_allowed`) is unchanged (`false`), no scene reaches
a player differently than yesterday, and no other scene's behavior changed. This round is
identity-table + census-composer construction and test coverage only.

## What's blocked / waiting

- The pairing (which Mob-Set number is which leader) is table inference only - no human
  has stood in this scene (registry `status: never_sent_to_any_client_by_this_project`).
  No ticket number opened for it this round since nothing is wired to a login path yet.
- Whether any of this scene's monster-shaped placements (Scythe Beetle, Orc Chief, Dragon
  Gladiator etc., the level 46-58 sets with a nonzero `n_RANK`) should be hostile is a
  LANE-B decision, explicitly not made here - matching the same M1-P split for Bg0002.
- Wiring (`CENSUS_SOURCES` / `ROSTER_COMPOSERS` / `lane_hooks` console reader) - next
  round of this same multi-round order.

## Numbers measured this round

Placements: 116 total, 109 shippable, 7 unshippable. Mob-Set numbers: 55 distinct used,
48 resolved, 7 unresolved. Multi-variant outfits: 9 sets, 44 of 109 shippable placements
affected. Set 107 (INVISIBLE, empty name): 25 placements. Full test suite (this repo,
`python3 -m pytest tests -q`): **5431 passed, 383 skipped, 9540 subtests passed, 0
failed** (118s) - up from 5430 passed / 11 failed before the actor-entry-census pin fix
above; the 383 skips are the pre-existing image-gated/capstone-gap modules this
environment cannot run, unchanged in count from before this round.

## CORE-REQUEST

None this round.

## ASK-COO

None this round (the COO decision this round carries out already answered the open
question from round `12lyda`).
