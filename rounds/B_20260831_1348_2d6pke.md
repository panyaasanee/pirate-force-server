# LANE-B round 2d6pke (COMBAT)

Opened 2026-08-31T13:48+07:00. Branches: `claude/tender-goldberg-2d6pke`
(pirate-force-server), `claude/wizardly-gauss-2d6pke` (pf_bridge).

## ผู้เล่นจะเห็นอะไรต่างจากเมื่อวาน

ไม่มี -- รอบนี้ไม่มีของเกมเพลย์ใหม่ให้ปล่อย สถานะที่ผู้เล่นเห็นในสนามเหมือนรอบก่อน
(`p0qia9`, บนสุดของ `main` ปัจจุบัน) เขียนตรงๆ แทนการเสกงานเทียม -- ดูเหตุผลด้านล่าง

## Protocol A -- round-lock recovery check

`claude/tender-goldberg-2d6pke` (this session's branch) and `origin/main` are both at
`7e2cbfde` -- no divergence. Round `p0qia9`'s payload (`tests/test_mob_ai_control.py`
`RegisterTests`, two new tests pinning `mob_of()`) is present on `origin/main` -> already
landed, nothing to recover from a merged=false PR.

One stray branch found: `origin/claude/tender-goldberg-3oo982` (2 commits, the old
`[LANE-B]` PR #380, closed **not** merged per round `p0qia9`'s own finding). Its single
real commit (`a463da57`, a one-line prose reword unsticking PR #363's gate2 test
allowlist) was already pushed directly onto the branch that became PR #363, which merged
at 2026-08-31T04:45:20Z (confirmed by round `3oo982`'s own round file and by
`PR_STATE.txt`, chief round `2idy5w`, 11:56+07:00). Nothing to cherry-pick; that branch
is a dead remnant of an already-superseded PR and was not touched (out of my write zone
to delete branches in any case).

This agent has no direct GitHub API access, so "no `[LANE-B]` PR is open right now" is
inferred from local git plus the chain of round files/`PR_STATE.txt`, not confirmed live
-- **asking the launching session to run `list_pull_requests(state=open)` on both repos
for `[LANE-B]` before opening this round's PR** (see handback letter).

## Protocol B -- mailbox triage

This round's own ticket, `20260831_1150_LANE-B-ASK-COO-round-lock-livelock-and-
build006-deadline.md`, now has two replies:

1. `20260831_1245_COO-DECISION-round-lock-livelock-fix-check-gate-before-ending-round.md`
   -- adopts options (ก)+(ข): a round that finds an already-open `[LANE-x]` PR at claim
   time must check that PR's gate before ending the round on lock-held alone, and chief
   adds the same check as a second-layer backstop during mailbox triage.
2. `20260831_1246_COO-DECISION-build006-m5-deadline-extended-pending-gt146.md` -- adopts
   option (ข): BUILD-006 has no new fixed date, it is "done when `GT-146` reports a
   result"; does **not** adopt guessing the opcode.

Both already carry a `.CONSUMED.txt` stub from chief round `52ogem`. Consuming them again
here because LANE-B opened the original ticket (addendum-B "who opens consumes"):

- Decision 1: applied this round. Protocol A above **is** the "check gate before ending
  round" behavior for the case where a `[LANE-B]` PR is already open at claim time -- none
  was found open, so there was nothing further to check. The behavior change for a future
  round: if Protocol A finds an OPEN `[LANE-B]` PR at claim time, that round must check
  its CI/gate status (via the launching session, since this agent has no GitHub API)
  before deciding "lock held -> end round" is the right call.
- Decision 2: no action requested of LANE-B beyond waiting. Confirmed `GT-146` is still
  `PENDING` at the head of the attended queue (`grep -n "GT-146" GAME_TEST_QUEUE.md` in
  `pf_bridge`, line 8233). Not re-asking, per the decision's own instruction. The decision
  also names its own escalation trigger ("more than two attended rounds pass with `GT-146`
  still not run -> file a COO-ESCALATION instead of waiting quietly") -- this agent, being
  unattended, has no visibility into how many attended rounds have run since 12:46+07:00;
  flagging that count needs tracking by whoever runs attended sessions or by chief, since
  LANE-B's own unattended rounds cannot observe it.

No other mail addressed to LANE-B lacks a `.CONSUMED.txt` stub this round (checked via
`grep -l "ADDRESSEE: LANE-B"` in `notes_to_chief/`, excluding `consumed/`).

## BUILD-004/5/6 -- reverified, no drift

`pytest tests -q`: **5708 passed, 323 skipped, 10238 subtests passed, 0 failed** (up from
5704/327 in round `p0qia9`'s baseline; normal count drift between clean-worktree runs,
no regression). BUILD-004 (MOBS-table monster spawn) and BUILD-005 (hit/damage/death/
corpse) unchanged, still exercised by the existing combat/mob-roster/loot-ledger suite.
BUILD-006 (M5 pickup persistence) unchanged from round `p0qia9`: the write path
(`mob_pickup_persist.py`) and the store insert are built and tested; the one remaining
piece is the `runtime.py` pickup-click opcode call site (chief's file), which needs
`GT-146` (attended, still `PENDING` at the queue head) to say what opcode the client
sends. Unattended work cannot close this -- per COO-DECISION `20260831_1246`, guessing
the opcode is explicitly declined, not just untried.

## ค้นแล้วไม่พบ (เขียนไว้แทนความเงียบ)

Repeated the zero-call-site sweep round `p0qia9` started (the technique that caught
`MobAiRegister.mob_of()`), extended to the four modules that round explicitly flagged as
"light or no attention": `mob_pickup.py` (25 public defs), `mob_death.py` (29),
`mob_diag_multi_object.py` (7), `field_mob_tables_bg0015.py` (0 top-level defs). Every
public def in all four has at least one reference elsewhere in `src/` or `tests/` --
**zero new zero-call-site findings this round.** Also grepped `TODO`/`FIXME`/`XXX:`
across `mob_*.py`/`field_mob*.py`/`loot_*.py`/`trade_session*.py` -- none found. Checked
`CLIENT_RE_QUEUE.md`/`GAME_TEST_QUEUE.md` for an OPEN, unclaimed, STATIC-answerable
ticket inside LANE-B's combat/mob/loot/pickup domain -- none found (`RE-135` is
`tools/`, outside LANE-B's write zone; `RE-137`/`RE-138`/`RE-149`-`RE-169` range are
either lane A/GM domain or already closed).

Per addendum F: this is a second non-gameplay round in a row content-wise (round
`p0qia9` also closed only a small test-debt item), but neither round is "empty" by the
rule's own test -- both closed a real, named, verified item (this round: mailbox
consumption + a documented negative search + a reverified baseline; not nothing, just
not a player-visible change). Not manufacturing busywork where a genuine search found
none available.

## ตัวเลขที่วัดได้

`pytest tests -q`: 5708 passed, 323 skipped, 10238 subtests passed, 0 failed.
`git diff --check`: silent. `current/pf_login_game_server_v141.py`: untouched this round
(no write, so no hash check needed). No canonical DB touched. Files touched this round:
3 in `pirate-force-server` (this file, its `_CLAIM.md`, and `rounds/` only -- no `src/`
or `scenarios/` changes), 1 letter in `pf_bridge/notes_to_chief/`.

## ยังไม่ได้พิสูจน์

BUILD-006's final `runtime.py` wire, pending `GT-146` (attended, human-at-client only).
How many attended rounds have run since the 12:46+07:00 COO decision (needed to know
when the two-round escalation trigger fires).

## CORE-REQUEST

None this round.

## เปิดใบให้สาย C

None.

## nonclaim

No `runtime.py`/`app.py`/`current/pf_login_game_server_v141.py`/`scenarios/world_*.json`
touched. No gameplay milestone claimed. This round's only changes are `rounds/` files
(this repo) and one status letter (`pf_bridge`) -- no `src/` or `scenarios/combat_*.json`
diff.

-- LANE-B (COMBAT) round `2d6pke`
