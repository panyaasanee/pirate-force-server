# LANE-B round p0qia9 (COMBAT)

Opened 2026-08-31T12:39+07:00. Branches: `claude/tender-goldberg-p0qia9`
(pirate-force-server), `claude/wizardly-gauss-p0qia9` (pf_bridge).

## ผู้เล่นจะเห็นอะไรต่างจากเมื่อวาน

ไม่มี -- รอบนี้เป็นรอบตรวจสอบ/ปิดหนี้เทคนิค ไม่ใช่รอบเกมเพลย์ใหม่ สถานะที่ผู้เล่นเห็นในสนามไม่
เปลี่ยนจากรอบก่อน (`3oo982`/`#380`).

## Protocol A -- round-lock recovery check

`pirate-force-server` PR #380 (`[LANE-B]`, closed 2026-08-31T04:59:33Z) itself closed
`merged=false`, but `pull_request_read get` on it confirms its actual payload was
pushed directly onto the lock-holding branch of PR #363, which merged at
2026-08-31T04:45:20Z. `list_pull_requests(state=open)` on both repos at round-open
time showed no open `[LANE-B]` PR (only `[LANE-A]` #383 / #593). Nothing to recover
from main.

## Protocol B -- mailbox triage

No unconsumed mail addressed to `LANE-B` since `FROM_CHIEF_R256_TO_LANE-B` (already
has a `.CONSUMED.txt` stub from a prior round). LANE-B's own `20260831_1150_ASK-COO`
letter (round-lock livelock + BUILD-006 deadline) is still awaiting a COO decision --
nothing to consume yet, no new reply landed before this round's mailbox check.

## BUILD-004/5/6 -- reverified, no drift

Same state as round `3oo982`: BUILD-004/005 built and exercised by the existing
combat/mob-roster/loot-ledger test suite; **BUILD-006 (M5 pickup persistence,
deadline 2026-08-31 12:00) has now missed its deadline.** Write path
(`mob_pickup_persist.py`) and the store insert are built and tested. The one
remaining piece is a `runtime.py` call-site wire (chief's file) that needs the
pickup-click opcode, which only `GT-146` (attended, still `PENDING` at the head of
the attended queue per `GAME_TEST_QUEUE.md`) can capture. Unattended work cannot
close this; already flagged to the owner in `3oo982`/`#380`, unchanged this round.

## Technical debt closed this round

`pf-adversary` pass over lane-B's combat/mob/loot modules (scope: everything
combat/mob/loot/drop/spawn/member/roster/aggro/pickup/ledger-related under
`src/pirateforce_foundation/`, cross-referenced against every test file) found one
concrete, safe item: `MobAiRegister.mob_of()` in `mob_ai_control.py:557-558` had
**zero call sites** anywhere in `src/` or `tests/`, unlike its sibling accessor
`state_of()` (~18 assertions across two test files). Correct code, but untested
public API on a class this module's own docstring calls "frozen, load-bearing" --
exactly the kind of thing that ships broken under time pressure if a future round
starts calling it without a test behind it.

Fixed by pinning it rather than deleting it (it mirrors `state_of`'s shape and
there is no evidence it is meant to go away): two new tests in
`tests/test_mob_ai_control.py` (`RegisterTests`) --
`test_mob_of_returns_the_row_s_own_monster` and
`test_mob_of_refuses_an_untracked_identity_the_same_way_state_of_does`.

No other defect surfaced in the modules reviewed
(`mob_aggro.py`, `mob_combat_membership.py`, `mob_ledger_admission.py`,
`mob_pickup_persist.py`, `mob_scene_recompose.py`, `trade_session_membership.py`,
`loot_roll.py` partial) -- several headline claims in their docstrings were
independently re-derived against live code and held. Not an exhaustive pass:
`mob_death.py` (2767 lines), `mob_pickup.py` (2018 lines),
`mob_diag_multi_object.py`, `mob_census_wire_count.py`,
`field_mob_tables_bg0015.py` got light or no attention this round.

## ตัวเลขที่วัดได้

Full non-client `pytest tests -q`: 5704 passed, 327 skipped, 10238 subtests passed
(up from 5642/387 in the adversary's clean-worktree baseline run earlier this round --
the two new tests plus normal count drift). `git diff --check` silent.
`current/pf_login_game_server_v141.py` untouched this round (no write at all, so no
hash check needed). No canonical DB touched.

## ยังไม่ได้พิสูจน์

BUILD-006's final wire, pending `GT-146` (attended, human-at-client only).

## CORE-REQUEST

None this round (nothing new to hand chief -- the outstanding one is
`GT-146`-gated and already on record).

## เปิดใบให้สาย C

None.

## nonclaim

No `runtime.py`/`app.py`/`current/pf_login_game_server_v141.py`/`scenarios/world_*.json`
touched. No gameplay milestone claimed. This round's only src-adjacent change is a
test-coverage addition (`tests/test_mob_ai_control.py`), not a behavior change.

-- LANE-B (COMBAT) round `p0qia9`
