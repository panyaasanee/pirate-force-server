# LANE-B round `jqxe6v` (COMBAT)

Opened 2026-08-31T20:37+07:00 (scheduled, no human watching). Branches:
`claude/beautiful-carson-jqxe6v` (pirate-force-server, this repo),
`claude/modest-wright-jqxe6v` (pf_bridge).

## Tools actually available this session

No `mcp__github__*` tool in the function list (Read/Grep/Glob/Bash/Edit/Write
only). Used `curl -H "Authorization: Bearer $GITHUB_TOKEN"` against the
GitHub REST API directly (confirmed `GET /user` returns `panyaasanee`, HTTP
200). No Agent/Task tool to invoke a `pf-adversary` subagent -- did a
self-review pass instead (see "pf-adversary" section in the status letter).

## Protocol A/B/C/D (ADDENDUM v2)

**A**: checked both repos' most-recent CLOSED `[LANE-B]` PR at round start --
`pirate-force-server#407` and `pf_bridge#625` both `merged=true`. Nothing to
recover from main.

**B**: `ADDRESSEE: LANE-B` letters with no `.CONSUMED.txt`:
`20260831_1900_INDEX-LANE-B-scene14-tier1-unlock-coo-decision-1648.md` and
`20260831_2007_LANE-A-TO-LANE-B-scene14-hostile-splice-design-proposal-re092.md`.
Both consumed this round (stubs + consumed/ copies + a reply letter to lane
A). No `CLIENT_RE_QUEUE.md`/`GAME_TEST_QUEUE.md` header opened by LANE-B
needed updating this round.

**C**: heartbeat at round start was `2026-08-31T20:32:02+07:00`; this round's
own clock read `2026-08-31T20:37+07:00` at start and `2026-08-31T21:01+07:00`
at write time -- both well under 60 minutes' drift.

## What this round built

`COO-DECISION 2026-08-31T16:48+07:00` unlocked "layer 1": lane B may import
`field_mob_tables_bg0015` under `src/pirateforce_foundation/`, now that lane
A's login-entry door for scene 14 merged and `GT-134` measured a monster
there on a real client. Reused that unlock plus lane A's own design letter
(`20260831_2007_...re092.md`) to build the actual thing rather than just
answer:

- `src/pirateforce_foundation/field_mob_hostile_bg0015.py` (new): the ONE
  approved importer of `field_mob_tables_bg0015`. Exposes
  `scene14_hostile_roster()`, `scene14_hostile_overrides(legacy, ...)` (the
  `dict[actor_identity, hostile_entry_bytes]` chief's future `runtime.py`
  branch needs), and `scene14_civilian_then_hostile_splice_proof(legacy)` --
  an end-to-end proof against `mob_scene_recompose.splice_identity_override`
  built from a synthetic civilian census, since the real `runtime.py` call
  site does not exist yet. Reuses `field_mobs.hostile_actor_entry` unmodified
  (no second encoder) and `FieldMob.actor_identity`'s existing formula (no
  new identity rule). Does NOT touch `field_mobs._SCENE_TABLE_MODULES` /
  `live_scenes()` -- Bg0015 is still not a loadable scene through
  `load_roster()`; that is layer 2/3, chief's `runtime.py:7501` CORE-REQUEST,
  deliberately left alone this round.
- `tests/test_field_mob_hostile_bg0015.py` (new): 11 tests, including one
  that cross-checks this module's actor-identity output against lane A's own
  independently-measured 12 numbers, and one that drives the real splice
  function and diffs civilian-vs-hostile bytes.
- `tests/test_field_mob_tables_bg0015.py`: renamed and rewrote the guard
  `test_nothing_under_src_imports_the_bg0015_module` ->
  `test_only_the_approved_hostile_composer_imports_the_bg0015_module`.
  Still refuses every OTHER importer (including `field_mobs.py` itself),
  now allowlists exactly the one new module. Every other test in the file
  (byte-identity of the generated table, bg0001-untouched pin, etc.)
  unchanged.
- Updated four other pinned-census test files
  (`test_field_mobs.py`, `test_mob_stat_fabrication_guard.py`,
  `test_npc_gait_wire.py`) plus the static-verifier tool
  (`tools/pf_runtimeres_actor_entry_static.py`), its bridge-only test
  (`tests/test_runtimeres_actor_entry_static.py`) and its report
  (`reports/PF_RUNTIMERES_ACTOR_ENTRY001_STATIC_20260819.md`) to account for
  the new module building one more actor entry / one more actor-stream call
  site -- all found by running the full suite, not by anticipating them.

## Numbers

```
tests/test_field_mob_hostile_bg0015.py : new, 11 tests, all pass
Full suite (pytest tests -q), run twice:
  before fixing 4 pinned-census drifts this new module caused (intentional,
  see below):
    14 failed, 5934 passed, 323 skipped, 11979 subtests passed (156.33s)
  after fixing all four:
    0 failed, 5938 passed, 323 skipped, 11989 subtests passed (159.57s)
git diff --check: silent
Files touched (pirate-force-server), 11 total:
  field_mob_hostile_bg0015.py [new]
  tests/test_field_mob_hostile_bg0015.py [new]
  tests/test_field_mob_tables_bg0015.py
  tests/test_field_mob_tables_bg0002.py
  tests/test_field_mobs_single_scene_guard.py
  tests/test_field_mobs.py
  tests/test_mob_stat_fabrication_guard.py
  tests/test_npc_gait_wire.py
  tools/pf_runtimeres_actor_entry_static.py
  tests/test_runtimeres_actor_entry_static.py
  reports/PF_RUNTIMERES_ACTOR_ENTRY001_STATIC_20260819.md
  rounds/B_20260831_2050_jqxe6v.md [new, this file]
```

## Not proven

- No live wire proof (no player sees this): `runtime.py` has no call site
  for this module yet -- that is chief's layer-2 CORE-REQUEST, still pending
  lane A's confirmation of the joint design.
- BUILD-006 (pickup persistence) final wire still waits on `GT-146`
  (attended, human-only) -- unchanged from prior rounds.

## pf-adversary (self-review, no subagent tool this session)

1. Checked the new guard test still refuses a SECOND stray importer (not just
   zero): added `field_mobs.py` explicitly to the "what still must not
   import it" list in the test's own docstring and verified by hand that
   `field_mobs.py` genuinely does not reference the new table.
2. Checked `scene14_hostile_overrides` fails closed on an unknown placement
   index rather than silently shipping fewer entries (pinned by
   `test_an_unknown_placement_index_is_refused_by_name`).
3. Checked the splice proof actually changes bytes (not a same-bytes pass):
   `test_splice_proof_changes_exactly_the_twelve_identities_and_nothing_else`
   diffs civilian vs. spliced entry bytes per identity, not just dict keys.
4. Re-ran the full suite after every pin fix rather than trusting the
   targeted subset stayed green in isolation.
