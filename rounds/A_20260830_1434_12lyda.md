# LANE-A round 12lyda

2026-08-30T14:27+07:00 - 2026-08-30T14:4x+07:00.

## What this round did

Section A: `git pull`/`git fetch` in both repos, confirmed `origin/main`'s tip in both is really
the merge of LANE-A's previous round (`pirate-force-server#311`, `pf_bridge#494`, both
"round re156-answer") -- merge-base of this branch and `origin/main` equalled `origin/main`'s
tip in both repos, no divergence, no recovery needed.

Section B (mailbox): consumed 11 `notes_to_chief` letters addressed directly to LANE-A
(`ถึง: LANE-A`), all part of the 2026-08-30T13:51 COO-DECISION batch. Two required action:

1. `COO-DECISION-cline-anchor-bar-cleared-gt131-pass` -- owner confirmed the bg0001 CLINE
   identity crosswalk in person via `GT-131` PASS ("ตำแหน่ง npc ถูก ตัวถูกต้อง ฉันให้เทสนี้ผ่าน").
   Struck (not deleted) the `[LANE-A ASSUMPTION - AWAITING COO/OWNER CONFIRMATION]` label on the
   seven-anchor-bar paragraph in `src/pirateforce_foundation/world_port_royal_identity.py`,
   following the same strike-through convention already used in `world_scene_marker.py`. Docstring
   only, zero behavior change -- 10/10 existing tests still pass.
2. `COO-DECISION-m2-destination-held-at-17-escalated-to-owner` -- COO assigned LANE-A to run the
   observational test proposed in `20260829_1410_LANE-A-ASK-COO-var2-is-a-markerid.md` (does scene
   126 at `MARKER[17]`'s coordinates actually turn the player into a ship on the sea, as the owner
   remembered). This needs a human at the client to judge by eye, so it became `GT-159` in
   `GAME_TEST_QUEUE.md` (pf_bridge) rather than a code change -- number reserved after a 0-hit grep
   for both `GT-159`/`RE-159`.

The other nine letters were pure ratifications/no-action items; each got a `.CONSUMED.txt` stub
and a copy under `notes_to_chief/consumed/`.

Also acted on `COO-DECISION-ten-doors-runtime-gate-confirmed`: read `scene_admission_gate.py`
(chief's R236 gate) and confirmed its own documented scope claim -- it disarms exactly ONE of the
five actor-composing lines (the frozen v141 legacy branch), while the other four
(`world_population`, `world_population_bg0002`, `world_population_bg0015`,
`field_mobs.roster_for_scene_id`) already self-restrict by `scene_id`. Measured
`field_mobs.roster_for_scene_id` for all ten still-shut doors (scenes 3,4,5,6,7,8,9,10,11,130):
0 rows for every one -- no composer exists for any of them yet. Pulled native placement counts
from `scenarios/world_scene_registry_001.json` for all ten and sent a recommendation (not a
decision) back to COO/owner in
`pf_bridge/notes_to_chief/20260830_1434_LANE-A-STATUS-r236-gate-verified-narrow-plus-door-priority-recommendation.md`:
scene 4 (Slave Market Island, Bg0004) has the most native placements (116) and already appears in
`world_m2_sea_destination`'s Q_TELEPORT1 sibling table, but building its composer is multi-round
work and this lane does not know whether LANE-B already has plans there, so it did not start
building and asked for a priority call first.

Re-verified BUILD-001 (108/115 actor census, per the already-ratified data ceiling) and BUILD-002
(scene 278 / Bg1177 registry entry) are both still present on current `main` with no regression --
`grep` for `BUILD-001`/`BUILD-002` markers across `src/pirateforce_foundation/` still finds the
same modules (`world_population.py`, `world_scene_travel.py`, `scene_admission_gate.py`, etc.), no
rebuild needed.

## What shipped in `src/`

One docstring edit (`world_port_royal_identity.py`, strike-through of a resolved COO assumption
tag) -- no behavior change, no new bytes on the wire, all existing tests still green.

## Player-visible claim

**None.** This round was mailbox consumption, one gate-scope verification, one queue-test
authorship (`GT-159`), and one label removal that does not touch runtime behavior. No player will
see anything different from yesterday.

## What's blocked / waiting

- `GT-134` (Bg0015 first-eyes), `GT-148`, `GT-151` (partial), `RE-155`, `GT-158`, `GT-159` (opened
  this round) -- all attended, all waiting on a human at the game client.
- Which of the ten shut doors to build a composer for first -- data sent to COO/owner this round,
  no decision made by this lane (deliberately, to avoid wasted work if another door is picked).

## CORE-REQUEST

None this round.

## ASK-COO

None new this round (used the existing `var2-is-a-markerid` letter's own option 3 instead of
opening a new one).
