# LANE-A round h1utu5

2026-08-31T12:27+07:00 - 2026-08-31T12:46+07:00 (+07:00 throughout).

## SUPERSEDED, 2026-08-31T13:0x+07:00 (orchestrator, same round's PR finalization)

**The Bg0004 crosswalk/composer built below was independently duplicated by a concurrent
round.** `git merge-base --is-ancestor` shows round `6p22bu` (server commit `e555666`,
2026-08-30T08:58:59Z) landed the same identity crosswalk on `main` about an hour after this
round's branch was reset from `main`, i.e. genuinely in parallel with this round, not a history
mistake on this round's part. `6p22bu`'s version resolves more (109/116 shippable vs this
round's 84/116, 55 Mob-Sets vs this round's coverage), and two further rounds already built on
top of it before this round finished: `2jdde8` wired the census into dispatch, `bq4mst` opened
scene 4's `login_entry_allowed` door on `main`. Scene 4 is live on `main` as of this merge; this
round's copies of `world_bg0004_identity.py` / `world_population_bg0004.py` / their tests were
**dropped** in favor of `main`'s versions during this round's merge-conflict resolution — nothing
from this round's own crosswalk/composer landed. Kept below, struck nowhere (the work itself was
real and independently correct per pf-adversary, just redundant), as the honest record of what
this round actually built and why none of it shipped. See
`notes_to_chief/20260831_13xx_LANE-A-STATUS-h1utu5-duplicate-work-collision.md` for the process
question raised to chief/COO about the round-lock race that let two Lane-A sessions pick the
same target.

## Section A - PR merge check (orchestrator pre-verified)

Previous LANE-A round `qoj8ei` merged cleanly on both repos (server PR #379, bridge PR #589,
`merged=true` on both). Nothing to recover or cherry-pick this round.

## Section B - mailbox

Live discovery: grepped `notes_to_chief/` for `ADDRESSEE: LANE-A` and for filenames prefixed
`LANE-A-*` with no `.CONSUMED.txt` sibling and not already under `consumed/`. One false positive
(`20260830_1136_LANE-A-STATUS-choose-npc-scene14-flipped-mailbox-clean.md` matches the string
`ADDRESSEE: LANE-A` in its own body text discussing the mailbox convention, but its real header is
`ADDRESSEE: LANE-E` -- it is a letter LANE-A SENT to chief, not one addressed to LANE-A). No real
unconsumed mail this round. Read the two most recent letters in the box regardless
(`20260830_1450_PANYA-ORDER-*`, `20260830_1509_KA1A-MEASURED-GT132-*`) to confirm neither is
addressed to LANE-A -- both are `ADDRESSEE: LANE-B (...), chief (...)`, unrelated to this lane's
scope (drop-persistence ordering and the Bg0002 combat-wall re-check).

## Section C - timestamp sanity

`TZ=Asia/Bangkok date` = `2026-08-31T12:27+07:00` at round start. `_BRIDGE_HEARTBEAT.txt` carries a
single stale line, `2026-08-30T14:58:02+07:00`, ~21.5 hours behind -- the heartbeat file itself has
not been updated since round `6yjio0`/R242 rather than this round's clock being wrong (the
`currentDate` context for this session independently states 2026-08-31, and every other letter in
the box from today's date range agrees with the `h1utu5`-round clock). Noted rather than silently
worked around.

## Section F - BUILD-001/BUILD-002 status check

BUILD-001 (M1, 108/115 actor ceiling) is closed at the wire/DB layer per `COO-DECISION
2026-08-29T19:41+07:00` and `GT-131` PASS (owner-confirmed 2026-08-30). No open work there.

BUILD-002's live frontier is `COO-DECISION 2026-08-30T14:41+07:00`: build a CLINE->MOBS identity
crosswalk for scene 4 (Slave Market Island, `BG0004`) -- the first of ten already-safety-checked
shut doors, picked for its native placement count (116, highest of the ten). Checked LANE-B's
recent letters/git log for any conflicting claim on scene 4 first (per the decision's own
instruction): `RE-117` (NPCATTR-LEVEL-MP-BIT-001) is the only RE ticket that was ever provisionally
numbered 117 before renumbering to a different topic, and it is about NPCAttr bit semantics, not
scene 4 specifically -- no conflict found.

## What this round built

Two new `src/` modules, following the exact pattern `world_bg0015_identity.py` /
`world_population_bg0015.py` already ship for scene 14, applied to scene 4 instead:

- `src/pirateforce_foundation/world_bg0004_identity.py` -- the identity half. Joins
  `gamedata/scene/bg0004/bg0004.placements.tsv` against `CONSTDATA_TH__CLINE.tsv` (type 4, read
  DIRECTLY off `SCENE_NAME.n_CLINE_TYPE` -- not inferred by key-set matching, since BG0004 is one
  of the 19 direct-selector scenes `world_bg0015_identity` already counted), then `MOBS`,
  `MOBS_TIP` and `STANDARD_MOB` by the same chain BUILD-001 and M3 already use. 47 of 61 CLINE
  type-4 rows resolve to a real shippable identity; 14 do not (empty outfit, no MOBS row, or no
  MOBS_TIP name). Of the scene's 116 real placements, 84 resolve (32 do not -- Mob-Set 107 alone,
  a 25-placement cluster, accounts for most of the loss because its leader has no `MOBS_TIP` name
  at all). Two placements (indices 82/83) carry a Mob-Set number in their raw `name` field that
  disagrees with their `template_ids` column; this module ships the `template_ids` reading
  (project convention: that column is authoritative) and records the disagreement rather than
  silently picking one. One shipped name (`"Orc Chief "`) carries a real trailing space in the
  source `MOBS_TIP` data -- shipped verbatim, not stripped. `_self_check()` runs at import and
  refuses a drifted table the same way the sibling modules do.
- `src/pirateforce_foundation/world_population_bg0004.py` -- the census half. Reuses the exact
  frozen serializer chain (`legacy.make_npc_attr` / `make_remote_movement_attr` /
  `make_remote_actor_entry` / `make_runtime_remote_actors`) and the wire constants imported from
  `world_population`, unchanged. No faction/hostile bit on any entry (that is lane B's splice, not
  this module's, matching every prior census in this tree). Prints `WORLD_CENSUS_BG0004
  assembled=84/116` with a per-actor and per-drop console line, so a headless boot can be checked
  before anyone opens the client.

Both modules are `production_allowed = True, test_only = False` (no scenario flag) but **nothing
under `src/` imports either one yet** -- confirmed by an AST-walk test
(`test_nothing_under_src_imports_this_module_yet` in the new population test file) that fails the
day a future round wires it, deliberately, the same tripwire pattern `world_population_bg0015.py`'s
own test used before ITS wiring round. `COO-DECISION 2026-08-30T14:41+07:00` says explicitly not to
flip scene 4's `login_entry_allowed` until the composer is ready and reviewed; this round's first
draft, with no attended eyes on it yet, is not that "ready" in the sense that decision means, so
this round does not ask for the door to open (see CORE-REQUEST below for the smallest next step).

## Tests

`tests/test_world_bg0004_identity.py` (14 tests) and `tests/test_world_population_bg0004.py` (13
tests), 27 tests total, all pass. Full repo suite re-run after the change: 5485 passed, 327
skipped, 0 failed (up from a 5458/327/0 baseline, measured directly on `main` in this checkout
after a pf-adversary pass caught the original write-up's arithmetic not reconciling with "27
tests total" -- 5458 + 27 = 5485, checked).

## Static verifier re-pin (mechanical, done in the same commit per the project's own instructions)

Adding `world_population_bg0004.py`'s one `make_remote_actor_entry` call and one
`make_runtime_remote_actors` call moved three numbers `tests/test_static_verifier_pins_cloud.py`
checks against three separate copies:
`src_actor_entry_call_sites` 17->18, `src_actor_stream_call_sites` 26->27,
`src_modules_building_actor_entries` 16->17 (module name added to the sorted tuple). Re-pinned in
all three places that file's own docstring instructs ("change the guard and the name tuple in the
verifier, the `RUNTIMERES_COUNTS` block in the report, and the assertions in the bridge-only test
module, all in the same commit"): `tools/pf_runtimeres_actor_entry_static.py`,
`reports/PF_RUNTIMERES_ACTOR_ENTRY001_STATIC_20260819.md`,
`tests/test_runtimeres_actor_entry_static.py`. These three files sit outside this lane's normal
`src/`/`scenarios/`/`rounds/`/own-tests write zone; touched only because leaving them stale would
have left the shared suite red from this round's own change, and the project's own drift-prevention
doc names this exact repair as mechanical and expected of any lane that adds an actor-entry call
site. Flagged here rather than done silently.

Also found and fixed one word-boundary hit in the new identity module against
`tests/test_npc_interaction_wire.py::QuestAndShopStateGuardTests` (foundation modules may not
contain the whole word "quest") -- a docstring sentence used "quest-guide character name", reworded
to "guide/narrator character name" with no change in meaning.

## pf-adversary self-review (no subagent tool this round; checked by hand against the same
checklist categories the real pass uses)

- **Partial actor construction / silent truncation**: `_self_check()` asserts exact counts (47
  resolved, 14 unresolved, 116 placements, 84 shippable, 32 unshippable) at import time and raises
  if any drifts -- checked by deliberately truncating `_RESOLVED_ROWS` by one row in the test suite
  and confirming the refusal fires.
- **Off-by-one on any target**: the 116 native placement count is cross-checked against the
  registry's own `native_placement_count: 116` for `n_id: 4` in
  `scenarios/world_scene_registry_001.json` (already committed, not written by this round) -- both
  agree.
- **Encoding**: `tests/test_tree_is_cp874_safe.py` passes (393 subtests) after this round's four
  new files land; the scene's real Chinese/CJK name
  (`SCENE_NAME.s_SCENE_NAME`, hex `e5a5b4e99ab8e5b882e5a0b4e5b3b6`) is never quoted as a literal
  anywhere in `src/` -- only the ASCII name already pinned in the scene registry
  (`"Slave Market Island"`) is used.
- **Scope**: no edit to `runtime.py`, `app.py`, or `current/pf_login_game_server_v141.py`. The
  three shared-verifier files touched above are outside the nominal write zone and are called out
  explicitly rather than treated as in-scope by default.
- **Fabricated data**: every row in `_RESOLVED_ROWS` and `_PLACEMENT_ROWS` was read directly out of
  the six committed `gamedata/` tables on the bridge clone (digests recorded in `SOURCE_SHA256`);
  nothing was invented, and the two anomalies found (name/template_ids disagreement, trailing-space
  name) are reported rather than silently resolved.

## Player-visible claim

**None.** Neither new module is imported by anything under `src/` yet, `login_entry_allowed` for
scene 4 is unchanged (`false`), and no wire behavior on any currently-reachable scene changed. A
player who logs in today sees exactly what they saw yesterday.

## CORE-REQUEST

None this round -- no `runtime.py`/`app.py` change requested. The next step (wiring scene 4's
census into the per-scene dispatch point, the same shape `lane_a_scene_census.py` already does for
scenes 2 and 14) is a follow-up round's work, not a one-line ask; noted in the handback letter
rather than requested here.

## ASK-COO

None new. `GT-160` opened in `GAME_TEST_QUEUE.md` (see below) records the open client-observable
question for when the door does open; no decision is needed from COO this round.
