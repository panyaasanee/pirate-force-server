# LANE-A round `czoo9t`

2026-08-30T21:4x+07:00 - 2026-08-30T22:0x+07:00 (+07:00 via `TZ=Asia/Bangkok date`).
Full record with the adversary pass, the mailbox pass and the gate table:
`pf_bridge/rounds/A_20260830_2148_czoo9t_m2_crossing_handoff_composed_not_sent.md`.

**ผู้เล่นจะเห็นอะไรต่างจากเมื่อวาน:** ยังไม่เห็นอะไรต่างบนจอ - แต่ตอนนี้คอนโซลบอกตรง ๆ ทุกครั้งที่มี
คนขึ้นเรือ Columbus ว่า Port Royal ทั้งเมือง 115 คนตามออกทะเลไปด้วย และเฟรมที่ไล่พวกเขากลับ
(clear 27 ไบต์) ประกอบเสร็จรออยู่แล้ว เหลือแค่ `runtime.py` คิวมัน

## Why this is not the cherry-pick round

PR #332 was closed by the gate-reaper without merging. Recovery needs Lane GM's fixture fix on
main first. Re-checked from a fresh `git fetch origin main` this round, not taken on trust:
`origin/main` is still `aa5ba78`; `git log origin/main -- tests/test_gm_login_scene_sanctioned_bypass_wiring.py`
still ends at `95d322f`; the commit to recover (`b5ca2b68716b1786244448c5ee4651f5bb8e4905`) is
still intact on `origin/claude/sleepy-ride-kpz6vo`. Deferred a second time, so this round did
real work instead of standing idle.

Numbers, so the deferral is a decision and not caution: HEAD *without* row 126 runs
**5568 passed / 327 skipped / 0 failed**; PR #332's own commit message records **20 failed /
5514 passed** *with* it.

## What this round built

The one scene change a player can make happen on a default boot - Columbus, row 3021, scene 17 -
sends a `TeleportVital` and **nothing else**. `world_population_handoff.handoff_on_crossing` is
wired into `runtime.py` at one call site and it is the disabled travel-gate path (`:7146`); the
Columbus branch (`:4971-5044`) never calls it. `make_runtime_remote_actors` replaces rather than
merges, so the login census - **115 actors**, re-derived this round via
`world_population.census_count_for_dispatch() -> (115, 'full_census')` - is still on the client
after the crossing. `RE-162` Job 4 records the same gap independently.

`src/pirateforce_foundation/world_m2_crossing_handoff.py` (new) feeds the `SceneEntry` the
dispatch already produces to the seam that already ships. No second selector and no second
encoder. Measured for scene 17: `kind=clear`, `pc=17B`, `frame=27B`, `slot=before_teleport`,
`membership_reset.clears_everything=True`, `frame == legacy.frame_pc(pc)` against the real frozen
v141. `dispatch_columbus_quest3021` now prints, on the default path with no flag:

```
WORLD_M2_CROSSING_HANDOFF scene=17 kind=clear held=115 composed=YES dispatched=NO
  pc=17B frame=27B slot=before_teleport reason=scene_17_left_empty_on_purpose_...
```

The bytes are composed and go nowhere: queueing them lives in `runtime.py`, which is the chief's
file. `crossing_handoff_dispatched=` is the one token that flips `dispatched=NO` in the same edit
that makes it untrue. Both states are driven by tests.

## Why the sea gets a clear and not a roster

Measured before proposing anything: `Bg1001.placements.tsv` has 8 placements and **all 8** are
`Mob_set_N` (sets 1-6) - the identity reading `GT-078` REJECTED. `CONSTDATA_TH__SCENE_NAME.tsv`
row 17 carries `n_CLINE_TYPE = 4294967295` (0xFFFFFFFF, none), so the Mob-Set -> CLINE -> MOBS
crosswalk has no key to travel on. Checked, not assumed: all seven sea scenes (17-23,
`Bg1001`-`Bg1007`) carry the same value. **A composer for the sea would be an invented cast.**
Scene 17 therefore joined `SCENES_INTENTIONALLY_UNPOPULATED` with the measurement written into
the table - the first row there with a production reader.

## Gate

`pytest tests -q`: **5568 passed, 327 skipped, 0 failed**, 9716 subtests.
`verify_hypothesis_ledger.py`: `PASS entries=47`. `verify_functional_coverage.py`: rc=0.
`current/pf_login_game_server_v141.py`: untouched. `git diff --check`: silent. Both new paths
come back NOT ignored. All six touched files are pure 7-bit ASCII and cp874-clean.

Two tripwires updated deliberately, each argued in the test file rather than re-pinned: the
bg0015 seam call-site census (third file, compose-only) and `test_columbus_quest_dispatch`'s
negative-index line order (moved by one, third time, still pinned by index not `assertIn`).

## Files touched (6)

* `src/pirateforce_foundation/world_m2_crossing_handoff.py` - NEW
* `src/pirateforce_foundation/columbus_quest_dispatch.py` - +1 import, +1 emit, +1 kwarg
* `src/pirateforce_foundation/world_population_handoff.py` - scene 17 entry + measurement
* `tests/test_world_m2_crossing_handoff.py` - NEW, 23 tests
* `tests/test_columbus_quest_dispatch.py` - line-order pin
* `tests/test_world_population_bg0015.py` - call-site census

`runtime.py` and `app.py` not touched. CORE-REQUEST:
`pf_bridge/notes_to_chief/20260830_2148_LANE-A-CORE-REQUEST-columbus-crossing-owes-a-population-handoff.md`.

## Not proven

Nobody has watched a client render scene 17 (`GT-106` PENDING; `RE-162`: in-session transition
client-observable UNPROVEN). Whether the client drops the old scene's actors when a clear lands
mid-session is a separate observable and needs its own attended ticket. This round sets no ticket
status, writes no PASS, and declares no milestone reached.
