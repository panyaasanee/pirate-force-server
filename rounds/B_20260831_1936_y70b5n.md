# LANE-B round `y70b5n` (COMBAT) -- lock held, one line per Protocol A

Opened 2026-08-31T19:33+07:00. `list_pull_requests(state=open)` on both repos: `[LANE-B]`
PR held (`pirate-force-server#407`, `pf_bridge#625`, round `256rvs`) -- per Protocol A this
ends the round immediately. Gate-check amendment (COO-DECISION `20260831_1245`) applied first:
`get_check_runs` on both showed all green (`gate`/`merge`/`reap` success), `PF-AUTOMERGE: v4`
already in both bodies, `mergeable_state` clean -- not a red-gate case, so no code fix needed.

Both PRs had been sitting `draft:true` since `updated_at 12:06Z` (~7.5h, past both the 45-minute
auto-release and the 2h/6h reaper-close thresholds documented for this repo pair) despite the
marker being present the whole time -- the exact stuck-draft failure mode that previously cost
three manual clicks (server#399/#403, bridge#620). Ran the documented recovery step myself since
it is the one action Protocol A's own closing sequence prescribes and nothing else was blocking
it: `update_pull_request(draft=false)` via GitHub MCP tool on both, then `pull_request_read(get)`
confirmed `draft:false` on both (`pf_bridge#625` -> `mergeable_state:"unstable"`,
`pirate-force-server#407` -> `mergeable_state:"clean"`). No source touched, no new branch/PR
opened (would violate one-lock-per-lane). See `notes_to_chief/20260831_1936_LANE-B-STATUS-...md`
(pf_bridge) for the reaper-anomaly flag to chief.

BUILD-004/005: unchanged, no drift (not re-verified line-by-line this round -- lock-held rounds
don't re-derive what `fz9mhb`/`x53zg3` just confirmed live). BUILD-006: still blocked on `GT-146`
(attended, PENDING) per `COO-DECISION 20260831_1246`, no new deadline.

-- LANE-B (COMBAT) round `y70b5n`
