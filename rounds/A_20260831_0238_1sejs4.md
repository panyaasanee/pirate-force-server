# LANE-A round `1sejs4`

2026-08-31T02:38+07:00 (`TZ=Asia/Bangkok date`).

## Mailbox

Checked every `pf_bridge/notes_to_chief/*.md` for an `ADDRESSEE: LANE-A` /
`ADDRESSEE: สาย A` header without a matching `.CONSUMED.txt` stub. Zero
matches for that header pattern in the mailbox at all (the addressing
convention this round's brief pointed at is not how letters in this mailbox
are tagged). Cross-checked by filename instead: every `LANE-A-*` file is this
lane's own outbound ASK-COO/STATUS/RE letter (addressed to chief/COO for
consumption, not to this lane), and the newest inbound item, `FROM_CHIEF_
R252_TO_ATTENDED_20260831_0059.md`, states the mailbox was fully cleared (11
letters stubbed, zero backlog) and no `src/` change landed that round. Nothing
found this lane owes a response to. Heartbeat at check time:
`2026-08-31T02:14:21+07:00 HEAD 3e1f204`, 24 minutes old at time of writing,
within the 60-minute rule.

## BUILD-001 / BUILD-002 re-verify

Targeted regression only (fourth independent confirmation after `e2q8c6`,
`mr5agz`, `i95a1z`): `tests/test_world_population.py
tests/test_world_port_royal_identity.py tests/test_columbus_quest_dispatch.py
tests/test_columbus_quest_dispatch_wiring.py` -> **115 passed, 0 failed**.
Zero diff, same conclusion as the prior three rounds; not re-litigated in
full here.

## M2 exploration this round (the real question this round asked)

The brief's standing addendum (`M2 ขั้นถัดไป -- sea map / becoming a ship,
independent of identity`) is dated 2026-08-27/28 and was explicitly
superseded in-tree once already (round `iyhrj0`, "ADDENDUM v2 item F ...
overridden by [the M2-pause] decision"), then un-paused and built out across
many rounds since. So this round's job was to check whether that build-out is
actually finished, not to re-open a closed question.

**Method**: the same shape round `i95a1z` used to find the sea-destination
gap -- grep every `def ..._console_line` / `def console_line` function under
`src/pirateforce_foundation/*.py` and check whether anything besides its own
test file calls it.

**Result: one hit, and it is not a gap.**
`world_scene_entry.relocation_console_line` has no caller outside its own
tests. Read in full: its own docstring says exactly why --
`resolve_entry` has *already* emitted the real line inline (`_relocated_line`
/ `_kept_row_line`, called at `world_scene_entry.py:445-446`, confirmed by
reading `resolve_entry`'s body directly); `relocation_console_line` is a
documented RE-COMPOSE helper for a caller or test that wants the string
without the side effect. Not a sleeping capability.

**The M2 wiring graph, re-read end to end rather than trusted from the
last letter**: `columbus_quest_dispatch.dispatch_columbus_quest3021` now
calls, in file order, on every default flagless dispatch: the no-vehicle
notice, the stowaway report, the return-leg report
(`world_m2_return_leg.return_leg_console_line`), the return-population
report (`world_m2_return_leg.return_population_console_line`), the
crossing-handoff report and frame queue
(`world_m2_crossing_handoff.crossing_handoff_console_line` +
`runtime.py`'s action-queue append, confirmed still present at
`runtime.py:5082-5133`), and the sea-destination report
(`world_m2_sea_destination.console_line_safe`). All five `world_m2_*`
reports this family has ever built are wired onto the default path today.

**The two things still genuinely open in this family, confirmed
un-buildable from source this round:**

1. `RE-077` -- what in-game client action starts a RETURN trip from scene
   17. Still unmeasured; no new committed-source evidence found. Already
   named as the standing gap in `world_m2_return_leg.py`'s own module
   docstring; not duplicated as a new ticket.
2. The `[CONTESTED]` var2-is-a-scene-id-vs-marker-id reading. Already
   escalated by COO straight to the owner
   (`pf_bridge/notes_to_chief/20260830_1351_COO-DECISION-m2-destination-
   held-at-17-escalated-to-owner.md`). Nothing for this lane to build until
   that ruling lands.

**Sea-scene census, checked separately and re-derived rather than quoted**:
scenes 17-23 (Bg1001-Bg1007) are already closed as genuinely unresolvable
from committed data (`world_population_handoff.SCENES_INTENTIONALLY_
UNPOPULATED[17]`). Re-ran the measurement this round instead of trusting the
comment: `awk` over `gamedata/tables/CONSTDATA_TH__SCENE_NAME.tsv` (bridge
repo) confirms rows 17-23 all carry `n_CLINE_TYPE = 4294967295`
(0xFFFFFFFF) -- no crosswalk column exists to resolve a Mob-Set placement
into an actor identity for any of the seven. A composer here would be an
invented cast, which this lane's own standing rule forbids.

**Open tickets addressed to this lane, checked in `CLIENT_RE_QUEUE.md`**:
`RE-149`, `RE-152`, `RE-156` are all CLOSED. `RE-155` (NPC name color,
green->yellow / Training Iron Man red) is the only OPEN item and its own
ticket text states static analysis has already hit BOUNDED-NEGATIVE three
times and it is explicitly `NEEDS-ATTENDED-CAPTURE` -- not resolvable by
writing more source without a human capture session.

Round `oprday` (2026-08-30T20:42+07:00) already landed the one remaining
non-identity item `CHIEF-DECISION R229` assigned this lane (the scene-126
registry row); confirmed present on this HEAD, not re-done.

## Conclusion: zero `src/` diff this round

Every thread in this lane's write zone that does not require identity
resolution (banned to guess by standing COO decisions), an attended capture
session (`RE-155`, `GT-134`/`GT-159`), or an owner ruling (var2, harbour) is
already built and merged. Writing that down rather than inventing scaffolding
to fill the round, per house rule against fabricated busy-work. This is the
first empty round after a build round (`i95a1z`), so the "no two empty rounds
in a row" rule is not yet in play.

## Player-visible change

None -- this round shipped no code. Everything a player can already see
today (108/115 Port Royal census, the Columbus -> scene 17 crossing with the
town clearing behind them, the five `WORLD_M2_*` console reports) was already
visible before this round started.

## Numbers

- Targeted BUILD-001/002 regression: 115 passed, 0 failed (unchanged).
- Full suite: `python3 -m pytest tests -q` -> **5608 passed, 323 skipped,
  9729 subtests passed, 0 failed** (was 5604 passed / 327 skipped at
  `i95a1z`'s reported HEAD, zero test-file commits between the two runs --
  the shift is an environment-conditional skip-marker swing, e.g.
  `@BRIDGE_GAMEDATA.skip_unless_present()`, not a code regression; flagged
  rather than silently normalized, not investigated further this round).
- `git diff --stat` on `src/ tools/ current/ scenarios/ tests/`: empty,
  before and after.

## Files touched

- `pirate-force-server`: `rounds/A_20260831_0238_1sejs4.md` (this file) --
  1 file, no `src/` change.
- `pf_bridge`: `notes_to_chief/20260831_0238_LANE-A-STATUS-m2-family-fully-
  wired-empty-build-round.md`, `rounds/A_20260831_0238_1sejs4_m2-fully-
  wired-empty-round.md` -- 2 files.

## Not proven by this round

Same standing list as `i95a1z`: no human has watched the M2 console lines
print on an attended run since `GT-106` walked scene 17 originally;
`RE-077`'s return trigger remains unmeasured; the var2 contest remains open
pending the owner.

## CORE-REQUEST

None.

## Tickets for lane C

None new -- `RE-077`'s gap and the var2 escalation are already open under
existing tickets/letters; not duplicating them.

-- LANE-A (WORLD) round `1sejs4`
