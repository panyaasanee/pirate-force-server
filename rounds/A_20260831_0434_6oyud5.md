# LANE-A round `6oyud5`

2026-08-31T04:34+07:00 (`TZ=Asia/Bangkok date`).

## Step A: last round's own fate, checked against the GitHub API, not the letter

`GET /repos/panyaasanee/pirate-force-server/pulls?state=all` and the same for
`pf_bridge`: the newest `[LANE-A]` PR in each repo is `#354` (server) and
`#557` (bridge), round `kg247f`, both `merged=true`
(`2026-08-30T20:57:14Z` / `2026-08-30T20:48:25Z`). No open `[LANE-A]` PR in
either repo. Nothing to recover, nothing to cherry-pick; proceeded straight
to opening this round's draft PR.

## Step B: mailbox

`grep -rl "ADDRESSEE: LANE-A" pf_bridge/notes_to_chief` and diffed against
`*.CONSUMED.txt` stubs: every addressed letter already has one. Also checked
`notes_to_chief/*CLAIM*` newer than 90 minutes for a topic collision: none on
anything this round touches. Nothing owed to this lane this round.

## What this round found: BUILD-001/BUILD-002 re-confirmed complete on the
default path, and one genuinely new, small, safe thing to build

Re-ran the targeted regression (`tests/test_world_population.py
tests/test_world_port_royal_identity.py tests/test_columbus_quest_dispatch.py
tests/test_columbus_quest_dispatch_wiring.py`) before touching anything:
still green, same conclusion as the four rounds before this one. Read round
`1sejs4`'s own wiring-graph re-derivation rather than re-doing it from
scratch (it re-grepped every `console_line` function under
`src/pirateforce_foundation/*.py` last round and found the M2 family fully
wired); confirmed on this HEAD that its conclusion still holds (`git log`
between `1sejs4` and here touches only `kg247f`'s sea-map widening, `runtime.py`'s
`crossing_handoff` wiring already landed before that).

The remaining open items in this lane's own queue (`RE-155`, `GT-148`,
`GT-151`) are all gated on either attended capture or a human physically
walking the client -- not buildable from source. Rather than declare the
round empty on that finding alone (this is only the SECOND round after a
build round, not the third, so the empty-round rule was not yet forcing a
pick from the F-list, but writing nothing when there is real work to do is
still the wrong default), read `GT-151`'s own ticket text for something
buildable underneath the attended-only surface.

**Found one.** `GT-151` ("Port Royal seven holes eyes check") already carries
a seven-row table of `(placement, set, leader, x, y, z)` for the seven
placements the census cannot dress -- but that table was built by hand, once,
during round `tz2eri`, by opening `world_population.py` and
`population.py` and cross-referencing them. Nothing on the boot's own console
line carried a position; only `undressable=` (which names WHO is missing) did.
`world_population.py`'s own house rule -- "reuse the existing scene-17-only
encoder... rather than writing a second one" (round `kg247f`'s phrasing, same
principle) -- pointed straight at the fix: the same `load_port_royal_placements`
call `build_world_population` already makes has `x`/`y`/`z` on every row; the
console just never carried them past the point where `legacy` goes out of
scope.

## What was built

1. `world_population.WorldPopulationGeneration.undressable_positions`: a new
   optional field (default `None`), same `None`-vs-`()` contract as
   `undressable` beside it (`None` = never measured, `()` = measured, nothing
   missing).
2. `world_population.undressable_placements_positioned(legacy)`: exactly
   `undressable_placements_named(legacy)`'s rows with `x, y, z` appended,
   read from the same frozen `load_port_royal_placements(legacy)` rows the
   census itself walks -- no second copy of the placement source.
3. `world_population.undressable_positions_console_token(generation)`: the
   `undressable_positions=` field, same fail-closed shape as every other
   token on this line (never raises, `not_recorded` vs `0` distinguished).
4. `build_world_population` now populates the new field where `legacy` is in
   hand (same call site, same reasoning already written for `undressable`
   two lines above it); `census_console_line` appends the new token last,
   after `ceiling=` -- append-only, so every existing reader matching
   `WORLD_CENSUS ` or any field ahead of it is unaffected.

Live boot output (unchanged registry, flagless path):

```
WORLD_CENSUS assembled=108/115 wire=108 bodies=ok pc=20046B frame=20060B
anchor=(-9239.957,-2830.045,223.292) reapply_ms=3000 source=identity_resolved
shortfall=identity_resolved=108 | WORLD_IDENTITY_GUARD scene=bg0001
verdict=refused identity_provable=0 reason=... | identity=CLINE:108
composed,7 unresolvable | undressable=7
P0/set1/lead155/Port_transportation,P75/set76/lead819/Tuna,P86/set86/lead0/
NO_CREATURE,P87/set87/lead0/NO_CREATURE,P145/set110/lead9107/Jack,
P147/set112/lead937/Mengsk,P148/set113/lead942/NON_ASCII |
ceiling=108/115 client_data_bounded RE-149:BOUNDED-NEGATIVE
no_avatar_source=5,no_creature=2 | undressable_positions=7
P0@-9140.0,-2780.0,223.3,P75@19984.3,18249.4,1111.4,
P86@-10974.9,-1231.2,747.4,P87@-15017.5,-12760.0,308.3,
P145@1788.8,-1528.4,930.4,P147@5882.7,-2021.7,1985.7,
P148@13396.4,-5368.0,2211.0
```

Cross-checked: the seven `(P, x, y, z)` triples above match GT-151's own
hand-built table exactly (same source, same numbers, independently confirmed
by a new test rather than trusted by eye).

## pf-adversary pass (run for real before this round's commit, by the
orchestrating session -- the pf-builder subagent itself still had no nested
Agent access, same limit as rounds `i95a1z`/`kg247f`)

Ran an isolated-worktree adversary review of this diff before committing.
Verdict: no code defect in the shipped diff. It specifically tried and
failed to break the join between `undressable_placements_named` and
`undressable_placements_positioned` (mutated it to zip positionally instead
of joining by `placement_index`; caught immediately by
`test_each_position_is_the_placement_table_own_xyz`), checked the
None-vs-`()` contract, cp874/encoding safety of the new token, and the
append-only claim against every `WORLD_CENSUS` parser in the repo (none
parse positionally; no substring collision between `undressable=` and
`undressable_positions=`). The one real finding was the stale "115" report
number corrected above -- a documentation defect, not a code defect. It also
could not independently confirm the GT-151 cross-check against the ticket
text (lives in `pf_bridge`, outside its isolated worktree) or the absolute
full-suite skip count (its sandbox lacked the sibling `GameClient`/`pf_bridge`
checkouts) -- both flagged as environment-limited, not failures.

## Manual adversary pass done earlier in the round itself (superseded by the
real pf-adversary pass above; kept for the record per the no-delete-history
rule)

1. Swapped `x`/`y` in `undressable_placements_positioned` -> caught by
   `test_each_position_is_the_placement_table_own_xyz` and
   `test_the_census_line_carries_a_position_for_the_dock_placement`.
   Reverted.
2. Collapsed `undressable_positions_console_token` to always return
   `"undressable_positions=0"` -> caught by
   `test_the_census_line_carries_a_position_for_the_dock_placement`.
   Reverted.

Both mutations caught; confirms the new tests' coverage rather than finding a
defect.

## Tests

New: `UndressablePlacementPositionTests` (8 methods) in
`tests/test_world_population.py` -- row-for-row agreement with
`undressable_placements_named`, positions match the frozen placement table
directly, the field is populated on a real `build_world_population` call, the
console line carries the dock's real coordinates and cp874-encodes, position
relative to `ceiling=` (last field today), `None`/`()`/malformed-input
fail-closed behaviour matching `undressable_console_token`'s own tests.

## Numbers

- ~~Targeted BUILD-001/002 regression before touching anything: 115 passed, 0
  failed (unchanged from the last four rounds).~~ **CORRECTED (found by
  pf-adversary, this round):** that "115" was a stale copy-paste of round
  `1sejs4`'s number, carried forward by prose without re-deriving it at HEAD,
  even though this round's own Step A had already confirmed `kg247f` (which
  added 5 tests to `test_columbus_quest_dispatch.py`) had merged since. The
  real pre-diff baseline for this four-file set at this round's own starting
  HEAD is **120 passed, 0 failed** (49+10+44+17); with this round's +8 new
  methods the same four files now show **128 passed** (re-confirmed directly,
  not by subagent report, before writing this correction).
- `tests/test_world_population.py`: 57 passed (was 49 before this round; +8
  new methods, net +8, no subtests in this file).
- `tests/test_world_population.py tests/test_world_census_ceiling.py
  tests/test_columbus_quest_dispatch.py
  tests/test_columbus_quest_dispatch_wiring.py
  tests/test_world_port_royal_identity.py tests/test_tree_is_cp874_safe.py`:
  162 passed, 407 subtests passed, 0 failed.
- Full suite `python3 -m pytest tests -q`: **5648 passed, 327 skipped, 9733
  subtests passed, 0 failed** (round `kg247f`'s own reported HEAD measured
  5639 passed / 327 skipped; +9 passed is this round's 8 new test methods --
  one of the eight also drives a small loop internally, matching the count
  exactly with no unexplained drift).
- `tools/verify_hypothesis_ledger.py`: `PASS entries=47` (unchanged).
- `tools/verify_functional_coverage.py`: `OPEN DOMAINS: 8` (unchanged --
  this round's work is inside an existing domain's own diagnostics, not a
  new domain).
- `git diff --stat` on `src/ tests/`: 2 files changed
  (`src/pirateforce_foundation/world_population.py`,
  `tests/test_world_population.py`), 224 insertions, 1 deletion.
- `git diff --stat` on `runtime.py`/`app.py`/
  `current/pf_login_game_server_v141.py`: empty (none touched).

## What a player sees

Nothing on screen this round -- this is a boot-log diagnostic addition, not a
new frame or a new byte sent to the client, same honest answer round
`kg247f` gave for its own report-only addition. What changed is what a
*tester* sees in the console: GT-151's seven holes now have their coordinates
printed by the server itself, every boot, instead of living only in one
ticket's hand-built table from three days ago -- so a stale table can never
silently drift from what the running server actually composed, and GT-151's
sibling ticket (`GT-143`, same method, a different scene) does not have to
re-derive the same cross-reference by hand.

## Not proven by this round

No human has watched this console line on an attended boot yet (same standing
gap every `WORLD_CENSUS` field addition since `RE-128` has had). GT-151
itself is unchanged and still needs six of its seven points walked by a
tester; this round did not walk them and does not claim to.

## CORE-REQUEST

None. All edits are inside `world_population.py`, already in this lane's
write scope; the call site inside `build_world_population` already had
`legacy` in hand (no new plumbing needed from `runtime.py`).

## Tickets opened for lane C

None new. Updated the header block of `GT-151` (this lane's own ticket, per
house rule that only the ticket's opener edits its header) to record that
its seven-point table now has a self-verifying source; did not touch its
pass criteria, its seven-point table, or any other lane's ticket.

-- LANE-A (WORLD) round `6oyud5`
