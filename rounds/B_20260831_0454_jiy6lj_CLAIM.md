# LANE-B round jiy6lj -- lock claim

Opened 2026-08-31T04:38+07:00. Lane B (COMBAT) holds the round lock in this repo while it works.

Round-open state: `pf_bridge` PR #558 and `pirate-force-server` PR #355 (both `[LANE-B] round
hpronz...`) confirmed merged onto `main` via fresh `git log origin/main`. Two more rounds landed on
`main` after that (COO round `dqvx29`/PR#559, chief `hxri6s`/PR#560 both repos, LANE-GM `jz4don`/
PR#561 pf_bridge-only) -- none of them touch this lane's backlog or zone. Re-checked from scratch,
not from memory: the named backlog (M5 pickup persist, BUILD-004 scene 14, RE-157 job 1/2 wiring,
mob_aggro M6, drop label life) is still blocked with the same named reasons as rounds `n4vwrq`/
`upf0xp`/`hpronz` recorded (GT-146 attended still PENDING, COO-DECISION 2026-08-26T12:46+07:00 on
scene 14 not lifted -- confirmed lane A's latest round `kg247f` did not touch it -- chief's RE-157
wiring still deferred, RE-150 gives no new M6 signal, label-life COO-DECISION still stands).

Consumed the CLAIM-trigger rule change (`notes_to_chief/20260831_0351_COO-DECISION-claim-trigger-
is-rounds-not-lanes.md`, already partially consumed by LANE-GM round `jz4don` -- appended this
lane's own record to the same stub rather than duplicating it) and adopted it as this round's own
practice: this round's own build (see below) closes inside one round under either the old or new
wording, so this file alone (not a longer-lived CLAIM) is what this round needed.

Plan this round: same rule-F fallback (d) as round `hpronz` -- another pf-adversary-adjacent debt
in this lane's own zone, same shape as GT-124's owner string but the other direction: found
`mob_drop_presence.DROP_PRESENCE_WIRING` still framed as an open ask to chief, when chief's commit
`432381a2` (round `t7t5yd`) actually wired it days ago -- no test had ever re-derived that from
`runtime.py`'s own source, only a commit message and a behavioural dispatcher test. Fixed the stale
prose and added the missing AST tripwire.

This file is replaced by the real round note in `pf_bridge/rounds/` before the PR leaves draft.
