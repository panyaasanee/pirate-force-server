# LANE-A round `n8fq3w`

Opened 2026-08-30T11:24+07:00, this account written 2026-08-30T11:36+07:00
(`TZ=Asia/Bangkok date`).
Lock: pf_bridge#(next) / pirate-force-server (this PR, opening now).

Previous round `e2q8c6` confirmed BUILD-001/BUILD-002 already shipped and
M2 formally paused, with zero source diff, and flagged one outstanding
CORE-REQUEST as the highest-leverage next unblock: a `runtime.py` guard for
scene 14's ChooseNPC responder. That guard landed on `main` in round
`hd6tac`/R237 (chief), with the flip itself explicitly left to this lane.
`e2q8c6` did not flip it (zero-diff round); one more zero-diff round would
have gone empty twice running, so this round does the flip chief's own
reply letter named as still outstanding.

## What changed

`src/pirateforce_foundation/lane_hooks/lane_a_choose_npc_scene14.py`:
`production_allowed` flips `False` -> `True`. Scene 14 (Hell Volcano
Island)'s login door is already open and defect D3 already closed from an
earlier round, neither depending on this flag; this flag only ever governed
whether a `ChooseNPC`/`TARGET_VITAL` click on one of scene 14's 81 composed
actors gets answered, never whether they are drawn. Before this flip, a
click on that scene got no wire response at all (no crash risk either --
the frozen per-actor loop never even runs while `population_indices` is
unarmed). After this flip, the same click is answered through the
`runtime.py` guard chief already wired, using the same frozen wire shapes
Port Royal already uses, sourced from scene 14's own placement table. Two
known gaps (v141 TARGET_VITAL arming skipped when a scene claims the frame;
multi-select answers only the first identity) are unchanged, already pinned
by tests, and explicitly left as this lane's own design judgement per
chief's own reply letter.

`src/pirateforce_foundation/lane_hooks/lane_a_scene_census.py`: two
docstring corrections describing the now-superseded closed gate and a
separately stale "scene 14 is not open to players" opening claim that
predated an earlier round opening the door and was never corrected there.
No behavior change in this file.

`tests/test_lane_a_choose_npc_scene14.py`: renamed and flipped the class
that pinned `production_allowed = False`; removed now-redundant/now-harmful
manual flag-forcing and `addCleanup` resets that would otherwise reset the
real shipped default back to `False` after running; converted the
"no responder" test from an undriven default into an explicit forced-absent
regression case so that path stays pinned even though scene 14 no longer
takes it by default. 22/22 tests pass; nothing deleted, history kept in
docstrings per this project's convention.

## Verification performed

`python3 -m unittest tests.test_lane_a_choose_npc_scene14` (22/22),
`test_lane_a_scene_census`/`test_lane_hooks`/`test_lane_scene_census_wiring`/
`test_world_lane_static` (69/69), full suite
`python3 -m unittest discover -s tests -p "test_*.py"` (5528 tests,
errors=18, all pre-existing `ModuleNotFoundError: capstone`, same shape as
`e2q8c6`'s 5508/18 baseline -- delta is unrelated commits between rounds,
not this diff), `tools/verify_hypothesis_ledger.py` (PASS entries=47),
`tools/verify_functional_coverage.py` (PASS domains=8), cp874
encodability of both touched `src/` files (OK), `git diff --check`
(silent). `runtime.py`, `app.py`, `current/pf_login_game_server_v141.py`
untouched.

pf-adversary was not available as a subagent tool this session (no
Task/launch tool, only Read/Grep/Glob/Bash/Edit/Write); I applied its
written checklist myself instead of skipping the step, and it caught a real
evidence-layer overclaim in my own first draft (a client-observable claim
with no `GT-134` result behind it) before this was written, corrected
before commit. Full account:
`pf_bridge/rounds/A_20260830_1136_n8fq3w_the-one-line-chief-left-for-you.md`.
Status letter:
`pf_bridge/notes_to_chief/20260830_1136_LANE-A-STATUS-choose-npc-scene14-flipped-mailbox-clean.md`.

## Mailbox

`ADDRESSEE: LANE-A` mail is clean (all consumed). One letter under the
`[ถึง: LANE-A ...]` header form was outstanding --
`20260830_1022_CHIEF-REPLY-CORE-REQUEST-choosenpc-scene-guard-wired.md` --
and is what this round acted on and consumed.

## Nonclaims

No client-observable evidence (no GameClient opened this round). Does not
touch M2, scene 278, or the walk-in travel gate. Does not change scene 14
reachability (still gated by a stored `character_positions` row naming 14,
or an account-scoped GM `/warp`).
