# LANE-A round re156-answer

Opened 2026-08-30T13:27+07:00.
Lock: pf_bridge (this round's PR) / pirate-force-server (this PR).

Checked last round's PR (`#307`/`pf_bridge#486`, `qp7brn`): confirmed
`merged: true` via `pull_request_read` (`get`, not the unreliable `list`
`merged` field). Mailbox: zero unconsumed letters genuinely addressed
`LANE-A`.

`qp7brn` closed as this project's first empty round under Rule F. This
round avoided a second empty round in a row by answering queue ticket
`RE-156 SCENE-IDENTITY-SIGNAL-001 [STATIC-ON-BRIDGE]` (opened by chief
round `hd6tac` after `qp7brn` had already started) from committed source
alone -- no attended session, no code change (the ticket's own objective
forbids one).

First draft of the `RE-156` answer wrongly concluded "no client->server
byte carries a scene number." `pf-adversary`'s review before commit caught
a real counter-example: `parse_action_vital`'s `field_u16_4a`
(`current/pf_login_game_server_v141.py:3250-3284`) is named/consumed as
`scene_id` by `src/pirateforce_foundation/action_ack.py:8-11,63` and
dispatched live in `src/pirateforce_foundation/runtime.py:247,6483-6501`
-- gated behind `--scene-load-scenario` (`app.py:98,287-288`), and outside
this lane's write zone (combat domain, `HYP-PF-002` frozen per
`docs/FUNCTIONAL_COVERAGE.json` / `tools/verify_hypothesis_ledger.py`).
The answer was corrected and re-verified by a second `pf-adversary` pass
before this commit. Whether that field tracks the client's live current
scene or a value baked per test scenario is unresolved and now tracked as
`GT-158` (attended, in-game) rather than answered here.

Zero diff under `src/ scenarios/ tools/ tests/` this round -- `RE-156` is a
static analysis answer, not a build; the correction itself also required
no code change. Full account in `pf_bridge`
`rounds/A_20260830_1327_re156-no-scene-carrying-client-byte-answered-static.md`.
Status letter: `pf_bridge/notes_to_chief/20260830_1327_LANE-A-STATUS-re156-answered-still-waiting-on-gt134-re155.md`.
RE-156 result: `pf_bridge/notes_to_chief/20260830_1327_RE-156-RESULT-no-scene-carrying-client-byte-teleport-check-echo-is-the-nearest-proxy.md`.

`GT-134` and `RE-155` remain the only two live Lane A threads and both still
need an attended human session; M2 remains paused directly by the owner.
