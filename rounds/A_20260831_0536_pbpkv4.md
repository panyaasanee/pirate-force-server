# LANE-A round `pbpkv4`

2026-08-31T05:36+07:00 (`TZ=Asia/Bangkok date`).

## Step A: last round's fate, checked against the GitHub API

`GET /repos/panyaasanee/pirate-force-server/pulls?state=all` and the same
for `pf_bridge`: the newest `[LANE-A]` PR in each repo is round `6oyud5`
(server) / `#563` (bridge), both `merged=true`. No open `[LANE-A]` PR in
either repo. Nothing to recover.

## Step B: mailbox

`grep -rl "ADDRESSEE: LANE-A" notes_to_chief/*.md` in `pf_bridge`, diffed
against `*.CONSUMED.txt` stubs: the single hit with no stub next to it
(`20260831_0434_LANE-A-STATUS-gt151-...md`) is round `6oyud5`'s own
outbound letter, and the match is prose *inside that letter quoting the
grep command itself*, not a real inbound addressee header -- confirmed by
reading the matched line directly (`grep -n "ADDRESSEE: LANE-A"` on that
one file returns line 16, which is a sentence describing this exact
mailbox-check step, not a header). Nothing owed to this lane this round.

## What this round found: BUILD-001/002 still fully wired, and a real,
## named coverage gap on an already-shipped player behaviour

Re-ran the targeted regression before touching anything (all M2/Columbus/
population files): 215 passed, 4 subtests passed, 0 failed. Consistent
with every round since `czoo9t`.

Rather than re-derive the wiring-graph conclusion from scratch a sixth
time, read the most recent CHIEF round instead (`R250`/`65etwo`,
`pf_bridge/rounds/R250_65etwo_columbus-crossing-handoff-wired-plus-cp874-
tool-cleanup.md`): chief wired this lane's CORE-REQUEST from round
`czoo9t` -- `world_m2_crossing_handoff.crossing_handoff()`'s CLEAR frame
for scene 17 is now actually QUEUED on the default Columbus 3021 path
(`runtime.py:5036`, `crossing_handoff_dispatched=True`), not merely
composed-and-printed. Confirmed still true at this round's HEAD by reading
`runtime.py:4990-5135` directly, not by trusting the letter.

That round's own report names its own gap in plain words: *"ไม่มีเทสไหน
assert บรรทัดคอนโซล/`dispatched=` ที่จุดรวมนี้โดยตรง ... นี่คือ 'false
green' ที่แท้จริง"* -- the join that actually sends bytes to the client
was verified once, by hand, during that round, and left with zero
permanent coverage on `main`. `grep -n "crossing_handoff_dispatched\|
dispatched=YES" tests/test_columbus_quest_dispatch_wiring.py` confirmed
this directly: zero hits before this round.

This is exactly the shape the task brief asks this lane to prefer over a
diagnostic-only round: real work, answerable from source today, closing a
gap on behaviour a player can already trigger on a flagless boot (talk to
Columbus, option 1, quest 3021) -- rather than opening a new build on top
of an unpinned join.

## What was built

`tests/test_columbus_quest_dispatch_wiring.py`: one new class,
`CrossingHandoffQueuedWiringTests` (3 methods), driven through the SAME
real harness (`runtime.make_state_class`, no double) every other class in
this file already uses. No `src/` edit this round -- there was nothing to
build, only something chief already shipped to pin down.

1. `test_the_crossing_handoff_frame_is_queued_before_the_teleport_frame` --
   recomputes the expected handoff through this lane's own public
   functions (`columbus_quest_dispatch.resolve_columbus_arrival` +
   `world_m2_crossing_handoff.crossing_handoff`, no second, hand-guessed
   encoder), then asserts the real `actions` list returned by
   `state.dispatch(...)` contains that exact `(label, pc, frame)` triple
   AHEAD of `CORE_REQUEST_014_COLUMBUS_Q3021_TELEPORT_SCENE17_ONCE`
   (`dispatch_slot == SLOT_BEFORE_TELEPORT`).
2. `test_the_console_line_says_dispatched_yes_exactly_once` -- asserts
   exactly one `WORLD_M2_CROSSING_HANDOFF` line on stdout, reading
   `dispatched=YES` (not `NO`), and that the same line is recorded in
   `state.events` (the `e0daaa` print+record convention).
3. `test_a_successful_crossing_clears_the_frozen_membership_fields` --
   `state.population_indices`/`state.world_census_indices`, armed
   non-`None` by the harness's own TargetPos frame, must read back `None`
   after a successful crossing (`MembershipReset.clears_everything` for a
   CLEAR handoff).

## Manual adversary pass (no subagent tool available in this environment,
## same limit every LANE-A round since `i95a1z` has reported)

Forced three mutations directly against `runtime.py` in a scratch copy,
reverted after each check, `git diff --stat` confirmed empty before and
after:

1. `crossing_handoff_dispatched=True` -> `False` at the one call site --
   `test_the_console_line_says_dispatched_yes_exactly_once` fails
   (`dispatched=NO` instead of `YES`). Reverted.
2. `if handoff.sends_a_frame:` -> `if False and handoff.sends_a_frame:`
   (neutralises the `handoff_actions` splice) --
   `test_the_crossing_handoff_frame_is_queued_before_the_teleport_frame`
   fails (`WORLD_POP_HANDOFF_CLEAR_SCENE_17` missing from `actions`
   entirely). Reverted.
3. Commented out `self.population_indices = reset.population_indices` --
   `test_a_successful_crossing_clears_the_frozen_membership_fields` fails
   (`population_indices` still non-`None` after the crossing). Reverted.

All three caught. `runtime.py` diff confirmed empty (`git diff --stat --
src/pirateforce_foundation/runtime.py`) after restoring from the scratch
backup.

## Gate, measured

| check | result |
|---|---|
| targeted (M2/Columbus/population files, 7 files) | 215 passed, 4 subtests, 0 failed |
| `python3 -m pytest tests -q` | 5661 passed, 327 skipped, 9758 subtests, 0 failed (baseline at this HEAD without this round's diff: 5658 passed, 9758 subtests -- +3 is this round's 3 new methods, exact) |
| `tools/verify_hypothesis_ledger.py` | `PASS entries=47` (unchanged) |
| `tools/verify_functional_coverage.py` | `OPEN DOMAINS: 8` (unchanged) |
| `tests/test_tree_is_cp874_safe.py` | 5 passed, 407 subtests (this round's new strings scanned clean) |
| `git diff --stat` on `src/` | empty (no `src/` edit this round) |
| `git diff --stat` on `tests/` | 1 file, 172 insertions |
| `git diff --stat` on `runtime.py`/`app.py`/`current/pf_login_game_server_v141.py` | empty (none touched) |

## What a player sees

Nothing new this round -- the behaviour this round pins down (the crossing
handoff CLEAR frame actually being queued ahead of the teleport when a
player takes Columbus's quest 3021) was already shipped by chief's round
`R250`/`65etwo`, before this round started. What changed is that a future
regression to that join (reverting the dispatch flag, or the action-order
splice, or the membership reset) will now fail a test on `main` instead of
silently reverting to the pre-`R250` behaviour (a discarded clear frame)
with the full suite staying green.

## Not proven by this round

No human has watched a client render scene 17 with the clear frame queued
-- `GT-148`'s client-observable layer is still `PENDING`, unchanged by this
round; this round only pins the wire/DB layer chief's own round already
updated `GT-148`'s criteria to describe.

## CORE-REQUEST

None. All edits are inside `tests/`, already in this lane's write scope;
no `src/`, `runtime.py`, or `app.py` change was needed.

## Tickets opened for lane C

None. This round answered nothing that needed a new RE -- it pinned an
existing, already-shipped join with new tests, driven from source and the
real harness.

-- LANE-A (WORLD) round `pbpkv4`
