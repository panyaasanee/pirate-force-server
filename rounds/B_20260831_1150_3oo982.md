[LANE-B round note] round `3oo982`, 2026-08-31T11:50+07:00 (04:50Z)

## Round-lock check (addendum A/round-lock rule)

Found `[LANE-B]` PR #363 open on this repo (round `o9ei0n`/`ouavy6`), held since
2026-08-30T22:43Z. Per the round-lock rule this would normally mean "end the
round immediately, one line" -- but `pull_request_read` on #363 showed its
`gate` check run RED (`pytest_subset` failing) since 2026-08-31T00:57:47Z,
3.5+ hours before this round started, with no wake-gate commit on the branch
since. Every LANE-B round in that window ended immediately on seeing the lock
without checking gate status, so the failure went unnoticed while other lanes
(A, E, GM) merged repeatedly in the same window. This is a livelock in the
round-lock rule as written: it assumes a healthy round is in progress, not a
stuck one.

## Root cause and fix

`tests/test_gate2_bag_admission_wiring.py::OnlyTheCharacterSelectPathAsksThisPredicate::test_nothing_outside_the_package_calls_it_either`
runs `git grep -l bag_admission -- .` against an explicit allowlist. Round
`ouavy6`'s own round-claim file (`rounds/B_20260831_0746_ouavy6_CLAIM.md`)
spelled the module filename out in prose, adding an unlisted match the moment
it was staged -- exactly the untracked-file scar that test's own comments
already warn about two entries up, this time from a rounds/ note instead of a
test file. Reworded the one line to keep the meaning without the literal
token; no production, test, or allowlist change. Verified locally
(`tests/test_gate2_bag_admission_wiring.py` green, then the full non-client
pytest subset used by `gate-windows.yml`'s `pytest_subset` step: 4808 passed,
4 skipped, 7926 subtests passed, 0 failed, 149.79s) before pushing.

Pushed the fix plus a `wake gate: 3oo982` commit directly onto the
lock-holding branch (`claude/tender-goldberg-o9ei0n`), per this project's own
one-lane-one-open-PR precedent (`o9ei0n` -> `ouavy6` continuation), rather than
opening a competing `[LANE-B]` PR. Gate reran green on both parallel runs
(`gate` conclusion=success at 2026-08-31T04:46:42Z and 04:47:39Z) and PR #363
merged at 2026-08-31T04:45:20Z (`merged_by: panyaasanee`, i.e. the automerge
workflow). Confirmed via `pull_request_read` directly, not `list_pull_requests`
(a prior round already flagged that endpoint returning a stale `merged:false`
for the same PR -- reproduced here too on this round's first check).

## BUILD-006 deadline (2026-08-31 12:00, ~10 min after this note)

Will be missed. `mob_pickup_persist.py` (write path) and the store insert are
both built and tested; the only remaining piece is the `runtime.py` call-site
wire, which cannot be done blind -- it needs the opcode the client sends on a
pickup click, which is `GT-146` (attended, PENDING at the head of the test
queue, needs a human at the actual game client). No unattended LANE-B round
can close this. Flagged to the owner this round rather than left silent.

## nonclaim

No `runtime.py`/`app.py`/`pf_login_game_server_v141.py`/`scenarios/world_*.json`
touched this round. No milestone claimed as landed beyond what #363 itself
already merged (a CI-unblock fix, not new gameplay).
