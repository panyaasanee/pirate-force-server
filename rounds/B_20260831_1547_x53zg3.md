# LANE-B round x53zg3 (COMBAT)

Opened 2026-08-31T15:47+07:00. Branches: `claude/tender-goldberg-c2jhc9`
(pirate-force-server, PR #395), `claude/wizardly-gauss-c2jhc9` (pf_bridge,
PR #610).

## ผู้เล่นจะเห็นอะไรต่างจากเมื่อวาน

**ไม่มี.** รอบนี้ไม่แตะ `src/`, `scenarios/`, `runtime.py`, `app.py` เลย -- เป็นรอบตรวจสอบสด
(ไม่ใช่จำจากจดหมายรอบก่อน) บวกการสืบเส้นทาง BUILD-004 ฉาก 14 (Bg0015) ให้ครบสามชั้นบล็อกจริง
เพื่อไม่ให้รอบถัดไปต้องไล่ซ้ำ

## เครื่องมือที่มีจริงในรอบนี้ (สำคัญกับ orchestrator)

ไม่มี `mcp__github__*` tool และไม่มี Agent/Task tool ให้เรียก subagent `pf-adversary`/
`pf-queue-author` ตรง ๆ ในเซสชันนี้ (สอดคล้องกับที่รอบ `n4vwrq`/`upf0xp` บันทึกไว้แล้ว) -- ใช้
`curl -H "Authorization: Bearer $GITHUB_TOKEN"` ยิง GitHub REST API ตรงแทนสำหรับดู/เปิด PR ทั้งหมด
(ยืนยันว่าใช้ได้จริง: `GET /user` คืน `panyaasanee`, HTTP 200) และทำ self-review เชิง adversarial ด้วย
ตัวเองแทน `pf-adversary` (ดูหัวข้อท้ายใบ)

## Protocol A -- round-lock check (live API, ไม่ใช่อนุมานจาก git)

`GET /repos/panyaasanee/pirate-force-server/pulls?state=open` และ
`GET /repos/panyaasanee/pf_bridge/pulls?state=open` ตอนเริ่มรอบ (15:4x+07:00): มีแค่
`pirate-force-server#394` (`[LANE-A]`) เปิดอยู่ -- ไม่มี `[LANE-B]` ค้าง เปิด draft ใหม่ทันที:
`pirate-force-server#395`, `pf_bridge#610`

Local branch `claude/tender-goldberg-c2jhc9` ตามหลัง `origin/main` 4 commit (LANE-GM รอบ `xxsulh`,
`docs/GM_LANE.md` เท่านั้น) -- fast-forward สำเร็จ ไม่มี conflict `claude/wizardly-gauss-c2jhc9`
เท่ากับ `origin/main` อยู่แล้ว

## Protocol B -- mailbox triage

ไล่ `notes_to_chief/*.md` หา `ADDRESSEE: LANE-B` (ไม่รวมใบที่ LANE-B เป็นผู้ส่งเอง ซึ่งไม่มี
`ADDRESSEE: LANE-B` เป็นผู้รับ) ที่ยังไม่มี `.CONSUMED.txt`: **0 ใบ** ใบล่าสุดที่มี `ADDRESSEE`
เกี่ยวกับสาย B ทั้งหมด (`FROM_CHIEF_R256_TO_LANE-B`, `COO-DECISION 20260831_1245/1246` ที่ตอบ
`20260831_1150_LANE-B-ASK-COO`) มี `.CONSUMED.txt` ครบแล้วจากรอบก่อน ใบเดียวที่ยังไม่มี stub
(`20260831_1244_COO-DECISION-attr-wire-shelved...`) จ่าหน้า `ADDRESSEE: LANE-GM` ไม่ใช่ของสายนี้
ตรวจแล้วไม่ใช่ของค้าง

## BUILD-004/005/006 -- ตรวจซ้ำจากซอร์สสด ไม่ใช่จากจดหมาย

- **BUILD-005** (ตี/ตาย/ศพ): wired จริง `runtime.py:4503`/`:4767` (อ้างจากจดหมาย `q6r3te` เดิม,
  ไม่ได้ re-grep เลขบรรทัดซ้ำรอบนี้ -- ไม่มีการเปลี่ยนแปลงที่ทำให้ต้องสงสัย) ไม่ drift
- **BUILD-004** ฉากที่ live วันนี้ (bg0001/Port Royal, Bg0002/Prison Exile): ยัง wired ผ่าน
  `field_mobs._SCENE_TABLE_MODULES` + `mob_census_hostility.hostile_override_for_scene_id` ที่
  `runtime.py:7501` (READ-ONLY, ยืนยันแล้วว่ายังเป็น one call site จริง) ไม่ drift
- **BUILD-004 ฉาก 14 (Bg0015/Hell Volcano)** -- สืบสามชั้นบล็อกให้ครบ (จดหมายเก่าพูดถึงแค่ชั้น a):
  1. **นโยบาย + เกทบังคับจริง**: `COO-DECISION 2026-08-26T12:46+07:00` สั่งห้าม import
     `field_mob_tables_bg0015` (โมดูลที่มีข้อมูลมอนสเตอร์มายด์ครบ 12 hostile placements แล้ว)
     ที่ไหนใต้ `src/pirateforce_foundation/` จนกว่า "lane A's second travel gate and
     geometry/reachability check" จะผ่าน -- เกทนี้เดินจริงด้วย
     `tests/test_field_mob_tables_bg0015.py::test_nothing_under_src_imports_the_bg0015_module`
     (AST walk + literal-string sweep ทั้งไฟล์) ยังไม่ถูกยกเลิก (ยืนยันจากซอร์สสด: `field_mobs.py`
     ยังคง comment เดิมทุกตัวอักษร ไม่มี `field_mob_tables_bg0015` ปรากฏที่ไหนใต้ `src/`)
  2. **แม้เกท (1) หมดอายุ**: `runtime.py`'s census/hostility dispatch ที่ `:7501` ยังเป็น
     if/elif ต่อฉาก (bg0001 branch, bg0002 branch แยกกัน มี log suffix `_bg0002` ระบุไว้ในคอมเมนต์
     ว่า "การันตีว่าแตกกิ่งนี้ compose แค่ฉาก 2") ฉาก 14 ต้องมีกิ่งใหม่ในไฟล์นี้ -- เป็นของ chief
     ไม่ใช่จุดที่สาย B ต่อเองได้แม้เกทนโยบายเปิด
  3. **ชนกับสาย A**: `world_population_bg0015.py` (สาย A) ส่ง 12 placement เดียวกัน
     (22,24,27,29,31,44,45,46,47,51,70,87) เป็น actor เป็นกลาง (ไม่มี faction bit) อยู่แล้ววันนี้
     -- โมดูลนั้นเขียนไว้เองว่า "Making them hostile is lane B's splice... A round that wants
     both should land this one first" (ฉบับนี้ landed แล้ว) การเปิด roster ของสาย B แบบลอย ๆ
     (ไม่ผ่าน override-in-place เหมือน bg0001/bg0002) จะส่งสอง collection ที่คำนวณ
     `actor_identity` เดียวกัน = ซ้ำ hazard `RE-092` (replace-by-omission) ที่ทั้งโปรเจกต์เพิ่งกันไว้

  **สรุป: ไม่ใช่แค่ "รอ COO ยกเลิกป้าย"** -- ต่อให้ยกเลิกป้ายวันนี้ ก็ยังเหลืองาน chief (ข้อ 2)
  และงาน splice ข้ามสาย (ข้อ 3) ก่อนผู้เล่นจะเห็นมอนสเตอร์ตัวแดงในฉาก 14 จริง ไม่ใช่งานที่ปลดได้
  ด้วยการแก้ไฟล์เดียวของสาย B

- **BUILD-006** (เก็บของ): บล็อกจุดเดียวเหมือนเดิม -- `GT-146` (attended, ยัง `PENDING` ที่หัวคิว
  `GAME_TEST_QUEUE.md:33`, ยืนยันสดรอบนี้) ตาม `COO-DECISION 20260831_1246` ไม่มีเดดไลน์ใหม่ ผูก
  กับผล `GT-146` ตรง ๆ ไม่เดาโอปโค้ด

## ข้อสังเกตใหม่ที่ยังไม่มีใครเขียนไว้ตรง ๆ -- ส่งเป็นคำถามให้ COO ไม่ใช่คำตอบ

`world_travel_gate.py`'s docstring (สาย A, อ่านอย่างเดียวรอบนี้) บันทึกว่า **"COO RULING 20260826"**
(วันเดียวกับ `COO-DECISION 2026-08-26T12:46+07:00` ที่ล็อก Bg0015 ไว้) สั่งถอนกลไก "เดินแล้วหยุด"
(`world_travel_gate` เอง) ออกจากเกณฑ์ยอมรับ M2 **ถาวร** ("never use it as an M2 acceptance
criterion again") และตั้งเป็น debug-only/ปิดโดย default ตลอดไป

ถ้า "lane A's second travel gate" ที่ล็อก Bg0015 หมายถึงกลไกตัวเดียวกันนี้ เงื่อนไขปลดล็อก
Bg0015 อาจ**ไม่มีทางผ่านได้อีกแล้วโดยการออกแบบ** (กลไกที่ต้อง "ผ่าน" ถูกสั่งปิดถาวรไปแล้ว) -- **นี่
เป็นข้อสังเกตจากการอ่านสองเอกสารคนละสาย ไม่ใช่ข้อสรุป**: อาจเป็นคนละเกทกับ "second travel gate"
ที่ COO-DECISION 12:46 ตั้งใจพูดถึงจริง (เช่น ประตูเรือ/ Columbus ของ BUILD-002 ที่ยังไม่เสร็จ) ไม่มี
หลักฐานในเอกสารทั้งสองฝั่งที่ผูกชื่อ "second travel gate" เข้ากับกลไกใดกลไกหนึ่งชัดเจน -- ส่งเป็น
คำถามให้ COO ยืนยันในจดหมายรอบนี้ (`pf_bridge/notes_to_chief/`) ไม่เดาแทน

## กวาดหา RE ที่เปิดค้างในเขตสาย B (combat/mob/loot/pickup)

`CLIENT_RE_QUEUE.md`: ใบ OPEN ที่เหลือทั้งหมด (`RE-155`, `RE-167`, `RE-168`, `RE-170`) เป็นเขต
LANE-A/RE-runner (scene identity, census abort, dialogue UI, level-control median) ไม่มีใบไหน
อยู่ในเขต combat/mob/loot/pickup ของสายนี้ -- ไม่มีของให้หยิบ

## ตัวเลขที่วัดได้

```
pytest tests -q (pirate-force-server, HEAD ก่อนรอบนี้ = ก็คือ HEAD หลังรอบนี้ เพราะไม่มี src diff):
  5740 passed, 323 skipped, 10606 subtests passed, 0 failed (215.78s)
  -- เท่ากับตัวเลขที่รอบ p3olrt บันทึกไว้เป๊ะ (5740/323/10606) ยืนยันว่าไม่มีอะไรเปลี่ยนที่ HEAD
     ระหว่างสองรอบ
git diff --check: silent
ไฟล์ที่แตะรอบนี้ -- pirate-force-server: 2 (rounds/B_20260831_1547_x53zg3_CLAIM.md, ไฟล์นี้)
ไฟล์ที่แตะรอบนี้ -- pf_bridge: 3 (rounds/B_20260831_1547_x53zg3_CLAIM.md, จดหมายสถานะ, stub
  .CONSUMED ถ้ามี -- ไม่มีใบให้บริโภครอบนี้ จึงไม่มี stub ใหม่)
```

## ยังไม่ได้พิสูจน์

- BUILD-006 การ wire สุดท้าย รอ `GT-146` (attended)
- ว่า "lane A's second travel gate" ที่ COO-DECISION 12:46 อ้างถึง คือกลไกเดียวกับ
  `world_travel_gate.py` ที่ถูกถอนออกจากเกณฑ์ M2 ถาวรแล้วหรือไม่ -- รอ COO ยืนยัน
- ว่า splice หลอมโหมด hostile เข้ากับ census ที่มีอยู่ของ Bg0015 (ข้อ 3 ด้านบน) ต้องออกแบบอย่างไร
  ให้ไม่ชน RE-092 -- ยังไม่มีใครออกแบบจริง เป็นงานของสาย B ในวันที่เกทเปิด

## CORE-REQUEST

ไม่มีรอบนี้ -- ยังไม่ส่งคำขอ "เปิดกิ่งฉาก 14 ใน `runtime.py`" เพราะเกทนโยบายชั้นที่ 1 (COO-DECISION
2026-08-26T12:46+07:00) ยังไม่ถูกยกเลิก ส่งตอนนี้จะเป็นการขอให้ chief เตรียมของที่ยังใช้ไม่ได้จริง

## เปิดใบให้สาย C

ไม่มี

## nonclaim

ไม่แตะ `runtime.py`/`app.py`/`current/pf_login_game_server_v141.py`/`scenarios/world_*.json`/
`scenarios/combat_*.json` เลยรอบนี้ ไม่มีการนำเข้า (`import`) `field_mob_tables_bg0015` ที่ไหน
ใต้ `src/` -- ตรวจด้วย `grep -rn "field_mob_tables_bg0015" src/ --include="*.py"` = **0 hit**
(ศูนย์แม้แต่ในไฟล์ `field_mob_tables_bg0015.py` เอง -- โมดูลไม่ต้องพิมพ์ชื่อไฟล์ตัวเองเป็นสตริง)
`grep -rn` แบบไม่จำกัดนามสกุลเจอหนึ่งจุด: `src/pirateforce_foundation/__pycache__/
field_mob_tables_bg0015.cpython-311.pyc` -- ไฟล์ bytecode cache ที่ build เอง ไม่ใช่ source และ
ไม่ผ่านตากวาดของ guard test (ซึ่งกวาดเฉพาะ `.py`) ไม่ได้แก้ตัวเองรอบนี้ ไม่มีการอ้างสถานะ PASS/FAIL
ของใบเทสใด ๆ

-- LANE-B (COMBAT) round `x53zg3`
