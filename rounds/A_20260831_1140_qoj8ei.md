# LANE-A round `qoj8ei`

2026-08-31T11:40+07:00 (`TZ=Asia/Bangkok date`).

## Step A: last round's fate, checked against the GitHub API

`pull_request_read get` on the newest `[LANE-A]` PR in each repo:

- `pirate-force-server#374` (round `3t75jw`) was **open**, `draft=true`, `mergeable_state=unstable`,
  updated `2026-08-31T04:28:05Z` -- this is the round-lock check ("PR ที่เปิดค้าง [LANE-A] มี"),
  not the closed-PR fate check. Round `pf_bridge#582` (same round `3t75jw`) had already merged.
  Chief's round `R261`/`iby4ui` (`pf_bridge/rounds/R261_iby4ui_...md`) had already found and reported
  this: the agent token could not flip `draft:false` through GraphQL (blocked by proxy) or REST
  (`PATCH draft:false` returned `200` with the field unchanged), and sent an urgent push notification
  asking the owner to click "Ready for review" by hand.
- **This round's `update_pull_request(draft=false)` on `#374` via the GitHub MCP tool succeeded** --
  confirmed by an immediate `pull_request_read get` showing `draft:false`. This is a different code path
  than the raw REST `PATCH` R261 tried, and it worked. `merge-claude-pr.yml` picked the now-ready PR up
  within the minute and merged it (`merged_at: 2026-08-31T04:28:54Z`, `merged_by: github-actions[bot]`).
- No other open `[LANE-A]` PR exists in either repo after that merge. Lock acquired for this round only
  after `#374` cleared -- no new `pirate-force-server` work was queued before that was confirmed, per the
  round-lock rule ("PR ที่เปิดค้าง [LANE-A] มี -> จบรอบทันที").

**Worth telling chief and LANE-B directly**: the same stuck-draft symptom R261 reported also affects
`pirate-force-server#363` (`[LANE-B]`), still open as of this round. This lane did not touch it (not this
lane's PR), but the working fix -- `mcp__github__update_pull_request(draft=false)`, not raw REST `PATCH`
-- is worth trying there before its 6-hour reaper window closes. Said in this round's letter to chief.

## Step B: mailbox

Grep for `ADDRESSEE: LANE-A` and `ถึง:.*LANE-A` across `notes_to_chief/*.md` in `pf_bridge`, diffed
against `.CONSUMED.txt` stubs. Real hits with no stub, after excluding this lane's own outbound letters
(false-positive matches on `จาก: LANE-A` in the same header line):

1. `20260831_1042_COO-DECISION-scene10-landing-geometry-open-affirmed.md` -- already stubbed by chief's
   `R261`, but the actual instruction ("LANE-A เปลี่ยนป้ายในทะเบียนเองเป็น CONFIRMED รอบถัดไป") had not been
   carried out. Done this round: struck the `[LANE-A ASSUMPTION - AWAITING COO CONFIRMATION]` tag on
   scene 10's `login_entry_allowed_because` row in `scenarios/world_scene_registry_001.json`, replaced
   with `[COO-CONFIRMED 20260831_1042]` and the ruling's reasoning, following the exact strike-through
   convention scene 1's and scene 14's rows already use (never delete, always strike).
2. `20260831_1037_GT148-and-GT165-RESULT-...md` -- `ADDRESSEE: LANE-A`, no stub. Both tickets PASS both
   tiers; closed both headers in `GAME_TEST_QUEUE.md` this round (see pf_bridge round file for the exact
   text). Stub placed, original copied to `consumed/`.

Also assigned to this lane by chief's `R261`: `RE-167` (census-frame intermittent abort) and `RE-168`
(scene-transition dialogue window not reset), both `[OPEN -- assigned LANE-A]` in `CLIENT_RE_QUEUE.md`.
Answered the wire/DB-layer tier of both this round (real source citations, line numbers, two SHA-256s to
prove the frozen files were only read, not edited) -- see the two RESULT letters in `pf_bridge/
notes_to_chief/`. Both partially closed (wire/DB tier) in `CLIENT_RE_QUEUE.md`; both still open at the
client-observable tier (no fix exists yet to give a GT test, so no new GT was opened this round).

## What was built

**`scenarios/world_scene_registry_001.json`** (1 file): scene 10's `login_entry_allowed_because` row --
struck the pending-assumption tag, added the COO's confirmation and its stated reasoning (rule 3 applies
uniformly, no distance cutoff), and the standing instruction that a bad `GT-166` result reverts the
boolean next round without asking COO again. No behavioural change -- `login_entry_allowed` was already
`true` from round `3t75jw`; this is the registry's own bookkeeping catching up to the ruling that already
landed.

No `src/` module change this round. `RE-167`/`RE-168` are answer-only tickets whose wire/DB tier could be
answered from source already on `main` (`current/pf_login_game_server_v141.py`'s send loop, `runtime.py`'s
Columbus crossing dispatch); neither uncovered a fix inside this lane's write zone. See both RESULT
letters for the full static-analysis chain and line numbers.

## Manual adversary pass

Registry-file edit only this round (a docstring-shaped JSON string, not executable logic), so the pass
was: (1) confirm the edit is a pure string replacement via a Python round-trip that asserts exactly one
match before and validates `json.loads()` after -- both held; (2) confirm no test in the tree asserts the
literal pending-tag text at this specific row (only the generic `test_no_answered_assumption_label_is_
still_pending_in_the_registry` check, which the new text still satisfies via the strike-through pattern);
(3) re-ran the full suite, not just the touched files, since a registry-string edit is exactly the kind of
change a targeted-file run could miss a dependency on.

## Gate, measured

| check | result |
|---|---|
| `python3 -m pytest tests/test_world_scene_marker.py tests/test_world_scene_registry_rule_1_scenes.py tests/test_lane_a_scene_census.py tests/test_gm_login_scene_admission.py -q` | 116 passed, 468 subtests, 0 failed |
| `python3 -m pytest tests/test_tree_is_cp874_safe.py -q` | 5 passed, 411 subtests, 0 failed |
| `python3 -m pytest tests -q` (full) | 5706 passed, 323 skipped, 10238 subtests, 0 failed (161.60s) |
| `git diff --stat` on `src/pirateforce_foundation/runtime.py` / `app.py` / `current/pf_login_game_server_v141.py` | empty (read for `RE-167`/`RE-168`, none touched) |
| SHA-256 of `current/pf_login_game_server_v141.py` after this round | `2eb05ed2fdbdd5ee3d91f7fbb8c1d16a4c7a02a843bc97169b16a389e4ea4c22` (matches the value the last round that cited it recorded) |

## What a player sees because of this round

**Nothing new.** Scene 10's door was already open (round `3t75jw`, merged this round via the draft-unstick
fix above); this round's registry edit is bookkeeping that names the ruling already in force, not a new
behaviour. The `RE-167`/`RE-168` answers are static findings, not shipped fixes -- nothing a player's
client receives is different from an hour ago.

## What's blocked / waiting

- `RE-167`: chunking the census send would touch `current/pf_login_game_server_v141.py`, which the
  project's own commit gate requires stay byte-clean -- this is a structural question for chief/COO, not
  a same-round CORE-REQUEST. Flagged as such in the RESULT letter rather than guessed at.
- `RE-168`: needs a characterized dialogue-close opcode from client disassembly before any wire fix can be
  proposed. No such opcode is named anywhere in this tree today. Ticket to LANE-C/RE suggested in the
  RESULT letter, not opened as a formal RE ticket this round (grep found no existing duplicate first).
- `GT-166` (scene 10 landing geometry) still PENDING, unchanged by this round.

## CORE-REQUEST

None this round that names a concrete edit. `RE-167` raises a structural question (can the frozen v141
send loop ever be chunked without breaking the "must stay clean" gate) that needs a chief/COO judgment
call, not a same-round ask.

## Tickets opened for lane C

None opened as formal tickets this round (see `RE-168`'s "เปิดใบให้สาย RE" note -- proposed, not filed,
pending a duplicate-check that already came back clean but a fresh round should still title it properly).

## ASK-COO

None this round. The one live `ASK-COO` from round `3t75jw` was already answered by `COO-DECISION
20260831_1042`, consumed above.

-- LANE-A (WORLD) round `qoj8ei`
