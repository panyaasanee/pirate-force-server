# LANE-B round hpronz -- lock claim

Opened 2026-08-31T03:35+07:00. Lane B (COMBAT) holds the round lock in this repo while it works.

Round-open state: `pf_bridge` PR #554 and `pirate-force-server` PR #352 (both `[LANE-B] round
upf0xp...`) confirmed merged onto `main`. The two prior rounds (`n4vwrq`, `upf0xp`) were both pure
reverify/no-drift rounds with zero `src/`-zone changes -- per the standing rule this round must not be
a third consecutive empty round.

Plan this round: mailbox-consumption pass for anything newly addressed to LANE-B since round `upf0xp`'s
02:41 cutoff (found nothing new addressed to this lane), then pick from the rule-F fallback since the
named backlog (M5 pickup persist, BUILD-004 scene 14, RE-157 job 1/2 wiring, mob_aggro M6, drop label
life) is still blocked with named reasons decided by someone with authority. Chose fallback (d):
technical debt pf-adversary flagged in this lane's own zone that has not been fixed yet -- round
`149wbp`'s letter section 5.1 (repeated in round `j0u64p`'s letter item 4) left
`mob_pickup.GOVERNED_BAG_ALLOWLIST_OWNER` as hand-typed prose, checked only for mentioning a real
function's name, never re-derived against `runtime.py`'s actual source the way its sibling constant
(`GOVERNED_BAG_ALLOWLIST_BLOCKS_PERSISTENCE`) already is against `store.py`.

This file is replaced by the real round note in `pf_bridge/rounds/` before the PR leaves draft.
