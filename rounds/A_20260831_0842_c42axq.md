# LANE-A round `c42axq`

2026-08-31T08:25+07:00 - 2026-08-31T08:42+07:00 (approx).

## Step A / B (mandatory, start of round)

No GitHub API token in this environment this round; the orchestrating session
confirmed both repos were already at `origin/main` tip (`pirate-force-server`
= merge of PR #368/#369, `pf_bridge` = merge of PR #577) with no salvage
needed, and opened two draft round-lock PRs (`#371`/`#579`) to hold the
`[LANE-A]` slot before handing off. Verified locally: both working trees were
clean and matched the stated merge commits before this round's edits began.

Mailbox: `grep -rl "ADDRESSEE: LANE-A" pf_bridge/notes_to_chief/*.md`, diffed
against `.CONSUMED.txt` stubs. The three hits with no stub next to them
(`gt151-holes...`, `crossing-handoff-dispatch...`, `bg0010-...built-not-
wired.md`) are all this lane's own outbound STATUS letters, confirmed by
reading the matched line directly (each one quotes this exact mailbox-check
grep command in its own "Step B" section, not a real inbound header) — the
same false-positive shape round `u3jo4g` found. The one real inbound item,
`FROM_CHIEF_R256_TO_LANE-A_20260831_0556.md`, already has its
`.CONSUMED.txt` stub from round `bq4mst`. Nothing owed to this lane this
round.

Checked `notes_to_chief/*CLAIM*` for a live reservation before starting:
none found, and per `PANYA-DECISION 20260830_2356` item 3 / `COO-DECISION
20260831_0351` the reservation rule only applies to tickets more than one
lane could pick up or work expected to span multiple rounds — this round
continues a single-lane, single-round build order (`COO-DECISION
2026-08-30T14:41+07:00`'s door sequence) that is this lane's own by name, so
no CLAIM file was needed either to start or to hold this slot.

## What this round found and built

Continued the multi-round door sequence `COO-DECISION 2026-08-30T14:41+07:00`
approved. Round `u3jo4g` built scene 10's crosswalk + composer
(`world_bg0010_identity.py`, `world_population_bg0010.py`) and deliberately
left it unwired, the same split scene 4's own history used (build `6p22bu`,
wire `2jdde8`, open `bq4mst`). This round is scene 10's wire round, mirroring
`2jdde8`'s diff shape file-for-file.

Read the reference commit directly (`git show --stat 2472f3b`, round
`2jdde8`'s own commit) rather than re-deriving the wiring shape from
scratch: three seam files gain a `bg0010_roster` entry
(`world_scene_travel.CENSUS_SOURCES`, `world_population_handoff.
ROSTER_COMPOSERS`, `lane_hooks/lane_a_scene_census.py`'s `_CONSOLE_LINES_OF`),
one cross-lane tripwire needed the same acknowledgement bg0004 needed
(`mob_scene_recompose.ACKNOWLEDGED_WITHOUT_COMPOSER[10]` — verified
independently this round, not assumed: `field_mobs.scene_for_scene_id(10)`
is `None`, same as scenes 4 and 14), and two test files needed the same
renames/additions bg0004's own wiring round made
(`test_nothing_under_src_imports_this_module_yet` ->
`test_only_the_population_seam_imports_this_module`, asserting the exact new
importer set rather than the empty one; a new `DeepSeaTempleRegistrationTests`
class in `tests/test_lane_a_scene_census.py` mirroring
`SlaveMarketRegistrationTests`' pre-`bq4mst` shape — composer registered,
real registry row still refuses it, plumbing proven against a temp-opened
registry).

`scenarios/world_scene_registry_001.json` is untouched: scene 10's
`login_entry_allowed` stays `false`. Neither the login path nor any
crossing path reaches scene 10 today — the new composer is registered and
reachable by nothing, exactly as bg0004's own wiring round left it for one
round before a separate round judged it ready and flipped the boolean.

## What shipped in `src/` and `tests/`

- `src/pirateforce_foundation/world_scene_travel.py` — added
  `DEEP_SEA_TEMPLE_SCENE_ID = 10` and a `bg0010_roster` row in
  `CENSUS_SOURCES`.
- `src/pirateforce_foundation/world_population_handoff.py` — imported
  `world_population_bg0010`, added a `bg0010_roster` entry to
  `ROSTER_COMPOSERS` (same `_SceneComposer` shape as bg0004's).
- `src/pirateforce_foundation/lane_hooks/lane_a_scene_census.py` — imported
  `world_population_bg0010`, added a `bg0010_roster` entry to
  `_CONSOLE_LINES_OF`.
- `src/pirateforce_foundation/world_population_bg0010.py` — docstring's "NOT
  WIRED" paragraph rewritten to "WIRED, ROUND c42axq, DOOR STILL SHUT" (same
  edit bg0004's own module made at its wiring round); `production_allowed`
  comment updated to name its two new importers.
- `src/pirateforce_foundation/mob_scene_recompose.py` — added
  `ACKNOWLEDGED_WITHOUT_COMPOSER[10]`, mirroring scene 4's entry, verified
  against `field_mobs.scene_for_scene_id(10)` directly rather than assumed.
- `tests/test_lane_a_scene_census.py` — imported `world_population_bg0010`;
  added `DEEP_SEA_TEMPLE = 10` / `DEEP_SEA_TEMPLE_ROSTER_COUNT = 94`;
  widened `ComposerContractTests.setUpClass`'s shared open-registry tuple
  from `(VOLCANO, SLAVE_MARKET)` to `(VOLCANO, SLAVE_MARKET,
  DEEP_SEA_TEMPLE)` (otherwise `test_the_reapply_field_can_never_reach_the_
  call_site_as_none`'s loop over `scenes_this_lane_composes_for()` would hit
  scene 10 with its door shut in that fixture and turn a `None` decline into
  an `AttributeError`); added `DeepSeaTempleRegistrationTests` (3 tests).
- `tests/test_world_population_bg0010.py` — renamed/widened the
  import-isolation test as described above; module docstring's "NOT WIRED"
  citation updated to "WIRED, DOOR STILL SHUT".
- `reports/PF_RUNTIMERES_ACTOR_ENTRY001_STATIC_20260819.md` — appended a NOTE
  section (round `c42axq`) recording that wiring adds two importers but
  moves none of the three actor-entry-site counts (the module that builds
  entries already existed and was already counted at round `u3jo4g`), and
  that the renamed test now asserts the importer set.

Not registered anywhere a player can reach: scene 10's `login_entry_allowed`
stays `false`. A player sees exactly what they saw yesterday because of this
round.

## Manual adversary pass (no subagent tool available in this environment,
## same limit every LANE-A round since `i95a1z` has reported)

Three mutations, scratch-copied one file at a time, restored from a `/tmp`
backup after each check, `diff` against the backup confirming byte-identical
restoration before moving to the next:

1. Removed the `bg0010_roster` entry from `lane_a_scene_census.
   _CONSOLE_LINES_OF` — `_compose_for_scene(10)` raised `KeyError:
   'bg0010_roster'` the moment a real generation reached the console-line
   step; `DeepSeaTempleRegistrationTests` (both live tests) failed
   immediately. Reverted.
2. Removed the `DEEP_SEA_TEMPLE_SCENE_ID: "bg0010_roster"` row from
   `world_scene_travel.CENSUS_SOURCES` — `scene_census_composer(10)` went
   back to `None` (`SkippedScenesAreNamedTests`' own subtest for
   `bg0010_roster` failed as a dead console reader with no source, plus both
   `DeepSeaTempleRegistrationTests` tests that need a live composer).
   Reverted.
3. Removed the `bg0010_roster` entry from `world_population_handoff.
   ROSTER_COMPOSERS` — the seam's own composer resolution failed, dropping
   `handoff.kind` out of `KIND_CENSUS`, and the temp-registry-open plumbing
   test (`test_opened_in_a_temp_registry_it_composes_the_full_roster`) went
   from 94 actors to a decline; the shared-loop reapply test in
   `ComposerContractTests` also caught it as a subtest failure for
   `scene=10`. Reverted.

`git diff --stat` on `src/pirateforce_foundation/lane_hooks/
lane_a_scene_census.py`, `world_scene_travel.py`, and
`world_population_handoff.py` each confirmed empty (0 diff from this
round's intended state) after every revert, one file at a time, before the
next mutation.

## Gate, measured

| check | result |
|---|---|
| targeted (5 files: the census/lane-hooks/handoff/travel/recompose group + both bg0010 test files) | 134 passed, 1063 subtests, 0 failed |
| `python3 -m pytest tests -q` (before mutation pass) | 5695 passed, 327 skipped, 10236 subtests, 0 failed (181s) |
| `python3 -m pytest tests -q` (after mutation pass, re-run) | 5695 passed, 327 skipped, 10236 subtests, 0 failed (181s) — identical, confirming the reverts left no trace |
| `python3 tools/verify_hypothesis_ledger.py` | `PASS entries=47` (unchanged) |
| `python3 tools/verify_functional_coverage.py` | `PASS domains=8`, `OPEN DOMAINS: 8` (unchanged) |
| `git diff --stat` on `runtime.py`/`app.py`/`current/pf_login_game_server_v141.py` | empty (none touched, none needed) |

Up from round `u3jo4g`'s baseline of 5692 passed / 10123 subtests: +3 passed
(the 3 new `DeepSeaTempleRegistrationTests` methods), +113 subtests (every
`scenes_this_lane_composes_for()`-loop-based test in
`tests/test_lane_a_scene_census.py` now also iterates scene 10).

## Player-visible claim

**None.** Scene 10's door (`login_entry_allowed`) is unchanged (`false`), no
scene reaches a player differently than yesterday, and no other scene's
behaviour changed. This round wires the composer into both seam tables and
the lane's console reader — reachable by nothing — plus test coverage.

## What's blocked / waiting

- Opening the door (flipping `login_entry_allowed` for scene 10) is a
  separate later round's judgment call, same three-round rhythm scene 4
  used. Not made here; no new evidence changes the landing-geometry caution
  round `u3jo4g` already recorded (registry `table_row_differences.
  the_two_interiors`, marker point 5174.7 units outside placement extents) —
  a future door-opening round's job.
- Whether any of this scene's monster-shaped placements should be hostile is
  still a LANE-B decision, not made here.

## CORE-REQUEST

None this round. All three seam edits are inside this lane's own write
scope (`world_scene_travel.py`, `world_population_handoff.py`,
`lane_hooks/lane_a_scene_census.py`, `mob_scene_recompose.py` — the last is
outside this lane's four named write-zone paths but a co-maintenance edit
fixing a cross-lane tripwire this round's own change tripped, same
precedent round `2jdde8` set); no `runtime.py`/`app.py` change was needed.

## ASK-COO

None this round. Continuing the already-approved door sequence
(`COO-DECISION 2026-08-30T14:41+07:00`'s own instruction: no need to ask
again per door absent an irreversible fork).
