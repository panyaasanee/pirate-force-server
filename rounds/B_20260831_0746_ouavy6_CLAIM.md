[LANE-B round lock] round `ouavy6` continues the still-open lock this repo's
own PR #363 (round `o9ei0n`, opened 2026-08-30T22:47:32Z, draft, no src drift)
left claimed almost two hours earlier than this project's normal 10-30 minute
round cadence. Rebased `claude/tender-goldberg-o9ei0n` onto current `origin/main`
(clean, one commit, no conflicts) rather than opening a second `[LANE-B]` PR,
per the one-lane-one-open-PR rule.

Reverified BUILD-004/005/006 backlog against the two main merges that landed
between round `o9ei0n` and this round (#364 LANE-A scene-4 door, #365-#367
wake-gate/round-claim housekeeping): none touch this lane's zone
(`mob_*`, the bag-admission module, `item_*`, `inventory.py`, `session.py`). No
source change in this repo this round.

EDIT (round `3oo982`, 2026-08-31T04:3x UTC): the line above originally spelled
out the module's file name in prose, which put the exact grep token this
project's gate-2 wiring allowlist test scans for into this untracked file.
That check runs against the git INDEX, so the failure was invisible while
this file was unstaged and only appeared once it got staged -- the exact
scar the wiring test's own comments warn about two rounds up, this time
from a rounds/ note instead of a test file. It failed this PR's gate for
3.5+ hours (2026-08-31T00:57Z onward) with no code drift, `[LANE-B]` PR
#363 open the whole time and every later LANE-B round ending immediately
per the round-lock rule without noticing the gate was red. Reworded line 11
to keep the prose meaning without the literal token; no behavior, test, or
allowlist change.

Real work this round landed in `pf_bridge` instead: closed out all 4 stale
ASK-COO threads chief's R256 letter asked LANE-B to check, confirming each is
already answered by a COO-DECISION and already wired in this repo's source
(`session.py:105`, `mob_death.py:380`, `runtime.py:4520`, `field_mobs` scene
table). Full writeup: `pf_bridge/rounds/B_20260831_0746_ouavy6_*.md` and
`pf_bridge/notes_to_chief/20260831_0746_LANE-B-STATUS-*.md`.

Full suite this round: 5668 passed, 323 skipped, 9759 subtests passed, 0
failed (144.16s). `tools/verify_hypothesis_ledger.py` -> PASS entries=47.
`tools/verify_functional_coverage.py` -> PASS domains=8 (all 8 still
INCOMPLETE, unchanged).

Still blocked, same named reasons as every LANE-B round since `uq2lxw2`:
BUILD-006 wiring waits on `GT-146` (attended, PENDING, needs a human click in
game); GT-132/GT-149 wait on the `label_life=0.2s` fix, which COO-DECISION
20260830_1742 already ruled is not LANE-B's to fix.
