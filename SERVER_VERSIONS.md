# SERVER_VERSIONS — เซิร์ฟเวอร์ของเรา นับเป็นเวอร์ชัน

สร้างตามคำสั่ง `COO-CHARTER-02` ⑤ (2026-08-25 ~23:45 +07:00) · ดูแลโดยสาย **E · PLATFORM**

> **นิยาม: `BUILD-00n` ที่ลงเสร็จ = เซิร์ฟเวอร์ `v n`**
> เวอร์ชันคือ **สิ่งที่คุณเข้าไปเดินเล่นได้จริงในวันไหนก็ได้** ไม่ใช่แท็กในรีโป
> วิธีเข้าเล่น: ดับเบิลคลิก `PLAY_PIRATE_FORCE.bat` บนเดสก์ท็อป (ไม่ส่งแฟล็ก scenario ใด ๆ)

## กฎ 4 ข้อ — ผิดข้อใดข้อหนึ่ง ไม่ใช่เวอร์ชัน
1. **ไม่มีแฟล็ก** — ต้องทำงานเต็มความสามารถโดยไม่ส่ง `--*-scenario` แม้แต่ตัวเดียว
2. **สะสม** — `v n` ต้องทำทุกอย่างที่ `v n-1` ทำได้ ครบ · ของที่เคยเล่นได้แล้วเล่นไม่ได้ = ของเสีย ไม่ใช่เวอร์ชันใหม่
3. **เล่นได้** — บูตแล้วล็อกอินได้ เดินได้ ไม่ค้าง ไม่หลุด ในเซสชัน ≥ 10 นาที
4. **มีประโยคของผู้เล่น** — เขียนได้ว่า "ผู้เล่นทำอะไรได้ ที่เวอร์ชันก่อนทำไม่ได้" · เขียนไม่ได้ = ยังไม่ใช่เวอร์ชัน

🔴 **บรรทัด `regression` เขียนจากการเล่นจริงเท่านั้น ไม่ใช่จากการที่เกตเขียว** — เกตของเราไม่เคยรู้ว่าเกมสนุกไหม
🔴 **ห้ามเขียนบล็อกของเวอร์ชันที่ยังไม่มีใครเล่น** — บรรทัด `commit` ต้องเป็น sha ที่บูตขึ้นจริงแล้ว ไม่ใช่ sha ที่ตั้งใจจะบูต

---

## v0 — 2026-08-25 · 🔴 **`[PROPOSED baseline · ชั้น static-source]` ยังไม่ใช่เวอร์ชันที่ประกาศ**
```
commit                : 7e5eb72e6eae274f6fbe44bfee8cf2b39dfe0499 (ยอด claude/dazzling-einstein-lh44g4, R172)
ผู้เล่นทำอะไรได้เพิ่ม  : (ฐาน) บูต ล็อกอิน เลือกตัวละคร เดินใน Port Royal ได้
ยังทำไม่ได้           : เมืองมีคนแค่ 3 ตัวจาก 115 จุดวางของแมพ · ออกไปแมพอื่นไม่ได้ · ไม่มีมอนสเตอร์ · ตีไม่ได้ · เก็บของไม่ได้
regression ที่ตรวจแล้ว : ไม่มีของก่อนหน้าให้เทียบ
```
🔴 **บล็อกนี้ยังผิดกฎแดงข้างบนของตัวเองอยู่หนึ่งข้อ และผมเขียนไว้ตรงนี้แทนที่จะกลบ (`pf-adversary` R172 ข้อ 11):**
`commit` เป็น sha จริงแล้ว **แต่ยังไม่มีใครบูต sha นี้แล้วเดินดู** ⇒ บรรทัด "ยังทำไม่ได้" เป็น **ข้อเท็จจริงชั้นซอร์ส ไม่ใช่ชั้นที่ตาเห็น**
⇒ **ห้ามยกบล็อกนี้ไปอ้างเป็นสิ่งที่เจ้าของเห็นบนจอ** · จะเลื่อนเป็น `[MEASURED]` เมื่อ `GT-075` ให้เฟรม + `OBSERVER_CONFIRMED` กลับมา

**ทำไมต้องมี v0 ทั้งที่ COO เริ่มนับที่ v1:** กฎข้อ 2 (สะสม) ต้องมีของให้เทียบ ⇒ บรรทัด `regression` ของ `v1` เทียบกับบล็อกนี้
**ที่มาของตัวเลข 3/115:** `COO-CHARTER-01` ① — `current/pf_login_game_server_v141.py:4292` ส่ง `V134_P0_P30_P91_ISOLATED`
บนเส้นทางรันไทม์ปกติ (ไม่ได้อยู่หลังแฟล็กใด ๆ) · ยืนยันชั้นที่สองแล้ว: `src/pirateforce_foundation/app.py:675`
โหลดไฟล์นั้นเป็น `legacy` และ `grep -rn "P0_P30_P91" src/` **ว่างเปล่า** ⇒ ตารางประชากรอยู่ในไฟล์ที่ห้ามแก้จริง

---

## v1 — 2026-08-30 · ประกาศโดย chief ตาม `COO-DECISION 20260830_2142` (R249)
```
commit                : cf1f63f6f16a70c1d3b17210e91338b7b73528fd (main HEAD ตอนบูต GT-131, PR #285, ไร้แฟล็ก)
ผู้เล่นทำอะไรได้เพิ่ม  : เดินใน Port Royal แล้วเจอ NPC ตัวจริงยืนตามชื่อ/ตำแหน่งของเซิร์ฟเวอร์เดิม (Columbus, Loie, Hields, Sase, Nayar ฯลฯ) แทน 3 ตัวหลอกของ v0
ยังทำไม่ได้           : ออกจากเมืองไปแมพอื่นไม่ได้ (v2) · เพดานข้อมูล client เองอยู่ที่ 108/115 (7 placement ไม่มีข้อมูลตัวตนจาก client เอง — `COO-DECISION 20260829_1941`) · สีชื่อ NPC/ท่ายืน/มอนซ้อมยังไม่สมบูรณ์ (`GT-151` PARTIAL)
regression ที่ตรวจแล้ว : v0 (บูต ล็อกอิน เลือกตัวละคร เดินใน Port Royal) ยังทำได้ครบในเซสชันเดียวกัน (27 นาที ไม่ค้างไม่หลุด) · คลิก NPC ได้คำตอบจริง (เฟรม V98_NPC_FACE_PLAYER_POSITION_HEADING/V98_NPC_CONVERSATION_DEFAULT ออกจริง) ไม่พบความผิดปกติจากสแนปช็อตประชากรก้อนใหญ่ขึ้น
```
🎯 **เกณฑ์ 4 ข้อครบ:** ไม่มีแฟล็ก (BOOT_COMMIT guard ตรวจ cmdline ยืนยัน) · สะสม (v0 ยังเล่นได้ครบ) · เล่นได้ (เซสชัน 00:00-00:27+07:00 ไม่ค้าง) · มีประโยคผู้เล่น (บรรทัดบน)
**หลักฐาน:** ชั้น client-observable และ wire มาจาก `GT-131` (`OBSERVER_CONFIRMED: 2026-08-30T00:2x+07:00` โดย Panya คำต่อคำ "ตำแหน่ง npc ถูก ตัวถูกต้อง ฉันให้เทสนี้ผ่าน") ซึ่งเป็นใบตรวจรับ**หลัง** `GT-078` ถูกปฏิเสธชั้น identity — ปิดช่องว่างที่ `GT-078` ทิ้งไว้ด้วยหลักฐานคนละใบตามที่ COO สั่ง ไม่ใช่รัน `GT-078` ซ้ำ
รายละเอียดเต็ม: `pf_bridge/notes_to_chief/20260830_0030_KA3A-GT131-PASS-owner-confirmed-gt151-partial-plus-four-polish-gaps-and-mob-vs-npc-question.md` · วิดีโอ/ภาพนิ่งเก็บไว้ ห้ามลบ

<details><summary>ประวัติการสร้าง (R173-R248, ก่อนประกาศ) — เก็บไว้อ่านประกอบ ไม่ใช่สถานะปัจจุบัน</summary>

**สถานะการสร้าง ณ R173 (2026-08-26 ~02:0x +07:00) — ชั้นซอร์ส ไม่ใช่ชั้นที่ตาเห็น:**
- สาย A สร้าง `src/pirateforce_foundation/world_population.py` เสร็จและ merge แล้ว **แต่ไม่มีใครเรียก**
- **สาย E เดินสายให้แล้วในรอบ R173** (`WORLD-CENSUS-001`): บูตที่ไม่มี `--*-scenario` แม้แต่ตัวเดียว
  จะคิว `WORLD_CENSUS_INITIAL_115` + `WORLD_CENSUS_REAPPLY_115` แทน `V134_P0_P30_P91_ISOLATED_*`
- `GT-078` (attended, 2026-08-26 12:55-13:37+07:00): ชั้น wire PASS ครบ (115/115) แต่ **OWNER-REJECTED บนชั้น identity** (placement ถูกทุกจุด แต่ตัว NPC ที่เกิดจริงผิดทุกตัว) — ค้างเปิดรอตารางแปลงตัวตนจากสาย A/RE
- `GT-131` (2026-08-30, ดูบล็อกด้านบน) ปิดช่องว่าง identity นั้นด้วยตารางแปลง `CONSTDATA_TH__CLINE` (`RE-128`) ที่แก้ที่มาแล้ว

</details>

---

## แผนที่เวอร์ชันข้างหน้า (จาก `COO-CHARTER-02` ⑤ · กำหนดคือ **เพดาน ไม่ใช่เป้า**)
| ver | ผู้เล่นทำอะไรได้ที่เมื่อวานทำไม่ได้ | จาก | กำหนด (+07:00) |
|---|---|---|---|
| v1 | เมืองมีคนอยู่จริง | `BUILD-001` | 26 ส.ค. 12:00 |
| v2 | ออกจากเมืองไปแมพอื่นได้ + มีแมพเทสขาวล้วนพื้นเรียบ | `BUILD-002` | 26 ส.ค. 23:59 |
| v3 | เจอมอนสเตอร์ชื่อแดงในสนาม | `BUILD-004` | 28 ส.ค. 12:00 |
| v4 | ตีมัน เลือดลด มันตาย | `BUILD-005` | 29 ส.ค. 23:59 |
| v5 | เก็บของที่มันดรอป · relog แล้วของยังอยู่ | `BUILD-006` | 31 ส.ค. 12:00 |
| v6 | ล็อกอิน → เมือง → สนาม → ฆ่า → เก็บ → กลับเมือง จบวงจร | `BUILD-007` | 2 ก.ย. 23:59 |

**สถานะการสร้าง `BUILD-005` (`v4`) ณ R177 (2026-08-26 ~16:0x +07:00) — ชั้นซอร์ส ไม่ใช่ชั้นที่ตาเห็น:**
- สาย B ส่งมอบ `mob_combat.py` + `mob_death.py` (`production_allowed=true`, ไม่มีแฟล็ก, ทดสอบแล้ว) ตั้งแต่ 04:0x
- **สาย E เดินสายให้แล้วในรอบ R177** (`CORE-REQUEST-005`, `pirate-force-server@6105d26`): บูตไม่มี `--*-scenario`
  เมื่อ EA7D ActionVital ชี้เป้าเป็น field-mob จะคิด damage → ส่ง announce+bar → เมื่อถึง HP floor **และ**
  target เป็น identity `0x201F` (owner ruling: sanctioned first target) จะส่ง dying/dead ด้วย
  🔴 target อื่นที่ถึง 0 HP **ยังไม่ตาย** (เงียบ ไม่ส่งเฟรม) — ข้อจำกัดที่รู้แล้ว ไม่ใช่บั๊ก
- 🔴 **กฎข้อ 2 (สะสม) ยังไม่ให้เรียกนี่ว่า `v4` ได้** — `v1`/`v2`/`v3` ยังไม่ประกาศเลยสักตัว ⇒ บล็อกนี้เป็น
  บันทึกความคืบหน้าชั้นซอร์สล่วงหน้าเท่านั้น รอคิว v1→v2→v3 ปิดก่อนจึงจะประกาศ `v4` ได้จริง
- ยังไม่มีใครสังเกตว่า input การโจมตีจริงจากไคลเอนต์สร้างเฟรม EA7D ที่โมดูลนี้อ่านหรือไม่ — `pf-adversary`
  R177 ยืนยันแล้วว่าไม่มีใบไหนอ้างว่าเคยเห็น ⇒ [PROPOSED] ทั้งก้อน จนกว่าจะมีรอบ attended มายืนยัน

## nonclaim ของไฟล์นี้
- ไฟล์นี้ **ไม่ใช่หลักฐาน** — มันเป็นดัชนีที่ชี้ไปที่ commit และรอบเทสที่เป็นหลักฐานจริง
- **ยังไม่มีเวอร์ชันไหนถูกประกาศ ณ เวลาที่สร้างไฟล์นี้** (`2026-08-26 00:0x +07:00`) · `v0` เป็นฐานเทียบ ไม่ใช่การประกาศ
- โลกที่คุณเข้าไปเล่นคือ `state\play.sqlite3` **คนละไฟล์กับ canonical** (คำเคาะ COO · `CHARTER-02` ⑤)
  🔴 **แหล่งเดียว (`pf-adversary` R172 ข้อ 11):** `PLAY_PIRATE_FORCE.bat` · `STOP_PLAYING.bat` · `state\play.sqlite3`
  **ไม่ปรากฏในไฟล์ใดเลยในสอง repo นอกจากจดหมายของ COO และไฟล์นี้** ⇒ ตาม `G1` ห้ามยกเป็นข้อเท็จจริงจนกว่าจะเห็นของจริง
  · ของพวกนี้อยู่บนเดสก์ท็อปของเจ้าของ ซึ่งไม่อยู่ใน VCS ⇒ **นี่เป็นข้อจำกัดของที่ตั้ง ไม่ใช่ข้อกล่าวหาว่าไม่มีจริง**
