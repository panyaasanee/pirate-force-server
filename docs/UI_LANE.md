# UI Lane -- non-core buttons and player-facing functions

Owner order: PANYA-ORDER `pf_bridge/notes_to_chief/20260905_1911_KA1A-TO-COO-PANYA-ORDER-lane-ui-ship-ui-b-logout-headless-first-registry-plan.md`,
registered in the write zone by COO-DECISION `20260905_1948` / chief `20260905_1949`.

## What this lane is, in one sentence

"Clear out buttons/functions and small subsystems outside the core systems
(mob/quest/combat/skill are not this lane) -- back-to-character-select /
exit-game buttons, auto-walk to an NPC/monster, NPC shop as an example" --
Panya's own words, `notes_to_chief/20260904_0328...` (this lane's opening
order). A button "works" when the player sees what the button PROMISES on
screen for real, not "the server refuses with a message" (LANE-A's refusal
layer, `GT-205`/`GT-211`, is a starting point, not the finish line) and not
a report-only module that only logs a frame.

## Write zone

- `src/pirateforce_foundation/ui_*.py` -- all new modules for this lane
- `tests/test_ui_*.py`
- `docs/UI_LANE.md` (this file)
- `rounds/UI_*` (pf_bridge side)

`runtime.py`, `app.py`, `store.py`, `gm/`, and
`current/pf_login_game_server_v141.py` belong to chief/other lanes. Any
wiring into those files is one `CORE-REQUEST` letter per hookup point in
`pf_bridge/notes_to_chief/`, naming the module, the function to call, and
the exact dispatch spot (vital id / branch).

Not this lane: mob/combat/loot (LANE-B), quest, skill/profession (LANE-CS),
scene/travel/TriggerVital into an island (LANE-A, M2), GMUI + `/` commands
(LANE-GM), DB rows (LANE-DB -- a new column is a letter), `v141` (frozen,
forever).

## The three-file function map (Panya order `20260905_19:0x`)

1. **Catalog of "what can this game do"** =
   `pf_bridge/VITAL_REGISTRY_FROM_CLIENT_BINARY_20260817.tsv` (327 rows, id +
   name on the wire, one row = one real action). Grouped by the prefix
   before the first `_`: `Community_` 38 (friendship/soulmate/bottle
   letter/penpal/system mail), `Equipment_` 17, `Pets_` 16, `Channel_` 16
   (chat), `Express_` 12, `BuildingCrystal_` 12, `Activity_` 9,
   `CollectionObj_` 6, `Winemaking_` 5, `KnowledgeGuru_` 5, `HitParade_` 5
   (rankings), `TreasureHunt_` 3, `Gathering_` 3, `NavigationEx_` 2,
   `UserSetting`/`Dyeing`/`Appraisal`/`Stall`/`Trade`/`Party`/`Arena`/
   `Vehicle`/`Potion`/`Relive`/`ItemLock`/etc. 147 unprefixed names are
   core systems (mostly A/B/GM/CS's, not this lane's, per the mission).
2. **"How would it be built"** = `external/PF_PROTOCOL_REGISTRY.tsv` (519
   classes, VA of serializer/handler/getter for static RE) -- 209 names
   beyond item 1 are data structs/modules, not player actions; 17 names
   exist only in item 1. Read both, always, before assuming something is
   unbuilt.
3. **"Do we already know the wire shape"** = `external/PF_SERIALIZER_FIELDS.tsv`
   (proven layouts + W/R direction). Start at `external/00_SEARCH_HERE_FIRST.md`
   before declaring anything "unknown".

MEASURED, round `splep7` (sweep over every UI-prefixed class in `VITAL_REGISTRY_FROM_CLIENT_BINARY_20260817.tsv` that has rows in `external/PF_SERIALIZER_FIELDS.tsv`, cross-checked against every `src/pirateforce_foundation/ui_*.py` in the tree): there are **zero** classes left that are both fully tagged and unimplemented. The first rung of the pickup order is EMPTY -- every remaining class in this lane's groups needs static RE first (closest by far: `KnowledgeGuru_`, see the last row of this table), and every class already built needs the `runtime.py` seam (`CORE-REQUEST 20260907_2020`, chief queue) before a player can see it. Do not re-derive this by hand next round; re-run the sweep only after an RE result lands or a new class ships.

**Pickup order for every row below: layout already known (no RE needed) >
needs static RE > needs attended capture, last.** Every new RE ticket must
cite a row in this table and its own "grepped `external/`+`archive/`
first: found/not found" section (`AGENTS.md` section 7).

**"n/327" whole-project coverage number** (PANYA `2032` job 2): see
`docs/UI_WIRE_COVERAGE.md`, generated by `tools/pf_ui_wire_name_census.py`.
That page counts every lane's wire modules, not just this one's; it is a
mechanical name-presence census, not this table's per-function status.
Current: **30/327** (round `mg3nr4` 2026-09-07). It fell from 160 in that
round, and the fall is a measurement fix, not a regression: the tool stopped
counting a wire name that appears only inside a docstring, and 126 of the 130
rows that moved are `ui_*_wire.py` modules whose docstring frame table spells
the wire name while the code below names the class something shorter. The movement log lives on that
page, one line per round that moves the number -- do not duplicate it here.
🔴 **This file is also one of the four NAME-ONLY sources that census reads.**
Writing a vital's name into the table below, or deleting one from it, moves
the number with no code written either way. Re-run
`python3 tools/pf_ui_wire_name_census.py --emit` before committing an edit to
this page, or the pinned test goes red in the next round with nothing to
point at (pf-adversary D5, round `mg3nr4`).

## Function table

Status legend: `LAYOUT-KNOWN` (item 3 proves the wire shape),
`NEEDS-RE-STATIC` (shape unknown, answerable from the client image),
`NEEDS-CAPTURE` (needs an attended live boot), `IN-PROGRESS`,
`HEADLESS-DONE` (server-side proven, no client-visible claim yet),
`DONE` (both evidence layers closed plus a GT ticket the tester actually
clicked), `BLOCKED-ON-RE-<id>` / `BLOCKED-ON-<LANE>`.

| Group (item 1) | Vital / id (dir) | Player sees | Status | Next step | GT/RE |
|---|---|---|---|---|---|
| core (exit/select) | `LogoutVital` `0x1B40` subcode 1 "exit game" (c->s), ack same id (s->c) | Click "Exit Game" -> session ends cleanly, can log back in | **HEADLESS-DONE, WIRED**: `ui_logout_exit_game.dispatch_real_exit_game_logout` composes the pinned HYP-PF-012 ack, calls the same `close_connection()` every disconnect already uses, and schedules the HYP-PF-013-proved delayed socket close -- proven against a real `SQLiteStore` (session row closes, a fresh login re-selects the same character). The `runtime.py` hookup LANDED (the `elif nested_id == LOGOUT_VITAL_ID:` branch calls this module first and returns `outcome.actions` when `handled`); LANE-A's refusal notice now only composes when this module declines the frame. | Hookup is landed; what was left is a GT ticket for the client-observable half (does the real client actually accept the FIN and return control, i.e. does the process/UI actually end), and the ONE thing that ticket could not board the capture bus without -- a `HEADLESS_PROOF:` token -- LANDED THIS ROUND (`uw3bxb`): `src/pirateforce_foundation/ui_logout_exit_game_headless.py` boots the real dispatcher with no flag and no scenario, drives the real subcode-1 bytes, and prints `UI_LOGOUT_EXIT_GAME_ARMED subcode=1 ack=1 lease_closed=1 close_scheduled_ms=250 closer_called=1 relogin_after=ok head=<12hex> code=<12hex> RESULT=PASS` plus a subcode-3 control line; the module itself now prints `UI_LOGOUT_EXIT_GAME_ACK_COMPOSED ...` at the moment of a real teardown (named COMPOSED, not SENT, because pf-adversary F6 measured the line printing while v141's `sendall` of that same ack raised `BrokenPipeError` -- an attended reader must not take it as "the bytes reached the client") and `UI_LOGOUT_EXIT_GAME_NOT_SENT reason=<token>` when a real exit-game click is refused, so a dead button and an absent branch stop looking alike on the console. pf-adversary came back NOT CLEAN before unlock and this round fixed six of its findings, the worst being its own: both prints were unguarded, and a closed stdout would have ended the game listener thread with the lease already closed (F1, now wrapped). Next: the GT body letter to LANE-K, rewritten to ask what the player gets TODAY on a default boot rather than to re-run the ack+close shape GT-008/GT-033 already falsified. | `GT-211` (refusal layer, PASS-partial) superseded for subcode 1 once wired; GT body letter to K sent round `uw3bxb` |
| core (exit/select) | `LogoutVital` `0x1B40` subcode 3 "back to character select" (c->s) | Click "Back" -> character-select screen renders | **BLOCKED, AND THE ACK+CLOSE SHAPE IS ALREADY FALSIFIED ON SCREEN -- READ THIS BEFORE PROPOSING IT AGAIN (round `719e10`)**: answering subcode 3 with the pinned HYP-PF-012 echo ack plus a clean server-side socket close is NOT an open question and must not be shipped as one. It has been run attended FOUR times and is negative every time: `GT-007` (2026-08-17, echo ack alone -- the client neither returns to character select nor exits), `GT-008` (2026-08-18, ack+close -- *the client never detects the TCP close at all*, no transition, no dialog, UI live 40+ s), `R311` (2026-09-04, `HYP_PF_013_LOGOUT_SUBCODE03_ACK_THEN_SERVER_SOCKET_CLOSE` 46 B then close -- screen unchanged 90 s, screenshot `20260904_192129.png`), `R319` (2026-09-05, `OBSERVER_CONFIRMED`, same result with Panya at the keyboard). `docs/FUNCTIONAL_COVERAGE.json`'s own conclusion is the one to carry: **client transitions here are protocol-driven, not transport-driven** -- closing the socket is not the lever. Round `719e10` implemented exactly this shape, headless-green, and REVERTED it before pushing when pf-adversary produced those four records; the server half being provable headless is what makes this trap easy to walk into, because every test passes. What is genuinely unanswered is only WHICH inbound frame flips the screen, and on that:  `RE-266` (`notes_to_chief/20260905_2242_RE-266-RESULT-*.md`) came back BOUNDED-NEGATIVE / STATIC ANSWERED -- the `0x709E` true branch does session/transport handoff only (no call opens the selection UI in that downstream), and `GetWorldInfoVital`'s natural R handler has no pending-reply flag/ack-clear/state-transition call; it fans out to three `SystemSetting_*` receivers instead. Static RE has hit method ceiling on this question (per RE-266's own BUILD_IMPACT: do not retry `0x709E` from HOME with a payload tweak, do not add a server-side wait-for-`0x3D4B` ack). Deciding whether the real character-select flow waits on world-info now needs an attended, dual-layer (client-observable + wire) capture, not another static ticket. | `GT-205` stays OPEN and its `BACK REFUSED` receipt must keep composing until something replaces it (it is the only client-observable positive this family has); `GT-184`/`GT-186` carry the narrowed question (does the client visibly wait after the ack, with the wire capture taken in the same pass) and an `ATTENDED:` block; queued behind `NEEDS-ATTENDED-CAPTURE` and Panya's machine per the RE->GT rule (`AGENTS.md` section 7, `COO-DECISION 20260904_2142` item 3) | `GT-205` (refusal layer, PASS-partial); `GT-184`/`GT-186` BLOCKED-ON-RE-266, method ceiling reached, waiting on attended capture |
| auto-walk / click-target | `CTracePathReqVital` `0x4391` (c->s) -> empty-vector reply (s->c) | Click ground/NPC/monster (incl. minimap, same frame per `FUNCTIONAL_COVERAGE.json` `MOVE-AUTHORITY-001/-002`) -> real auto-walk to that point | Empty-vector "unstick" fallback landed (`RE-119`, `GT-120` PASS) -- that only clears the "stuck forever" bug, it is NOT auto-walk. `CORE-REQUEST-025`/`20260905_0347` installed a log-only observer at the dispatch site (`lane_hooks.fire`), decides nothing about the request's own fields. `RE-236` item (b) (`u16@+0x14` = quest id / NPC id / list index) still open; tracepath CORE-REQUEST is `BLOCKED-ON-LANE-A accessor` per chief `20260905_1407`. | Wait on the LANE-A accessor unblock, then read the observer's captured payloads for the discriminator | queue item 4 in `prompts/LANE-UI.md`; no GT yet |
| NPC shop | buy/sell frames (LANE-A's `TradeCmdVital` family) | Open an NPC shop, buy/sell an item | `GT-230` OPEN (attended capture item, chief `20260904_0835`). Blocked on LANE-DB money/inventory interface (`20260904_0715`, queued behind all 5 PLAYER/CHARACTER pieces) | Wait on LANE-DB interface, then wire buy/sell | `GT-230` |
| Community/Party/Trade (8 classes) | friend/mail/party/trade vitals | Add friend, send mail, invite to party, propose trade | **SEAM OPEN; FIVE OF THE EIGHT NOW ANSWER** (rounds `spdxy0`, `xqxadg`, `m54yxh`, `asw0n3` and `ncejt8` -- see the `PartyInviteVital`, `TradeInviteVital`, `PartyCmdVital`, `Community_RequestBeFriendVital` and `Community_RemoveFriendVital` rows directly below; the other three are unchanged and still get `[]`, and the answered set is no longer all party/trade: `asw0n3` opened the `CommunityModule_Client` half of the seam and `ncejt8` took its second id) (round `gws4gs`, was "wiring CORE-REQUEST queued behind chief since 2026-09-04"). Layout resolved (`0400`/`1120` catalog). The eight frames have reached a dispatch branch since `1120`, but that branch could not reply: it fires a `lane_hooks` point, and `lane_hooks.fire()` returns `None` by construction, so no return value could exist. COO route (b) (`notes_to_chief/20260908_0142_COO-ROUND-0142-DECISIONS-*.md` item 3, answering this lane's letter `20260908_0031`) let this lane place the seam itself under chief review: `src/pirateforce_foundation/ui_dispatch.py` plus **three lines** in `runtime.py` (one import; a two-line `return ui_dispatch.answer(...)` replacing that branch's `return []`), pinned as a call by `tests/test_ui_dispatch.py::SeamIsRealTests`. It SHIPPED inert (the answerer registry was empty) -- **and that sentence is history, not behaviour, as of round `spdxy0`**: two of the eight now answer on a default flagless boot. **CORRECTION (round `xqxadg`, pf-adversary D4/D6)**: this cell went on citing `tests/test_lane_ui_friend_mail_party_trade_dispatch_wiring.py` as the standing proof of inertness for a whole round after that stopped being true, and that test is green only because its probe payload `b"\x00\x01"` is undecodable and never reaches an answerer -- measured: swap in a well-formed party invite and it fails with a real `UI_PARTY_INVITE_ANSWERED` action. The test's assertion message now says what it actually owns (an undecodable payload is answered with nothing). Fail-closed everywhere else (no answerer / module without `production_allowed`, re-read on every frame / answerer raises / return value that is not a list of `(label, pc, frame, delay)` tuples -- the whole batch refused, never filtered). 96 tests / 89 subtests (round `ihf029`; the `23 / 36` this cell carried was written against an earlier commit of the same branch and survived five more -- pf-adversary round 3, D6), including an end-to-end case where a registered answerer's action does come back out of `state.dispatch()` -- which is what makes today's `[]` a decision and not a dead branch. **No player could do anything new ON THE DAY THE SEAM LANDED**: no frame was composed. Nothing here knows what any of the eight vitals MEAN (letter `1120` nonclaim (2) still stands) -- and note that this is exactly the evidentiary situation in which the closest precedent (CHAT-CHANNEL-003) shipped scenario-gated with `production_allowed false`, while the two answerers on this seam ship on the default path. That difference is a decision to defend, not a detail (raised by pf-adversary round `xqxadg`). **pf-adversary round 3 (round `ihf029`) was NOT CLEAN and its four measured defects are fixed on this branch**, two of which defeated the seam's own safety claim. D1: the production gate read a fixed `sys._getframe(2)`, so an ordinary allowed helper exposing `def wire(vital_id, fn)` laundered a `production_allowed = False` lane's decision onto the wire under the helper's innocent name -- the gate now clears EVERY lane module on the registration stack plus `fn.__module__`, as a union with the registrar and never a substitution for it (substituting reopens round 2's forged-`__module__` hole, which this branch's own suite caught mid-fix). D2: the answerer was handed the live `session`, so it could arm `gm_warp_position_pending` directly while returning `[]` under a green token -- it now gets a sealed view with an explicit read allowlist (today empty) whose writes raise, and widening that tuple is a reviewed edit to one file instead of a rule each lane remembers. D3/D4: `isprintable()` admitted a space and every printable non-ASCII character, so a label could forge a second `SENT label=RUNTIME_RES_ACK_FIRST_REQ` inside a real evidence line (turning an attended round's Port Royal stage green from a party-invite frame) and could kill the v141 connection thread on the cp874 console AFTER `sendall` -- replaced by a positive grammar `^UI_[A-Z0-9_]{1,64}$`, which is the boundary the round-2 file asked for and could not answer. D5: the success token said `ANSWERED` while firing on validator acceptance before the wire, and prints `actions=0` for the blessed empty answer -- renamed `UI_DISPATCH_ACCEPTED`. D8/D10 were test defects (both parties gated in the yield test; a line census that forbade the module's name in a comment) and are fixed too. **pf-adversary round 4 then ran against those fixes and was ALSO NOT CLEAN, and it refuted one of this row's own claims**: the sentence here previously read "five mutants, one per fix, all killed", and round 4's M1 -- a one-hunk deletion of the D1 fix's entire stack walk -- SURVIVED, because the D1 test called the helper from the test method, so the gated lane never had a frame on the registration stack and only `fn.__module__` was catching it. Fixed this round with a test where the gated lane calls the helper itself and the helper wraps the answerer (so `fn.__module__` names the ALLOWED module and only the walk can catch it); M1 now dies. Round 4 also defeated the D2 fix outright: `_SealedSession` kept the real object in a slot and five one-liners walked past it (`object.__getattribute__`, the slot descriptor via `type()` and via `__class__`, `__reduce_ex__`, `gc.get_referents`), reproducing the D2 symptom verbatim under a green token -- replaced by `_SessionSnapshot`, a `tuple` of the allowlisted values COPIED OUT, which holds no session for any route to reach. D-D (a gated incumbent squatting a vital because the yield rule read the registrar while `answer()` read the whole gate), D-E (a helper in a non-`lane_*` file inside the package was gated forever with a reason naming a flag nothing reads), D-G (the label grammar is sound, but the foreign-substring list beside it had not folded in `wait_for_pf_stage.py`'s own bare needle, so `UI_PARTY_GAME_CONNECTED_ACK` turned a stage green) and D-H (`isinstance` where the file's own R7 lesson says `type`) are fixed too. Seven mutants this round, one per fix, all killed -- and this sentence is worth exactly as much as the last one was, which is why the mutants are named in the PR body. **STILL OPEN, and this PR is DRAFT because of it: D-A** -- the production gate is a stack walk, and a lane that appends to a table which an allowed module flushes later has no frame on that stack, so an unreviewed lane's bytes came out of the real `state.dispatch()` while discovery printed `SKIPPED_NOT_PRODUCTION_ALLOWED` for it. That is not patchable from inside this file: `production_allowed` is a property of a FILE, the gate is evaluated over a STACK, and what runs is a FUNCTION OBJECT that outlives every frame. Letter to COO this round asks who owns the unit of trust, with `_discover()`-driven declarative registration as this lane's proposal. **D-F** (a lane registering from a thread during `_discover()` deadlocks boot on the import lock `_say()` takes) is recorded, not fixed | Land the seam (chief review), then the first real answerer in this lane's own `lane_hooks/lane_ui_*.py`. **CORRECTION (round `ihf029`, letter `notes_to_chief/20260908_0453_LANE-UI-CORRECTION-the-eight-ui-classes-do-have-a-proven-read-path.md`)**: the wording this cell carried until now -- "the catalog resolves what the client sends, and no round has yet derived what the server must send back for any of the eight" -- was wrong about the committed artifact, and it was this lane's own sentence. `external/PF_SERIALIZER_FIELDS.tsv` carries `direction=R` rows for ALL EIGHT classes (3 / 2 / 3 / 3 / 9 / 4 / 3 / 3 rows, in this cell's own class order), and an R row is not a symmetry assumption written on paper: it cites a DIFFERENT helper VA from the W row beside it (`PartyInviteVital` field 3 is `target=0x0089A810 direction=W` writing and `target=0x0089A880 direction=R` reading, both `basis=exact_helper_bytes_and_pe_imports`), so the client image contains a real read path for each of the eight. The reply frame's SHAPE is therefore KNOWN and is not an RE question. What is still unknown is the FIELD SEMANTICS in the R direction (for `PartyInviteVital`: whether the u64 at tag `0x32` names the inviter or the invitee, and what the u8 at tag `0x08` selects) and what the client does on screen once it decodes -- and `external/PF_FIELD_VALIDATION.tsv` reads `observed_frames=0` in BOTH directions for all eight, so none of this has ever been seen on a live wire. The RE body is already with K (`notes_to_chief/20260908_0336_LANE-UI-TO-K-re-body-what-the-client-does-when-it-RECEIVES-the-eight-ui-vitals.md`) and the `0453` letter asks COO/K to narrow its scope to semantics only, so the RE runner is not spent re-deriving a layout item 3 already holds. The sentence this cell used to carry -- "until that answer lands NO BYTES GO OUT" -- is **no longer true and is kept here struck through rather than deleted**: bytes DO go out for two of the eight since rounds `spdxy0`/`xqxadg`. What still holds is the reason behind it: no field's MEANING is guessed, because the reply is the player's own payload re-encoded and refused unless byte-identical (`/warp x y`, ticket 1744, is what guessing costs). The one byte that is NOT the player's is the version byte -- see the answerer rows below and pf-adversary `xqxadg` D3. STATIC RE RUN THIS ROUND on the committed artifacts (letter `notes_to_chief/20260908_0502_LANE-UI-TO-K-re0336-addendum-one-disassembly-answers-two-classes.md`) adds two things the RE body did not know. (i) `PF_PROTOCOL_REGISTRY.tsv`'s `handler_va` CANNOT answer "is this called from the receive loop": its generator (`pf_extract_protocol.py:8500-8507`) reads the dword at vtable record `+12` and validates it on one test, that it points into an executable section -- nothing disassembles it, nothing looks for a caller -- and the census proves the column is not per-message: `PartyUpdateVital` / `PartyInviteVital` / `PartyCmdVital` / `PartyPickTypeSetting` have four different serializers and ONE shared `handler_va 0x0062EA70`, and the whole 519-row file resolves to just 194 distinct handler values (top bucket `0x00710440` covers 69 messages). The RE ticket must ask for a real xref on vtable `0x00F34958` slot `+12`, not re-ask the column. (ii) `PartyInviteVital` and `TradeInviteVital` SHARE ONE SERIALIZER BODY `0x00664550` -- same `span_sha256 81bb0c0e...`, same per-direction `file_off_claim` values, and two genuinely distinct pointer slots (`0x00B32D70` / `0x00B36544`) holding the same value, so the sharing is in the image and not an extractor dedup. One disassembly of `0x00664550` answers both `0x37B1` and `0x3700`; and any meaning assigned to the u8/u64/wstring must hold for a party invite AND a trade invite at once, which removes candidates (a "party slot" reading of the u8) without supplying an answer. `external/PF_TAG_CENSUS.tsv` reads `proven_semantics=UNKNOWN` for both `0x08` and `0x32`, and `Community_RequestBeFriendVital` puts its wstring at `+0x28` with a `0x0B` byte tag, so tag numbers carry no meaning across classes either | none yet |
| Party (1 of the 8 above) | `PartyInviteVital` `0x37B1` (c->s), answered with the SAME id (s->c) | Press the party-invite button in the shipped client UI and the server sends a frame back, instead of the silence every one of these eight vitals got before | **FIRST REAL ANSWER, PRODUCTION PATH, NO FLAG** (round `spdxy0`). **ROUND `1gc6hl` PAID THREE MORE pf-adversary FINDINGS, AND ONE OF THEM WAS A HOLE A PLAYER COULD FEEL. D7: the eight-vital branch is chosen by the frame's ID, so a peer that never logged in got an answer and could drain the module's process-wide budget until the button was silent for everyone -- measured through the real `state.dispatch()` on a connection that sent nothing but the vital. `ui_dispatch.answer()` now refuses before any answerer runs unless the session holds a selected character (`_SESSION_IN_GAME_READS`, the same precondition `runtime.py` already uses for in-game frames); four tests drive the real dispatcher with and without a login, and disabling the check turns them red. D9: registration was first-wins with `pkgutil.iter_modules` FILENAME ORDER deciding who was first, so a `lane_ui_aaa_*.py` shipping `production_allowed = True` could take this id and answer with its own bytes while every test stayed green -- `ui_dispatch._ANSWERER_OWNERS` is now a reviewed table binding each id to one lane module, and an id with no row cannot be taken by a lane at all. Residual, named not hidden: a registrar in `lane_hooks/` whose name does not start with `lane_` is not judged by that table (it is also not discoverable, so it cannot ship, but it is a route). **AND THE SAME ROUND'S pf-adversary PASS THEN BROKE THE OWNER TABLE (D-A, paid in the same round)**: `_ANSWERERS` is a module global, so one line in a `production_allowed = False` lane -- `ui_dispatch._ANSWERERS[0x37B1] = (owner_name, (owner_name,), forged)` -- made every name the seam reads say "the reviewed answerer" and put a `TeleportVital` frame on the wire under the party label with `UI_DISPATCH_ACCEPTED module=<the owner>` in the console, suite and arming proofs green. `answer()` now checks, when the stored registrar IS the id's reviewed owner, that the callable is BY IDENTITY one the owner module holds -- a name is copyable and `fn.__module__` is writable; being an attribute of an imported module object is neither. Registration accepts a helper lane doing the wiring only when what it wires is the owner's own callable. NOT closed, and stated rather than implied: a forger storing its OWN allowed lane name is judged by the production gate instead, and nothing stops a lane that writes into the owner module's namespace -- the question of what the unit of trust IS (file, module object, function object, dict entry) went to COO as a letter this round. D-B/D-C/D-F/D-G from that pass are PAID in round `vy1m79`; D-E (the shared vital census counting 32 where the truth is 38) is chief's by COO-DECISION `1441`. **ROUND `vy1m79` PAID D-B, D-C, D-F AND D-G FROM THE SAME PASS. D-B: the answer allowance was PROCESS-WIDE, so one correctly logged-in player pressing 32 times silenced the button for every other logged-in player until the server restarted -- the counter is gone from both lane modules and `ui_dispatch` now holds it PER SESSION (`SESSION_ANSWER_BUDGET`, keyed by session identity with a weakref callback dropping the row when the session is collected), read before the answerer runs and charged at the send point. D-F: a refusal now costs the session nothing (only a batch that carries actions is charged), and the lanes' `if shape is not None and len(...) >` -- a guard that vanished exactly when the registry row it guards went missing -- is now `if shape is None: refuse`. D-C: the registry's numbers are pinned against literals a test file owns (widening `max_frame_bytes` to a billion turns the suite red) and every `raise` in `_compose` is reached by a test. D-G: these test counts, and the party module's comment claiming a mutated version constant leaves the suite green -- which is no longer true. Nine mutants run this round, nine dead.** D5: the round-trip `not_byte_exact` refusal is DEAD CODE through today's decoder -- measured, 4,000 structurally valid payloads, every decodable one re-encodes byte for byte -- and the test that claimed to cover it was asserting only `reason=`; the test now names the refusal it really gets (`undecodable`) and a second test reaches the guard by making the encoder disagree, so the comparison cannot be deleted under a green suite.** `lane_hooks/lane_ui_party_invite_answer.py` (`production_allowed = True`) registers the first answerer the seam has ever had. What unblocked it is `RE-312` (pf_bridge `notes_to_chief/20260908_1038_RE-312-RESULT-*` and `20260908_1105_RE-312-RESULT-2-*`), which answered this lane's `0336` question off the shipped image: all eight classes are `INBOUND_YES` -- a real inbound handler in vtable slot `+0x1C` (NOT `+0x0C`, which the ticket's own addendum had assumed), the party family's being `0x0062EA70`, which looks up the `"PartyModule_Client"` window, type-checks it and hands the vital to `0x0062D0E0` -- and RESULT-2 pinned the caller at `0x005F38B2` inside the batch dispatch loop `0x005F3840`, so the slot is reached by the ordinary receive path. That is what makes answering with the SAME id an answer rather than a guess. The reply carries the player's OWN bytes: the payload is decoded with `ui_party_wire`'s pinned shape, re-encoded, and REFUSED unless byte-identical to what arrived, so this lane invents no field value -- letter `1120` nonclaim (2) and RE-312's own nonclaim 5 both still stand, `proven_semantics` is UNKNOWN, and naming `field2_u64` "the invited player" would be the guess this project forbids. Composition happens in `ui_dispatch.answer()`, not in the lane: a lane returns a `VitalReply(label, vital_id, version, payload, delay)` and the seam builds the frame, because handing a lane the session or a closure over it is not a boundary (pf-adversary rounds 3 D2 / 4 D-B: a reference IS reach) and this file may not name the frozen module that owns the envelope builder. A reply may only answer the id it was sent, and the composed action still faces `_actions_are_well_formed` unchanged. Storm guard: **round `vy1m79` moved it out of this module entirely** -- it was a process-wide `ANSWER_BUDGET = 32`, and the sentence that stood here ('process-wide and not per-session BECAUSE the seam hands an answerer no session identity') was true of the LANE and wrong as a conclusion: the seam sees the session. `ui_dispatch` now holds the allowance per (session, vital id), with a process ceiling (`PROCESS_ANSWER_BUDGET`) kept because pf-adversary measured that a per-session cap does not bound a client that RECONNECTS. `ANSWER_BUDGET` no longer exists anywhere in `src/`. Cost paid in `runtime.py`: the seam's own call line gains `envelope=legacy`, taking COO route (b)'s budget from three added lines to FOUR -- a lane assumption filed for COO this round, not a granted allowance. 25 tests / 10 subtests, two independent layers (the real `state.dispatch()` returns the action; the frame is compared to one built separately by `legacy.make_runtime_vitals`). | What is NOT proven is what the client DRAWS -- that is pixels, and this project decides pixels on a screen. Next: the GT ticket filed this round; then the same shape for the remaining seven, for which RE-312 BUILD_IMPACT 3 says the `Community_` five need `0x0063F9B0` read first (their five classes share one handler, so the type split is inside that function). | `RE-312` consumed; GT filed round `spdxy0` |
| Trade (1 of the 8 above) | `TradeInviteVital` `0x3700` (c->s), answered with the SAME id (s->c) | Press the trade-invite button in the shipped client UI and the server sends a frame back | **SECOND REAL ANSWER, PRODUCTION PATH, NO FLAG** (round `xqxadg`). **ROUND `1gc6hl` PAID THREE MORE pf-adversary FINDINGS, AND ONE OF THEM WAS A HOLE A PLAYER COULD FEEL. D7: the eight-vital branch is chosen by the frame's ID, so a peer that never logged in got an answer and could drain the module's process-wide budget until the button was silent for everyone -- measured through the real `state.dispatch()` on a connection that sent nothing but the vital. `ui_dispatch.answer()` now refuses before any answerer runs unless the session holds a selected character (`_SESSION_IN_GAME_READS`, the same precondition `runtime.py` already uses for in-game frames); four tests drive the real dispatcher with and without a login, and disabling the check turns them red. D9: registration was first-wins with `pkgutil.iter_modules` FILENAME ORDER deciding who was first, so a `lane_ui_aaa_*.py` shipping `production_allowed = True` could take this id and answer with its own bytes while every test stayed green -- `ui_dispatch._ANSWERER_OWNERS` is now a reviewed table binding each id to one lane module, and an id with no row cannot be taken by a lane at all. Residual, named not hidden: a registrar in `lane_hooks/` whose name does not start with `lane_` is not judged by that table (it is also not discoverable, so it cannot ship, but it is a route). D5: the round-trip `not_byte_exact` refusal is DEAD CODE through today's decoder -- measured, 4,000 structurally valid payloads, every decodable one re-encodes byte for byte -- and the test that claimed to cover it was asserting only `reason=`; the test now names the refusal it really gets (`undecodable`) and a second test reaches the guard by making the encoder disagree, so the comparison cannot be deleted under a green suite.** `lane_hooks/lane_ui_trade_invite_answer.py` (`production_allowed = True`). NO NEW RE WAS NEEDED, and that claim is re-measured rather than quoted: `RE-312` BUILD_IMPACT 2 says `PartyInviteVital` and `TradeInviteVital` share an encoder (u8 + u64 + tagged wstring, same tags, same order), and `tests/test_lane_ui_trade_invite_answer.py::SharedShapeTests` feeds the two encoders the same three field values and asserts the same 26 bytes come out. The same RESULT-1 / RESULT-2 argument the party row rests on covers this class too: `INBOUND_YES`, a live inbound handler at vtable slot `+0x1C`, reached from the batch dispatch loop at `0x005F38B2`. The reply carries the player's OWN bytes -- decoded with `ui_trade_wire`'s pinned shape, re-encoded, REFUSED unless byte-identical -- so no field is named: letter `1120` nonclaim (2) and RE-312 nonclaim 5 both stand, `proven_semantics` UNKNOWN, and `ui_trade_wire`'s own header keeps the two field types apart precisely because a shared SHAPE is not a shared MEANING. **ROUND `vy1m79`: this module's own process-wide `ANSWER_BUDGET = 32` is GONE (pf-adversary D-B).** It was separate from the party module's so a storm on one vital could not silence the other, which was true and still left the real hole: process-wide meant one logged-in player spending it silenced the button for every other logged-in player. `ui_dispatch` holds the allowance now, per (session, vital id) (`SESSION_ANSWER_BUDGET`), plus a process ceiling (`PROCESS_ANSWER_BUDGET`). **The first draft of this row said 'per session and still per label through the outbound registry', which was FALSE and pf-adversary measured it (D1): the registry separates shapes, not allowances, so 32 trade answers silenced the party button on the same session.** The per-vital half is now in the key. D-F is paid here too: the lane's `if shape is not None and len(...) >` is now `if shape is None: refuse`, so a renamed registry key refuses instead of skipping the check and dying inside `_compose`. It is a separate FILE and not a shared factory on purpose: the production gate's witness is the module that defines the body that RUNS (COO `20260908_1142` item 7, D-zeta), and a factory would move that body out of the module carrying the flag -- `SharedShapeTests` pins the two modules refusing on the same grounds so the duplication cannot drift silently. It adds NO new `make_runtime_vitals` call site (the lane returns a `VitalReply`; `ui_dispatch._compose` builds the frame), which is the property that shape exists to keep. 16 tests / 5 subtests, two independent layers, plus the arming proof `tools/pf_ui_trade_invite_answer_headless.py` -- in `tools/`, not `src/`, because `tests/test_npc_interaction_wire.py::QuestAndShopStateGuardTests` refuses trade vocabulary in a top-level foundation module and the answer to another lane's guard is not an allowlist entry (NOW `2050`) -- (`UI_TRADE_INVITE_ANSWER_ARMED ... frame_bytes=58 frame_matches=1 echo_is_the_players_bytes=1 junk_refused=1 RESULT=PASS`). | What is NOT proven is what the client DRAWS -- pixels are decided on a screen. Next: a GT ticket for this button, and the `Community_` five still need `0x0063F9B0` read first (RE-312 BUILD_IMPACT 3). | GT body not yet filed (round `xqxadg` filed the outbound-registry debt instead) |
| Party command (1 of the 8 above) | `PartyCmdVital` `0x2466` (c->s), answered with the SAME id (s->c) | Press a party COMMAND in the shipped client UI -- the controls that act on a party that already exists, beside the invite -- and the server sends a frame back instead of the silence the remaining six of these eight vitals still get | **THIRD REAL ANSWER, PRODUCTION PATH, NO FLAG** (round `m54yxh`). `lane_hooks/lane_ui_party_cmd_answer.py` (`production_allowed = True`). NO NEW RE WAS NEEDED: `RE-312` RESULT-1 lists `0x2466 PartyCmdVital INBOUND_YES handler=0x0062EA70` -- the SAME handler VA as `PartyInviteVital`, inside `"PartyModule_Client"` (`0x00F0DA14`) -- and RESULT-2 pins the caller at `0x005F38B2` inside the batch dispatch loop, so the vtable slot is reached by the ordinary receive path; that is what makes answering with the same id an answer rather than a guess. `ui_party_wire` already carried this class's proven layout (`u8` tag `0x08` + `u64` tag `0x32`, both directions, from `external/PF_SERIALIZER_FIELDS.tsv`), so nothing new was derived and no RE ticket was spent. The reply carries the player's OWN bytes -- decoded, re-encoded, REFUSED unless byte-identical -- so no field is named: letter `1120` nonclaim (2) stands and `proven_semantics` is UNKNOWN for both party classes. **WHAT IS DIFFERENT FROM THE TWO ROWS ABOVE: THIS PAYLOAD CANNOT VARY.** It is `u8 + u64` with no string, so every payload the lane can emit is exactly 11 bytes, and the reviewed outbound row pins the EXACT width (`max_payload_bytes=11`) instead of the 512-byte headroom the two wstring classes need -- the answerer therefore requires EQUALITY, not a ceiling: a ceiling nothing approaches is a check that never fires, and headroom nobody needs is reach nobody reviewed. The property that pin rests on is measured, not asserted: 4,000 random field pairs plus both ends of both fields all encode to 11 bytes, the reviewed literal is compared against the encoder's own width, and the equality check is driven from BOTH sides (a re-encoding one byte SHORT is refused with `reason=not_the_reviewed_fixed_width`, which a `>` ceiling would pass; and a registry row widened to 12 is refused too). `max_frame_bytes=64` is deliberately NOT exact -- the envelope is `legacy.make_runtime_vitals`'s and not this lane's, it measures 43 bytes on this commit, and pinning a number this file does not own would turn somebody else's envelope change into this button going silent. It inherits the seam's login gate (D7) and the per-(session, vital id) allowance (D-B/D1) rather than re-implementing either, and both inheritances are MEASURED here on the new id: a peer that never logged in gets nothing, a storm of 32 party commands does not silence the invite button of the same session, and a second session is not charged for the first one's storm. It adds NO new `make_runtime_vitals` call site (the lane returns a `VitalReply`; `ui_dispatch._compose` builds the frame). Two reviewed rows landed in the SAME commit as the answerer, which is the rule the owner table exists for: `_ANSWERER_OWNERS[0x2466]` (an id with no row cannot be taken by a lane at all -- the D9 thief is re-run against this id and loses) and `_OUTBOUND_FRAME_SHAPES['UI_PARTY_CMD_ANSWERED']`. **pf-adversary RAN ON THIS ROUND AND WAS NOT CLEAN (13 findings); the ones that could bite a player were paid in the same round.** **D1 (the one a player feels): the process ceiling above the per-session allowance was ONE counter for every session AND every vital, so this third answerer cost the two shipped buttons a third of the pot they share** -- measured: what one player could draw went 64 -> 96, sessions needed to empty the pot went 64 -> 43, and after that a bystander who had pressed nothing got nothing back until a restart. `PROCESS_ANSWER_BUDGET` is now counted PER VITAL ID (`_PROCESS_ANSWERS_SENT`), so a new button cannot spend an old one's allowance and the constant's own arithmetic (32 answers x 128 sessions) can be re-derived at HEAD again. **D3: the arming proof could not fail when the guard it exists to justify was deleted** -- `width_is_fixed` asked the encoder a question, not the answerer, so removing the whole width check left it printing PASS; it is now `width_is_enforced`, which widens the reviewed row inside the same boot, requires the press to answer nothing, restores the row and requires the button to work again (measured: with the guard deleted the token reads `width_is_enforced=0 RESULT=FAIL`). **D7: `_PARTY_CMD_PAYLOAD_BYTES` was dead** -- the guard read the registry only, so the constant whose comment claimed the module and the wire module 'cannot disagree' could be set to 999 with the button still working; the payload now has to match BOTH the encoder-derived width and the reviewed one. **D5/D2: the justification for `!=` over `>` was wrong on its face** -- `>` DOES catch a wire module that gains a field (12 > 11); what it misses is a field being LOST, or the row drifting permissively. And `!=` is not 'strictly safer': widening the reviewed row -- harmless everywhere else in that registry -- kills this button. That trade is taken on purpose and now says so, because the failure is loud (proof FAIL, suite red) instead of a silent widening of what may leave. **D6: the proof's second negative control was named for the wrong guard** (`require_exhausted` refuses the trailer, not the width pin). **D11: 'one byte that leaves is not the player's' was refuted by this round's own proof** -- 32 of the 43 bytes are the envelope; the claim is about the vital triple. **D12: RE-312 RESULT-2's own verdict on a version-byte mismatch (the client logs and CARRIES ON -- a warning) was carried as evidence without its conclusion.** **D8: three surviving mutants** -- the `delay` field, which `v141` SLEEPS ON in the connection thread with no bound anywhere, plus the accept token, are now pinned. **D9: the registry declined to pin 43 while the test file pinned it exactly** -- both stay, with the reason written down: a red test is a message to a person, a refusal is a dead button. **D10: the arming proof was tracked but not allowlisted in `.gitignore`** (a re-add would have made it vanish from CI); the allowlist row is added. **Still open and named, not fixed here: D13** -- three answered buttons across three rounds and not one of them appears in `docs/FUNCTIONAL_COVERAGE.json`; that is a shared file and a cross-round debt, filed rather than edited at the end of a round. 32 tests / 3 subtests, two independent layers (the real `state.dispatch()` returns the action; the frame is compared against one built separately by `legacy.make_runtime_vitals`), plus the arming proof `tools/pf_ui_party_cmd_answer_headless.py` -- in `tools/`, not `src/`, because a `make_runtime_vitals` call inside `src/` moves the shared `SRC_VITAL_STREAM_SITES` census pin whose own blind spot is under repair by chief (COO-DECISION `1441` item 2) -- (`UI_PARTY_CMD_ANSWER_ARMED answered=1 label=UI_PARTY_CMD_ANSWERED payload_bytes=11 frame_bytes=43 frame_matches=1 echo_is_the_players_bytes=1 junk_refused=1 width_is_enforced=1 RESULT=PASS`). | What is NOT proven is what the client DRAWS -- pixels are decided on a screen, and `observed_frames = 0` for this class as for the other seven. Next: one GT ticket covering all three answered buttons, once the chain reaches main and the arming tokens can be measured there. | GT body not yet filed (the chain is not on main, so a `HEADLESS_PROOF:` measured on this branch would not board the capture bus) |
| Friend request (1 of the 8 above) | `Community_RequestBeFriendVital` `0xB9E9` (c->s), answered with the SAME id (s->c) | Press "add friend" in the shipped client UI and the server sends a frame back instead of the silence the remaining four of these eight vitals still get | **FOURTH REAL ANSWER, PRODUCTION PATH, NO FLAG** (round `asw0n3`), and the FIRST of the five `CommunityModule_Client` ids -- the three before it were all party/trade. `lane_hooks/lane_ui_friend_request_answer.py` (`production_allowed = True`). NO NEW RE WAS NEEDED: `RE-312` RESULT-1 lists `0xB9E9 Community_RequestBeFriendVital INBOUND_YES handler=0x00645BF0 next=0x0063F9B0` inside `"CommunityModule_Client"` (`0x00F18854`), and RESULT-2 pins the caller at `0x005F38B2` inside the batch dispatch loop, so the vtable slot is reached by the ordinary receive path. `ui_friend_wire` already carried this class's proven layout (`u64` tag `0x32` + tagged wstring tag `0x48` + `u8` tag `0x0B`, from `external/PF_SERIALIZER_FIELDS.tsv`), so nothing new was derived and no RE ticket was spent. The reply carries the player's OWN bytes -- decoded, re-encoded, REFUSED unless byte-identical -- so no field is named and no friendship is created, stored or implied: nothing in the module touches a row, letter `1120` nonclaim (2) stands and `proven_semantics` is UNKNOWN for both friend classes. **WHAT IS DIFFERENT FROM THE PARTY COMMAND ROW ABOVE: this width is the player's again.** A tagged wstring makes the payload grow with the name, so the reviewed row is back to the 512/1024 CEILING the two wstring rows carry and the answerer compares with `>`; the party command's equality check would be wrong here (it would answer only the one name length a reviewer measured). **AND A CLAIM THIS ROUND MADE AND THEN DISPROVED ITSELF, BEFORE COMMITTING**: the module's first draft argued that a wstring class must have a LIVE round-trip guard, because the decoder READS a u32 length the encoder RECOMPUTES. Measured instead of argued: all 65,536 two-byte code units plus 200,000 random four-byte name payloads, of which the decoder accepted 251,145, re-encoded byte for byte -- ZERO mismatches. Strict UTF-16LE decode is injective and `read_untagged_wstring` refuses the two shapes that would break it (odd byte length, unpaired surrogate) before the codec is reached, so this guard is the same dead-code-for-a-later-decoder the button beside it carries, and the docstring now says so with the number. The scan is a test, not a paragraph. **WHAT THIS BUTTON HAS THAT THE OTHER THREE DO NOT: its id was ALREADY read on the production path.** `lane_hooks/lane_ui_friend_wire_log.py` (`production_allowed = True`) has decoded this exact class since round `4u0ncx` and printed a report-only line with `bytes_out=0`. `runtime.py` fires that hook and THEN calls `ui_dispatch.answer()`, so both still run -- measured end-to-end here (one press, both `LANE_UI_FRIEND_REQUEST decoded` and `UI_FRIEND_REQUEST_ANSWER len=` on stderr), because an answerer that silenced the watcher beside it would take away evidence an attended round depends on. It inherits the seam's login gate (D7) and the per-(session, vital id) allowance (D-B/D1, unit confirmed by COO-DECISION `20260908_1642`) rather than re-implementing either, and both inheritances are MEASURED on the new id: a peer that never logged in gets nothing, a storm of 32 friend requests does not silence the invite button of the same session, and a second session is not charged for the first one's storm. It adds NO new `make_runtime_vitals` call site. Two reviewed rows landed in the SAME commit as the answerer: `_ANSWERER_OWNERS[0xB9E9]` (the D9 thief is re-run against this id and loses with `reason=not_the_reviewed_owner`) and `_OUTBOUND_FRAME_SHAPES['UI_FRIEND_REQUEST_ANSWERED']`, both pinned a second time as literals in `tests/test_ui_dispatch.py`. **NO NEW ARMING RUNNER WAS WRITTEN, AND THAT IS THE POINT OF ROUND `ly40b5`'s DESIGN**: this module declares `ARMING_TOKEN` + `arming_sample()` in its own file, which is the whole cost of putting a button on the arming proof -- the runner that reads them landed with `#1167`'s recovery (round `t4nxwq`) and is now exercised end-to-end (`UI_SEAM_ANSWERS_ARMED_SUMMARY buttons=5 failed=0 RESULT=PASS` on branch, `head=b03cf522`), so what this round could pin is the half this lane owns (the token is ASCII for the cp874 console, and the sample is a frame this module's own guards accept -- a sample its own answerer refused would make the proof read FAIL for a working button). 27 tests in `tests/test_lane_ui_friend_request_answer.py`, two independent layers (the real `state.dispatch()` returns the action; the frame is compared against one built separately by `legacy.make_runtime_vitals`). | What is NOT proven is what the client DRAWS -- pixels are decided on a screen, and `observed_frames = 0` for this class as for the other seven. Next: this button joins the single GT ticket covering the answered buttons once `#1167`'s recovery PR (`#1190`) and the send-mail PR (`#1189`) both reach main and the arming token is re-measured there. | GT body not yet filed (same reason as the row above: a `HEADLESS_PROOF:` measured on a branch would not board the capture bus) |
| Community friend removal (1 of the 8 above) | `Community_RemoveFriendVital` `0x98A1` (c->s), answered with the SAME id (s->c) | Press "remove friend" in the shipped client UI and the server answers the frame instead of dropping it -- what the client DRAWS is unmeasured | **ANSWERED ON MAIN'S SEAM, UNOBSERVED ON A SCREEN** (round `ncejt8`): `lane_hooks/lane_ui_friend_remove_answer.py` echoes the player's own payload after a decode/re-encode round trip; `ui_dispatch` carries the reviewed owner row and outbound shape. Fixed width: `u64+u64+u8` is 20 bytes for every value the type can hold (measured over 4,000 triples incl. both u64 extremes), so the row states the exact width and the answerer requires EQUALITY, the same choice `PartyCmdVital` made. `RE-312` RESULT-1 lists it `INBOUND_YES handler=0x00645BF0 next=0x0063F9B0`, RESULT-2 pins the caller at `0x005F38B2`. 🔴 **What RE-312 did NOT settle and this row does not hide**: BUILD_IMPACT 3 says the five `Community_` ids share one vtable entry and split INSIDE `0x0063F9B0`, and nobody has read that function -- so "this id has a handler" is measured, "the split reaches a body for this id" is not. `observed_frames = 0` — และ **ยังไม่มีใครเห็นไคลเอนต์ *ส่ง* id นี้ด้วย** (`RE-312` nonclaim 2/4: `observed_frames = 0` ทั้งสองทิศ และใบปฏิเสธที่จะอ้างทิศ) ⇒ ประโยค "ปุ่มลบเพื่อนของไคลเอนต์ส่ง `0x98A1`" เป็นการอ่านของสายนี้ ไม่ใช่ของที่วัดแล้ว. **pf-adversary รอบ `ncejt8` ไม่ clean (14 ข้อ) และข้อที่กัดผู้เล่นจ่ายในรอบเดียวกัน**: F1 (ชุดเทสแดงจริงที่หมุดสองตัวของ `test_ui_dispatch.py` — หมุดเดียวกับที่ D4 ของ `m54yxh` เคยจับ) · F2 (`delay` ที่ `v141` `time.sleep` ในเธรดคอนเนกชัน ไม่มีใครปัก — ปักแล้ว มิวแทนต์ 30.0 แดง) · F3/F4 (เช็คความกว้างเทียบเลขเดียว และเทสแยก `!=` จาก `>` ไม่ออก — เพิ่มค่าคงที่ของโมดูลเอง + เทสขยับแถวทั้งสองทิศ 19 และ 512) · F5 (อ้าง nonclaim 5 ที่ผูกกับ serializer `0x00664550` ของ party/trade — คลาสนี้ serialize ที่ `0x006E7B20` ⇒ nonclaim ที่ถูกคือข้อ 3) · F6 (อ้างไฟล์ที่ไม่มีในรีโป) · F7 (ย่อหน้า "TWO … six" ในหัว `ui_dispatch` ผิดมาสามรอบ) · F8 (โทเคน `..._ANSWER` พิมพ์ก่อนสี่ด่านและ `sendall` ⇒ เปลี่ยนเป็น `..._ACCEPTED bytes_out=pending`) · F9 ("สองชั้น" ที่จริงเป็นชั้นเดียวเรียกสองครั้ง) · F10 (ไบต์ version ถูกไคลเอนต์เทียบที่ `0x005F3EF4` — ยกมาพร้อมข้อสรุปของใบแล้ว) · F11 (20 ไบต์เป็นสมบัติเชิงโครงสร้างจาก mask + `struct.pack` ไม่ใช่ผลของ 4,000 ตัวอย่าง) · F12 (ไม่มี arming proof ⇒ เขียนแล้ว `tools/pf_ui_friend_remove_answer_headless.py` + allowlist `.gitignore` ตาม D10). arming token: `UI_FRIEND_REMOVE_ANSWER_ARMED answered=1 label=UI_FRIEND_REMOVE_ANSWERED payload_bytes=20 frame_bytes=52 frame_matches=1 echo_is_the_players_bytes=1 junk_refused=1 width_is_enforced=1 own_width_is_read=1 RESULT=PASS` (วัดบนกิ่งรอบนี้ ยังไม่ใช่ค่าบน main — ต้องวัดซ้ำหลัง merge ก่อนขึ้นรถบัส) | GT body covering the four answered ids (this round wrote the coverage rows; the ticket is the next paper item) -- and it must carry the adversary's own question: what does `0x00645BF0` DO when it receives a friend vital carrying the receiving player's own id and name (`/warp x y`, letter `1744`, closed a client through a path that "arrived" the same way) | `docs/FUNCTIONAL_COVERAGE.json` row `ui_buttons.friend_remove_answer` |
| ALL outbound UI answers | n/a -- the send point of `ui_dispatch.answer()` | (nothing on screen by itself; it is what stops an unreviewed shape reaching a screen) | **DEBT PAID** (round `xqxadg`). COO-DECISION `20260908_1142` item 7 route (b) made the outbound frame-shape allowlist the bill of the PR that registers the first real answerer. `_OUTBOUND_FRAME_SHAPES` in `ui_dispatch.py` names, per label, the vital id it may answer, the reviewed version byte set, and a payload/frame byte budget; `_compose` spends the half a frame hides (version, payload length) and `_outbound_shapes_are_registered` spends the rest at the send point, on EVERY action -- a composed `VitalReply` and a hand-built 4-tuple face the same table, because gating only the composed half would be a gate with a door beside it. What it closes: before it, a lane with `production_allowed = True` could return any grammar-legal label carrying any bytes of any length and the seam would put it on the socket. There is deliberately NO `register_outbound_shape()`: adding an entry is a diff in `ui_dispatch.py`, the same rule `_SESSION_VIEW_FIELDS` lives by. 10 tests in `tests/test_ui_dispatch.py::UnregisteredOutboundShapeTests`, including a `str` subclass that lies to a dict lookup and a budget measured at the boundary in both directions. | Every future answerer on this seam carries an entry, or it cannot answer. | covered by the two answerer rows above |
| Options apply | settings vitals | Change a setting, see it take effect | RE ticket open, needs dynamic capture | wait for capture slot | none yet |
| Black market / ship survey window | 2 classes, fields incomplete | Open black-market or ship-survey UI | RE ticket open (`1137`), needs dynamic capture; fields incomplete even for static | wait for capture slot | none yet |
| Stall / Guild Storage | `Stall_*`/`GuildStorage_*` | Open personal stall / guild storage | Split as of round `gkxzei`: the three `Stall*Vital` classes are ANSWERED and implemented -- see the `Stall (3 of 3 `Stall*Vital` classes)` row below, which is now the single place this group's status is kept. `GuildStorageOpenVital` `0x5CAD` / `GuildStorageResultVital` `0x70D0` are still `UNTOUCHED` and still need field-by-field static RE; `RE-261` stays open and blocked on attended capture. | continue field-by-field static RE on the two `GuildStorage*` classes only | none yet |
| TreasureHunt (2 of 3 classes) | `TreasureHunt_StartExcavatingVital` `0xE40B` / `TreasureHunt_ExcavatingResultVital` `0xF33F` -- direction and caller/verb both `[UNKNOWN]`: `external/PF_FIELD_VALIDATION.tsv` rows for both classes read `status=NOT_OBSERVED`, `observed_frames=0` in both `W` and `R`, so nothing below claims which side sends which class or that either is an ack of the other -- that would be an unlabeled guess by analogy to the (separately, actually proven) `LogoutVital` ack pattern above, which this row does not make (pf-adversary D1, round `fzwt82`) | -- (server side only so far; no client-visible claim) | **HEADLESS-DONE, pure wire shape only**: `ui_treasurehunt_wire.py` encodes/decodes both classes field-for-field from `external/PF_SERIALIZER_FIELDS.tsv:6341-6360` (all ten rows fully tagged, no `CALL_UNCLASSIFIED`, `W`/`R` rows identical) -- 9 tests (`tests/test_ui_treasurehunt_wire.py`), round-trip + fail-closed (truncated/wrong-tag/trailing-bytes) all green. `TreasureHunt_UpdateSceneTreasurePointVital` (`0x6D75`, the group's third class) is explicitly NOT covered: its rows mix real tags with ten `CALL_UNCLASSIFIED`/`PE_IMPORT_*` entries per direction. No caller/verb semantics proven for any field (`proven_semantics` = `UNKNOWN` throughout) -- this is the same "รับเฟรม (decode) + ตอบ ack ที่วางเปล่า" scope as the eight `CORE-REQUEST 1120` classes, not business logic. | An attended capture (not a `CORE-REQUEST` hookup yet) is needed first to settle direction/verb before any dispatch wiring is proposed; `0x6D75`'s `CALL_UNCLASSIFIED` rows separately need static RE before that class can join this row | none yet |
| Gathering (2 of 3 classes) | `Gathering_StartGatheringVital` `0xAFF7` / `Gathering_GatheringResultVital` `0xBD8E` -- direction and caller/verb both `[UNKNOWN]`: `external/PF_FIELD_VALIDATION.tsv` rows for both classes read `status=NOT_OBSERVED`, `observed_frames=0` in both `W` and `R`, same as the `TreasureHunt` row above, so nothing below claims which side sends which class or that either acks the other | -- (server side only so far; no client-visible claim) | **HEADLESS-DONE, pure wire shape only**: `ui_gathering_wire.py` encodes/decodes both classes field-for-field from `external/PF_SERIALIZER_FIELDS.tsv:4315-4322` (`StartGathering`) and `:4323-4334` (`GatheringResult`) -- all rows fully tagged, no `CALL_UNCLASSIFIED`, `W`/`R` rows identical -- 9 tests (`tests/test_ui_gathering_wire.py`), round-trip + fail-closed (truncated/wrong-tag/trailing-bytes) all green. `Gathering_UpdateSceneGatheringPointVital` (`0x4966`, the group's third class) is explicitly NOT covered: its rows mix real tags with unresolved entries per direction (14 rows total, 10 `CALL_UNCLASSIFIED`/`PE_IMPORT_*`/atomic-helper entries and 4 real `0x12` tags). No caller/verb semantics proven for any field (`proven_semantics` = `UNKNOWN` throughout) -- same "รับเฟรม (decode) + ตอบ ack ที่วางเปล่า" scope as the sibling classes above, not business logic. | An attended capture (not a `CORE-REQUEST` hookup yet) is needed first to settle direction/verb before any dispatch wiring is proposed; `0x4966`'s `CALL_UNCLASSIFIED` rows separately need static RE before that class can join this row | none yet |
| Winemaking (3 of 5 classes) | `Winemaking_LearnFomulaVital` `0x972E` / `Winemaking_StartWinemakingVital` `0xC8EB` / `Winemaking_FinishWinemakingVital` `0xD4D1` -- direction and caller/verb all `[UNKNOWN]`: `external/PF_FIELD_VALIDATION.tsv` rows for all three classes read `status=NOT_OBSERVED`, `observed_frames=0` in both `W` and `R`, same as the `TreasureHunt`/`Gathering` rows above, so nothing below claims which side sends which class or that any acks another | -- (server side only so far; no client-visible claim) | **HEADLESS-DONE, pure wire shape only**: `ui_winemaking_wire.py` encodes/decodes all three classes field-for-field from `external/PF_SERIALIZER_FIELDS.tsv:4577-4580` (`LearnFomula`), `:4545-4552` (`StartWinemaking`), `:4553-4562` (`FinishWinemaking`) -- all rows fully tagged, no `CALL_UNCLASSIFIED`, `W`/`R` rows identical -- 15 tests (`tests/test_ui_winemaking_wire.py`), round-trip + fail-closed (truncated/wrong-tag/trailing-bytes) all green. `Winemaking_UpdateLearnedFormulaVital` (`0x0AEA`) and `Winemaking_UpdateWindPotSlotVital` (`0xE16E`), the group's other two classes, are explicitly NOT covered: their rows mix real tags with `CALL_UNCLASSIFIED`/`PE_IMPORT_INVALID_PARAMETER_NOINFO_CALL`/atomic-helper entries per direction, independently confirmed unproven in `pf_bridge/notes_to_chief/reference_codex_attr/PF_PROTOCOL_PRIORITY.md:113-114`. No caller/verb semantics proven for any field (`proven_semantics` = `UNKNOWN` throughout) -- same "รับเฟรม (decode) + ตอบ ack ที่วางเปล่า" scope as the sibling classes above, not business logic. | An attended capture (not a `CORE-REQUEST` hookup yet) is needed first to settle direction/verb before any dispatch wiring is proposed; the two excluded classes' `CALL_UNCLASSIFIED` rows separately need static RE before they can join this row | none yet |
| BuildingCrystal (11 of 13 classes) | `BuildingCrystal_PurchaseServiceVital` `0x0D27` / `OpenCrystalSlotVital` `0x0ED3` / `IncreaseCrystalSlotMaxNutrientVital` `0x1C37` / `AddCrystalLusterVital` `0x1D0C` / `SpeedUpBuildCrystalVital` `0x4942` / `InsertCrystalToSlotVital` `0x4D86` / `ExtractCrystalFailedVital` `0x59AB` / `ExtractCrystalFromSlotVital` `0x80A0` / `ExtractCrystalSucceededVital` `0x8E98` / `AddNutrientToCrystalSlotVital` `0xA3CD` / `DoAbsorbingVital` `0xD339` -- direction and caller/verb all `[UNKNOWN]`: `external/PF_FIELD_VALIDATION.tsv` rows for all 11 classes read `status=NOT_OBSERVED`, `observed_frames=0` in both `W` and `R`, same as the `TreasureHunt`/`Gathering`/`Winemaking` rows above, so nothing below claims which side sends which class or that any acks another | -- (server side only so far; no client-visible claim) | **HEADLESS-DONE, pure wire shape only**: `ui_buildingcrystal_wire.py` encodes/decodes all 11 classes field-for-field from `external/PF_SERIALIZER_FIELDS.tsv` (`:4085-4092`, `:4093-4102`, `:4103-4110`, `:4111-4120`, `:4121-4126`, `:4127-4136`, `:4137-4138`, `:4139-4146`, `:4147-4150`, `:4151-4154`, `:4155-4162`) -- all rows fully tagged, no `CALL_UNCLASSIFIED`, `W`/`R` rows identical -- 46 tests (`tests/test_ui_buildingcrystal_wire.py`), round-trip + fail-closed (truncated/wrong-tag/trailing-bytes) all green. `BuildingCrystal_UpdateCrystalSlotVital` (`0x2D5C`, `:4073-4084`) and `BuildingCrystal_UpdateNextAbsorbTime` (`:4163-4164`), the group's other two classes, are explicitly NOT covered: the first's rows mix real tags with `CALL_UNCLASSIFIED`/`DYNAMIC_INTERLOCKED_DECREMENT_ECX_PLUS_0C_VTABLE_PLUS_04`/`ATOMIC_INTERLOCKED_INCREMENT_ECX_PLUS_0C` entries per direction; the second has no vital id at all in `VITAL_REGISTRY_FROM_CLIENT_BINARY_20260817.tsv` (not a real client-facing action) even though its own field row is fully tagged. No caller/verb semantics proven for any field (`proven_semantics` = `UNKNOWN` throughout) -- same "รับเฟรม (decode) + ตอบ ack ที่วางเปล่า" scope as the sibling classes above, not business logic. | An attended capture (not a `CORE-REQUEST` hookup yet) is needed first to settle direction/verb before any dispatch wiring is proposed; `0x2D5C`'s `CALL_UNCLASSIFIED` rows separately need static RE before that class can join this row | none yet |
| Channel_ chat (10 of 17 classes; 5 owned by a sibling module, 2 deferred to LANE-CS) | `Channel_ForbidTalkNotificationVtial` `0xFDF2` / `Channel_WhisperVital` `0x556C` / `Channel_CustomChannelMessageVital` `0xE064` / `Channel_OriginalSinChannelMessageVital` `0x265C` / `Channel_JoinCustomChannelVital` `0xBA58` / `Channel_LeaveCustomChannelVital` `0xC663` / `Channel_OnActorJoinCustomChannelVital` `0x18DA` / `Channel_OnActorLeaveCustomChannelVital` `0x2770` / `Channel_JoinOriginalSinChannelVital` `0xFA07` / `Channel_LocalPerformanceVital` `0xAE8C` -- all `[UNKNOWN]` caller/verb semantics, `external/PF_FIELD_VALIDATION.tsv` reads `status=NOT_OBSERVED`, `observed_frames=0` for both `W`/`R` on every one of these ten (unlike `Channel_LocalTalkMessageVital`, which is separately `VALIDATED` with real captured frames -- that class is NOT this row's, see below) | -- (server side only so far; no client-visible claim) | **HEADLESS-DONE, pure wire shape only**: `ui_channel_wire.py` encodes/decodes all ten classes field-for-field from `reports/PF_CHAT_CHANNEL001_CHANNEL_FAMILY_AND_ROUTING_STATIC_20260818.md`'s byte-exact static table (lines 63-76) -- deliberately NOT from `external/PF_SERIALIZER_FIELDS.tsv`'s own `UNTAGGED_WSTRING16LE_LEN32LE` label for these same fields, which that report proves is coarser than reality (every wstring here actually carries a leading tag byte `0x48`, confirmed by disassembly of the wire codec at `0x89A810`/`0x89A880`) -- 36 tests (`tests/test_ui_channel_wire.py`), round-trip + fail-closed (truncated/wrong-tag/trailing-bytes) all green (`pf-adversary` this round: found the 10th class, `ForbidTalkNotification`, missing from an earlier draft that only accounted for 9 of 17, plus two missing trailing-bytes test cases -- both fixed same round, no second adversary call needed, wording/coverage-only fix). Five classes sharing serializer `0x65AD40` (`LocalTalk`/`Party`/`Guild`/`ActorBoardcast`/`GMGlobal`) are explicitly OUT of scope here -- a sibling module in this same package already owns them under its own ownership-gate test (ids/codec not duplicated; grepped and confirmed no collision). Two more (`Channel_JoinClassChannelVital`/`Channel_ClassChannelMessageVital`) are also explicitly OUT of scope: only the first is a sourced LANE-CS grep-hint in `prompts/COMMON_LANE_ROUND.md`, the second is this round's own judgment call to keep the pair together (flagged as such, not presented as a second citation). No caller/verb semantics proven for any field (`proven_semantics` = `UNKNOWN` throughout) -- same "receive frame (decode) + compose the same shape back (encode), no business logic" scope as the sibling wire modules above.  **2026-09-07 (round `0bzq9y`): five decoders in this module are no longer covered by the bridge-side cross-check.** `tools_bridge/pf_wire_fields_xcheck.py` reads them through the module-local helper `read_channel_tagged_wstring()`, whose body spells no recognised primitive, and it now reports them `UNRESOLVED OPAQUE-BODY` instead of guessing their fields from the `*TAG*` constants the helper body names: `decode_whisper_payload`, `decode_custom_channel_message_payload`, `decode_original_sin_channel_message_payload`, `decode_join_custom_channel_payload`, `decode_leave_custom_channel_payload`. Their round-trip tests are unchanged and still green; what is gone is the SECOND, independent check against the field table. Next step for this row: make the helper legible to the checker (or check the helper directly), then re-run the cross-check.| none yet |
| Activity (7 of 12 classes; 1 owned by LANE-GM, 4 excluded) | `Activity_NewActivityVital` `0x858C` / `Activity_ActivityStateChangedVital` `0xEE8D` / `Activity_ActorJoinActivityVital` `0xCA78` / `Activity_ActorLeaveActivityVital` `0xD6CA` / `Activity_UpdateActivityPointVital` `0xE5C9` / `ActorActivity_ClientReportActivityResultVital` `0xAA3F` / `ActorActivity_ResetDailyActivityResultVital` `0x83B1` -- all `[UNKNOWN]` caller/verb semantics (`proven_semantics` = `UNKNOWN` for every row in `external/PF_SERIALIZER_FIELDS.tsv`) | -- (server side only so far; no client-visible claim) | **HEADLESS-DONE, pure wire shape only**: `ui_activity_wire.py` encodes/decodes all seven classes field-for-field from `external/PF_SERIALIZER_FIELDS.tsv` (grep `^Activity_\|^ActorActivity_`); vital ids come from `pf_bridge/VITAL_REGISTRY_FROM_CLIENT_BINARY_20260817.tsv` (grep `Activity_`), the id-on-the-wire source per item 1 above, not the serializer table (no id column there) -- all rows fully tagged (`0x0B`=u8, `0x0F`/`0x12`=u16, `0x14`/`0x26`=u32, the last confirmed by `external/PF_TAG_CENSUS.tsv` row `0x26 len=4 FIXED`), no `CALL_UNCLASSIFIED`, `W`/`R` rows identical -- 29 tests + 14 subtests (`tests/test_ui_activity_wire.py`), round-trip + fail-closed (truncated/wrong-tag/trailing-bytes) all green. `Activity_CheatCodeVital` (`0x6CEC`), the group's other fully-tagged class, is explicitly NOT covered here -- already owned by LANE-GM (`src/pirateforce_foundation/gm/activity_cheat_code_wire.py`). `Activity_BasicVital`/`Activity_ActorCommandVital` (registry rows `UNKNOWN(registry_serializer_unresolved:getter_hits=0)`, no tag), `Activity_SendRankingVital` (`CALL_UNCLASSIFIED`/`PE_IMPORT_*`/atomic-increment entries mixed with real tags), and `ActorActivity_UpdateDailyActivityStateVital` (single row is `JUMP_UNCLASSIFIED:INDIRECT(...)`, no proven serializer at all) are excluded -- same exclusion policy as the sibling rows above. No caller/verb semantics proven for any field -- same "รับเฟรม (decode) + ตอบ ack ที่วางเปล่า" scope as the sibling wire modules above, not business logic. | An attended capture (not a `CORE-REQUEST` hookup yet) is needed first to settle direction/verb before any dispatch wiring is proposed; the four excluded classes separately need static RE before they can join this row | none yet |
| Pets (10 of 16 classes; 6 excluded) | `Pets_SummonPetVital` `0x4CEC` / `Pets_UnsummonPetVital` `0x5E3C` / `Pets_UpdatePetPropertyVital` `0x9B50` / `Pets_RestorePetAmityVital` `0x83B5` / `Pets_NotifySailorDeadVital` `0x8B12` / `Pets_MergePetsVital` `0x4C4D` / `Pets_MergePetsResultVital` `0x845C` / `Pets_ClaimPetsMegringItemVital` `0xB96F` / `Pets_LearnPetSkillVital` `0x6E55` / `Pets_UpdateSummonPetsTimeOutVital` `0xE28A` -- all `[UNKNOWN]` caller/verb semantics, same as every sibling row above | -- (server side only so far; no client-visible claim) | **HEADLESS-DONE, pure wire shape only**: `ui_pets_wire.py` encodes/decodes all ten classes field-for-field from `external/PF_SERIALIZER_FIELDS.tsv` (grep `^Pets_`); vital ids come from `pf_bridge/VITAL_REGISTRY_FROM_CLIENT_BINARY_20260817.tsv` (grep `Pets_`), the id-on-the-wire source per item 1 above -- all rows fully tagged (`0x0B`=u8, `0x0F`=u16, `0x14`=u32, `0x32`=u64), no `CALL_UNCLASSIFIED`, `W`/`R` rows identical -- 41 tests (`tests/test_ui_pets_wire.py`), round-trip + fail-closed (truncated/wrong-tag/trailing-bytes) all green. `Pets_MergePetsVital`'s 3rd/4th fields carry a `PHI(...)` `field_offset` (client-object address is a compiler-level merge of two call-site locations) instead of a plain `+0xNN` -- does not affect the wire shape, which is tag+len in `order` sequence only (see module docstring). `Pets_ChangePetEquipmentVital` (`0xA466`), `Pets_SetPetAIVital` (`0x4115`), `Pets_SetPetSkillVital` (`0x5C79`), `Pets_UpdateLearnedPetSkillVital` (`0xC574`), `Pets_UpdatePetsDataVital` (`0x76B9`), and `Pets_UpdatePetsMegringDataVital` (`0xC45C`), the group's other six classes, are explicitly NOT covered: each mixes real tags with `CALL_UNCLASSIFIED`/`PE_IMPORT_*` entries per direction. No caller/verb semantics proven for any field (`proven_semantics` = `UNKNOWN` throughout) -- same "รับเฟรม (decode) + ตอบ ack ที่วางเปล่า" scope as the sibling wire modules above, not business logic. | An attended capture (not a `CORE-REQUEST` hookup yet) is needed first to settle direction/verb before any dispatch wiring is proposed; the six excluded classes separately need static RE before they can join this row | none yet |
| Express (4 of 12 classes; 4 unresolved, 4 not in the serializer TSV at all) | `Express_ClientRemoveExpressVital` `0xD82D` / `Express_ClientSendExpressResultVital` `0x1091` / `Express_ClientcClaimExpressVital` `0xD5A8` / `Express_ResetExpressCountVital` `0xBECD` -- all `[UNKNOWN]` caller/verb semantics, `external/PF_FIELD_VALIDATION.tsv` reads `status=NOT_OBSERVED`, `observed_frames=0` for both `W`/`R` on all four, same as every sibling row above | -- (server side only so far; no client-visible claim) | **HEADLESS-DONE, pure wire shape only**: `ui_express_wire.py` encodes/decodes all four classes field-for-field from `external/PF_SERIALIZER_FIELDS.tsv` (grep `^Express_`); vital ids come from `pf_bridge/VITAL_REGISTRY_FROM_CLIENT_BINARY_20260817.tsv` (grep `Express_`), the id-on-the-wire source per item 1 above -- all rows fully tagged (`0x0B`=u8, `0x32`=u64, one `UNTAGGED_WSTRING16LE_LEN32LE`), no `CALL_UNCLASSIFIED`, `W`/`R` rows identical -- 25 tests + 8 subtests (`tests/test_ui_express_wire.py`), round-trip + fail-closed (truncated/wrong-tag/trailing-bytes) all green. `ClientRemoveExpressVital`/`ClientcClaimExpressVital` share an identical field table and span (shared serializer, same pattern as `ui_channel_wire.py`'s five shared-serializer classes) but keep separate dataclasses/ids. `Express_InitalizeActorExpressVital` (`0xF375`, 20/40 rows unresolved), `Express_ClientGetExpressItemAttrsVital` (`0x2C5D`, 12/22 unresolved), `Express_ClientReceiveNewExpressVital` (`0x0E0C`, 6/26 unresolved), and `Express_ClientSendExpressVital` (`0xBD0D`, 12/28 unresolved) are explicitly NOT covered: each mixes real tags with `CALL_UNCLASSIFIED`/`PE_IMPORT_*` entries. `Express_GetActorExpressDataFromDBVital`/`Express_GSConfirmActorExpressDataVital`/`Express_GSAddNewExpressVital`/`Express_GSSendSystemExpressVital` are in the registry but have zero rows in `PF_SERIALIZER_FIELDS.tsv` at all -- no wire layout known, needs static RE from scratch, not a field-completeness call. No caller/verb semantics proven for any field (`proven_semantics` = `UNKNOWN` throughout) -- same "receive frame (decode) + compose the same shape back (encode), no business logic" scope as the sibling wire modules above. | An attended capture (not a `CORE-REQUEST` hookup yet) is needed first to settle direction/verb before any dispatch wiring is proposed; the four unresolved classes separately need static RE, and the four TSV-absent classes need static RE from scratch, before they can join this row | none yet |
| CollectionObj (4 of 6 classes; 2 excluded) | `CollectionObj_CollectObjVital` `0xABA4` / `CollectionObj_GetCollectEffectVital` `0xF851` / `CollectionObj_SailorLevelUpRequestVital` `0x3B06` / `CollectionObj_SailorLvUpResponseVital` `0x1B8B` -- all `[UNKNOWN]` caller/verb semantics, `external/PF_FIELD_VALIDATION.tsv` reads `status=NOT_OBSERVED`, `observed_frames=0` for both `W`/`R` on every one of these four, same as every sibling row above | -- (server side only so far; no client-visible claim) | **HEADLESS-DONE, pure wire shape only**: `ui_collectionobj_wire.py` encodes/decodes all four classes field-for-field from `external/PF_SERIALIZER_FIELDS.tsv` (grep `^CollectionObj_`, lines 5279-5316); vital ids come from `pf_bridge/VITAL_REGISTRY_FROM_CLIENT_BINARY_20260817.tsv` (grep `CollectionObj_`), the id-on-the-wire source per item 1 above -- all rows fully tagged (`0x0B`=u8, `0x12`=u16, `0x32`=u64), no `CALL_UNCLASSIFIED`, `W`/`R` rows identical, each class its own distinct span (no shared serializer here) -- 22 tests + 8 subtests (`tests/test_ui_collectionobj_wire.py`), round-trip + fail-closed (truncated/wrong-tag/trailing-bytes) all green. `CollectionObj_UpdateCollectEffectVital` (`0x254E`, 10/14 rows unresolved: `PE_IMPORT_INVALID_PARAMETER_NOINFO_CALL`/`CALL_UNCLASSIFIED`) and `CollectionObj_UpdateCollectionObjBagVital` (`0x51A2`, 10/12 rows unresolved: `CALL_UNCLASSIFIED:INDIRECT(...)`/`DYNAMIC_INTERLOCKED_*`/`ATOMIC_*`) are explicitly NOT covered. The sibling `KnowledgeGuru_` group (5 classes) was checked this round and found NOT eligible under this same rule: every one of its five classes carries an unresolved `SUBCALL:0x0069F980` first field (a call into another, unresolved sub-serializer -- same "shape unknown" meaning as `CALL_UNCLASSIFIED` for this lane's purposes), so none qualify as fully tagged; `HitParade_` has zero rows in `PF_SERIALIZER_FIELDS.tsv` at all. No caller/verb semantics proven for any field (`proven_semantics` = `UNKNOWN` throughout) -- same "receive frame (decode) + compose the same shape back (encode), no business logic" scope as the sibling wire modules above. | An attended capture (not a `CORE-REQUEST` hookup yet) is needed first to settle direction/verb before any dispatch wiring is proposed; the two excluded classes separately need static RE before they can join this row | none yet |
| Community (16 of 38 classes; 5 owned by a sibling module, 8 excluded, 9 no-layout) | `Community_ChangeActorCommentVital` `0xE1AB` / `Community_ChangeActorPenNameVital` `0xDCCD` / `Community_ChangeActorPersonalDataVital` `0x26D5` / `Community_CommunityCommandNotAllowVital` `0x3C4D` / `Community_CommunityPropertyChangedVital` `0x3E50` / `Community_OpenLetterInABottleVital` `0xEBF8` / `Community_OpenPenpalLetterVital` `0xC8D5` / `Community_RemoveBlackListVital` `0xBA6D` / `Community_ReplyLetterInABottleVital` `0xFAF1` / `Community_RequestorConfirmSoulMateMatchVital` `0x8E24` / `Community_SetReceiveActiveChangeVital` `0x1666` / `Community_TargetConfirmSoulMateMatchVital` `0x57BC` / `Community_ThrowLetterInABottleVital` `0xFB51` / `Community_ThrowPenpalLetterVital` `0xD72C` / `Community_UseBlankPenpalLetterVital` `0xFD4B` / `Community_WriteBlankPenpalLetterVital` `0x1B3C` -- all `[UNKNOWN]` caller/verb semantics, `external/PF_FIELD_VALIDATION.tsv` reads `status=NOT_OBSERVED`, `observed_frames=0` for both `W`/`R` on all sixteen, same as every sibling row above | -- (server side only so far; no client-visible claim) | **HEADLESS-DONE, pure wire shape only**: `ui_community_social_wire.py` encodes/decodes all sixteen classes field-for-field from `external/PF_SERIALIZER_FIELDS.tsv` (grep `^Community_`); vital ids from `pf_bridge/VITAL_REGISTRY_FROM_CLIENT_BINARY_20260817.tsv` (grep `Community_`), the id-on-the-wire source per item 1 above -- all rows fully tagged (`0x32`=u64, `0x0B`=u8, `0x08`=u8 alternate flavour, `0x14`=u32, and every `UNTAGGED_WSTRING16LE_LEN32LE` row encoded as a TAGGED wstring, tag `0x48`, per `pf_bridge/notes_to_chief/reference_codex_attr/PF_A2_STRING_WIRE_TAG_DELTA.tsv`'s `corrected_tag` column -- migration finished round `d1b231`, every one of the module's 16 W/R wstring field pairs checked against that table by class name first), no `CALL_UNCLASSIFIED`, `W`/`R` rows identical -- 75 tests + 76 subtests (`tests/test_ui_community_social_wire.py`), round-trip + fail-closed (truncated/wrong-tag/trailing-bytes) all green, plus a table-driven byte-level check that every wstring field of every class carries the `0x48` tag and that zeroing any one of those tag bytes fails the decode closed (proven by mutation: a full revert = 21 failed, dropping a table row = 1 failed; the same sentence also claimed a single-field revert = 2 failed, CORRECTED round `fvp9ke` after pf-adversary re-derived it: 2 holds only for the three round `on8hbb` classes, which still carry their own per-class method on top of the table; for the ten classes round `d1b231` migrated it is 1 failed. Round `fvp9ke` added two more table guards -- exact-position and row-to-encoder binding -- each proven by its own mutant). Five sibling classes (`RequestBeFriendVital`/`RemoveFriendVital`/`SendMailVital`/`GetMailContentVital`/`DeleteMailVital`) are explicitly OUT of scope here -- already owned by `ui_friend_wire.py`/`ui_mail_wire.py` under `CORE-REQUEST 1120`. `AddBlackListVital`/`AddFriendVital`/`GetActorVowLockListVital`/`InitalizeActorCommunityVital`/`ReceiveNewMailVital`/`ReplyPenpalLetterVital`/`RequestSoulMateMatchVital`/`UpdateActorVowLockVital` (8 classes) are explicitly NOT covered: each mixes real tags with `CALL_UNCLASSIFIED`/`PE_IMPORT_*` entries per direction (checked this round via `awk` row counts). The remaining 9 registry rows (`GSRequestChangeFriendshipValueVital`/`GetActorCommunityDataFromDBVital`/`QuerySoulMateCandidateVital`/`SSRequestLetterInABottleDataVital`/`SSRequestPenpalLetterDataVital`/`SendSystemMailVital`/`UpdateActorCommunityDataToDBVital`/`UpdateLetterInABottleDataToDBVital`/`UpdatePenpalLetterDataToDBVital`) have zero rows in `PF_SERIALIZER_FIELDS.tsv` at all -- no wire layout known, needs static RE from scratch. No caller/verb semantics proven for any field (`proven_semantics` = `UNKNOWN` throughout) -- same "receive frame (decode) + compose the same shape back (encode), no business logic" scope as the sibling wire modules above. | An attended capture (not a `CORE-REQUEST` hookup yet) is needed first to settle direction/verb before any dispatch wiring is proposed; the eight excluded classes separately need static RE, and the nine TSV-absent classes need static RE from scratch, before they can join this row | none yet |
| Appraisal (2 of 2 classes -- full) | `AppraisalVital` `0x2AB1` / `AppraisalStopVital` `0x45CF` -- all `[UNKNOWN]` caller/verb semantics, `external/PF_FIELD_VALIDATION.tsv` reads `status=NOT_OBSERVED`, `observed_frames=0` for both `W`/`R` on both, same as every sibling row above | -- (server side only so far; no client-visible claim) | **HEADLESS-DONE, pure wire shape only**: `ui_dyeing_appraisal_relive_wire.py` encodes/decodes both classes field-for-field from `external/PF_SERIALIZER_FIELDS.tsv` (grep `^AppraisalVital`, `^AppraisalStopVital`); vital ids from `pf_bridge/VITAL_REGISTRY_FROM_CLIENT_BINARY_20260817.tsv` (grep `Appraisal`) -- all rows fully tagged (`0x08`=u8, `0x14`=u32, `AppraisalStopVital` is an EMPTY 0-field payload both directions), no `CALL_UNCLASSIFIED`, `W`/`R` rows identical -- this is the full 2-class `Appraisal` group, nothing excluded. Combined with the `Dyeing`/`Relive` rows below in one module + one test file to stay under the ~6-files-per-PR cap (`AGENTS.md` section 7). No caller/verb semantics proven for any field -- same "receive frame (decode) + compose the same shape back (encode), no business logic" scope as the sibling wire modules above, not business logic. | An attended capture (not a `CORE-REQUEST` hookup yet) is needed first to settle direction/verb before any dispatch wiring is proposed | none yet |
| Dyeing (4 of 4 classes -- full) | `DyeingVitalRes` `0x2A00` / `DyeingRemoveVital` `0x3E1C` / `DyeingShipVitalReq` `0x441A` / `DyeingVitalReq` `0x29E4` -- all `[UNKNOWN]` caller/verb semantics, same `NOT_OBSERVED`/0-frame status as every sibling row above | -- (server side only so far; no client-visible claim) | **HEADLESS-DONE, pure wire shape only**: `ui_dyeing_appraisal_relive_wire.py` encodes/decodes all four classes field-for-field from `external/PF_SERIALIZER_FIELDS.tsv` (grep `^DyeingVitalRes`, `^DyeingRemoveVital`, `^DyeingShipVitalReq`, `^DyeingVitalReq`); vital ids from `pf_bridge/VITAL_REGISTRY_FROM_CLIENT_BINARY_20260817.tsv` (grep `Dyeing`) -- all rows fully tagged (`0x08`=u8, `0x32`=u64, `0x14`=u32, `0x44`=tagged string8), no `CALL_UNCLASSIFIED`, `W`/`R` rows identical. `DyeingVitalReq` (the group's 4th class) was resolved this round (round `42w728`): its 2nd field carries the `UNTAGGED_STRING8_LEN32LE` label, which `src/pirateforce_foundation/logout_hypothesis.py`'s `ReturnSelectServerVital` erratum (RE-196/GT-055) already proved is not safe to trust at face value on its own -- the same label sits on `DeleteActorVital`'s string field, which GT-018 independently confirmed DOES carry a real 0x44 tag byte the label's helper-call span does not cover. This round's static-RE pass (cloud-clone-only, no client binary needed) found the answer already committed: `notes_to_chief/reference_codex_attr/PF_A2_STRING_WIRE_TAG_DELTA.tsv:362-363` records `DyeingVitalReq`'s string call sites as helper-VA/span-end/SHA-256-IDENTICAL to `DeleteActorVital`'s own proven string field (same table, line 18), with the same `push_0x44` tag instruction recorded for both -- so the GT-018 finding transfers by construction (byte-identical shared helper), not by name-similarity guesswork. This group is now full: no class excluded. | none -- group complete; an attended capture (not a `CORE-REQUEST` hookup yet) is needed before any dispatch wiring is proposed | none yet |
| Relive (1 of 2 classes; 1 excluded) | `ReliveVital` `0x1AD4` -- `[UNKNOWN]` caller/verb semantics, same `NOT_OBSERVED`/0-frame status as every sibling row above | -- (server side only so far; no client-visible claim) | **HEADLESS-DONE, pure wire shape only**: `ui_dyeing_appraisal_relive_wire.py` encodes/decodes this class field-for-field from `external/PF_SERIALIZER_FIELDS.tsv` (grep `^ReliveVital`); vital id from `pf_bridge/VITAL_REGISTRY_FROM_CLIENT_BINARY_20260817.tsv` (grep `Relive`) -- both rows fully tagged (`0x08`=u8, `0x05`=u8, the latter a tag value new to this codebase, documented in `ui_social_wire.py`'s TAG LEGEND), no `CALL_UNCLASSIFIED`. `ReliveMarkerVital` (`0x3DD6`, the group's other class) is explicitly NOT covered: it mixes real tags with `SUBCALL:`/`CALL_UNCLASSIFIED:0x004B1C40`/`DYNAMIC_INTERLOCKED_*`/`ATOMIC_*` entries (10 of 26 total rows unresolved, both directions -- corrected this round after pf-adversary caught the module's first draft miscounting this as "6 of 15"; a closed ticket, `RE-112`, separately confirms `NOT_OBSERVED`/0-frame capture validation for this class, consistent with this exclusion). | An attended capture (not a `CORE-REQUEST` hookup yet) is needed first to settle direction/verb before any dispatch wiring is proposed; `ReliveMarkerVital`'s unresolved rows separately need static RE before that class can join this row | none yet |
| Stall (3 of 3 `Stall*Vital` classes) | `StallStartVital` `0x30FE` (22 W rows / 22 R rows) / `StallOpenVital` `0x2A3E` (20 W / 20 R) / `StallOperateVital` `0x3DE4` (13 W / 13 R) -- `external/PF_SERIALIZER_FIELDS.tsv:6807-6890` and `:6891-6916`. Serializer VAs are `0x0076A740` / `0x0076A960` / `0x0076A630`. CORRECTION (`RE-294`, round `gkxzei`): the VAs this row previously called serializers -- `0x0076AC20` / `0x0076ACB0` / `0x0076A080` -- are the `getter_va` column of `PF_PROTOCOL_REGISTRY.tsv` (`mov ax, ds:...; ret`, a class-id getter that writes nothing), and the `0x0076B0D0` all three share is the `handler`, not a shared serializer. | the player's own market stall: open it, put an item in a priced slot, another player buys | **HEADLESS-DONE, pure wire shape only** (round `gkxzei`, was `NEEDS-RE-STATIC`). `RE-294 STALL-VITAL-TAIL-CALLS-WRITE-BYTES-OR-NOT-001` is ANSWERED (`notes_to_chief/20260907_1350_RE-294-RESULT-stallopen-writes-nothing-extra-stallstart-writes-via-766C00.md`): the `PE_IMPORT_*` / `CALL_UNCLASSIFIED:` / `MUTATING_CHAIN_*` / `ATOMIC_*` sites after the tagged prefix do NOT write into the serializer stream (they are MSVC's invalid-parameter reporter, `InterlockedIncrement`/`Decrement` on a refcount at `this+0x0C`, and a container insert taking two stack locals), so `StallOpenVital`'s tagged rows ARE the whole frame; `StallStartVital` and `StallOperateVital` additionally write through `SUBCALL:0x00766C00`, which the same RE round decoded end to end (four tagged writes per container member: `0x32`@`elem+0x10`, `0x14`@`+0x18`, `0x0F`@`+0x1C`, `0x19`@`+0x20`). `src/pirateforce_foundation/ui_stall_wire.py` encodes/decodes all three classes on that answer -- `StallOpen` u64/u16/wstring/u32/u16/u16 (5 tagged + 1 string, matching RE-294's own call census for `0x0076A960` exactly), `StallStart` u8/u16/wstring/u16/u16 + N member records, `StallOperate` u8/u64/wstring/u32/u8 + N member records, member record 22 bytes. 46 tests / 232 subtests (`tests/test_ui_stall_wire.py`), round-trip + fail-closed (per-byte truncation, per-byte wrong tag, trailing bytes) green. Round `splep7` added the explicit-window entry points `decode_stall_{open,start,operate}_payload_at(buf, offset, length)` (the reviewer counter-proposal that closed `pirate-force-server#1045`): the caller declares the payload boundary instead of slicing for it, and the three whole-buffer decoders now delegate to them, so there is one parse body per class. What that does and does not buy is MEASURED and kept in the suite as two negative results, after pf-adversary caught this row's first draft claiming the opposite: a wrong boundary is silent in BOTH directions at member-record granularity on `StallStart`/`StallOperate` -- N whole 22-byte member records too many decode as N extra members, N too few as N fewer -- so the window form does NOT make a mis-declared length safe; only a boundary that does not land on a record edge fails closed (`StallOpen` has no member tail, so for it any surplus or shortfall fails closed). What the window form does buy is that the boundary is two arguments this module range-checks (out of range / negative / non-integer all return `None`, never raise through the public surface) instead of an implicit property of a slice made elsewhere. Still no caller: the counter-proposal's other half -- reviving `require_exhausted` for the two member-tail classes -- is NOT delivered (deleting both calls leaves the suite green), and nothing here derives a vital body's end from the bytes, so the declared length is a question for the framing layer. NOT wired into `runtime.py`/`vital_walk.py`, no field claims a meaning, no direction claimed (`PF_FIELD_VALIDATION.tsv` = `NOT_OBSERVED` both directions, all three classes). Census tier stays `NAME-ONLY`: the census counts CODE TOKENS and these names appear in the module only in prose, exactly like every sibling wire module (`Gathering_StartGatheringVital` reads `NAME-ONLY` with its module shipped, while `PartyInviteVital` reads `SOURCE` only because `runtime.py` spells it at a dispatch site) -- so the artifact does NOT drift and the `n/327` bar does not move. | Two open threads, both recorded, neither blocking the module: (a) `RE-294` reports `primitive 13/13/3/3` for `0x0076A630` while the table lists 4 tagged + 1 string per direction -- letter to COO, round `gkxzei`; (b) `RE-294` explicitly declines to confirm R77's per-field layout for that class, so the table's `order` column is what this module follows. Next: an attended capture to settle direction/verb before any dispatch wiring is proposed. | none yet -- a GT can only be written once direction is settled; `GT-262` stays cancelled |
| everything else in item 1's 15 non-core groups not listed above (Equipment_/KnowledgeGuru_/HitParade_/NavigationEx_/UserSetting/Vehicle/Potion/ItemLock) | -- | -- | **NOT YET ITEMIZED** -- this table currently covers the rows this lane's rounds have actually touched (per the catalog letters `20260904_0400`/`0453`/`1159`/`1244`). Each future round expands only the row(s) it works on; do not re-derive all 327 rows in one sitting. `KnowledgeGuru_` (5 classes, the in-game quiz) is now MEASURED rather than restated, round `splep7`: every one of the five carries the SAME unresolved `SUBCALL:0x0069F980` at `+0x00` (one row per direction) and NOTHING ELSE unresolved -- `ActorAnswerQuizVital` `0xF1D4` 2/8 rows unresolved, `ActorUseCardVital` `0xC3D6` 2/10, `UseHalfProbabilityCardResultVital` `0xC546` 2/10, `CurrentQuizResultVital` `0x11E4` 2/12, and `NewQuizVital` `0x8E17` 4/22 (which adds `SUBCALL:0x006A2900` at `+0x18`, the nested object holding its five wstrings and two u8s). Every other row in the group is a real tag (`0x14`=u32, `0x0B`=u8, `0x0F`=u16, `0x26`=u32 fixed-4 per `external/PF_TAG_CENSUS.tsv`, `UNTAGGED_WSTRING16LE_LEN32LE`), `W`/`R` identical, and the first tagged field sits at `+0x14` exactly like every implemented sibling class. So ONE VA unblocks four of the five classes and a second unblocks the fifth -- the highest-yield RE target this lane has: RE body sent to LANE-K round `splep7` (`pf_bridge/notes_to_chief/20260908_*_LANE-UI-TO-K-re-body-knowledgeguru-shared-prefix-0069F980.md`). Grepped first per `AGENTS.md` section 7: no hit for `KnowledgeGuru` in `GAME_TEST_QUEUE.md` or `CLIENT_RE_QUEUE.md` (0 lines each), so no ticket existed for it before that letter. `UserSetting` (1 row) and `ItemLock` (1 row) were checked this round and found NOT eligible: both mix real tags with `CALL_UNCLASSIFIED`/`PE_IMPORT_INVALID_PARAMETER_NOINFO_CALL`/`DYNAMIC_INTERLOCKED_*`/`ATOMIC_*` entries (see `ui_dyeing_appraisal_relive_wire.py`'s module docstring for the row-level detail). `Vehicle`/`Potion` were also checked this round: zero rows match those exact prefixes in `VITAL_REGISTRY_FROM_CLIENT_BINARY_20260817.tsv` -- the only related classes on the wire are `CVehicle*`/`CPotion*`, which `prompts/COMMON_LANE_ROUND.md`'s grep-hint table assigns to LANE-A/LANE-B respectively, not this lane. | pick the next `LAYOUT-KNOWN` row from `external/PF_SERIALIZER_FIELDS.tsv` when the queue above is blocked | -- |

## Nonclaims

- This table's group-level statuses are a compaction of catalog letters
  already sent to COO 2026-09-04 (`0400` 15-row survey, `0453`/`1159`
  status, `1244` closure at 14/15) -- it does not re-verify every one of
  those 327 rows fresh this round; a stale sub-claim in the source
  letters would still be stale here until a round re-checks it.
- The `LogoutVital` exit-game row's "HEADLESS-DONE" status is a claim
  about server-side behavior only (ack bytes, DB session row, relogin),
  proven against a real `SQLiteStore` and the real `pf_login_game_server_v141`
  legacy parser/composer -- it does NOT claim anything about what a real
  client does when the FIN actually arrives (no live boot performed this
  round; COO-DECISION `20260905_1352`'s no-more-guessing-boots rule is
  about the character-select transition, not this).
- The auto-walk row's "log-only observer" claim is unchanged from
  `CORE-REQUEST-025`'s own docstring in `runtime.py` -- not re-verified
  independently this round.
- The `TreasureHunt` row's "HEADLESS-DONE" is a wire-shape claim only (pure
  encode/decode functions, checked against the registry's own field rows) --
  it does not claim a live capture confirms these shapes against a real
  client frame, and it does not claim any field's meaning, or which side
  (client/server) sends either class, or that either class acks the other
  (`PF_FIELD_VALIDATION.tsv` shows `NOT_OBSERVED`/zero frames for both).
- The `Gathering` row's "HEADLESS-DONE" carries the identical nonclaim as
  the `TreasureHunt` row immediately above, for the same reason
  (`PF_FIELD_VALIDATION.tsv` shows `NOT_OBSERVED`/zero frames for both
  `Gathering_StartGatheringVital` and `Gathering_GatheringResultVital`).
- The `Winemaking` row's "HEADLESS-DONE" carries the identical nonclaim as
  the `TreasureHunt`/`Gathering` rows above, for the same reason
  (`PF_FIELD_VALIDATION.tsv` shows `NOT_OBSERVED`/zero frames for
  `Winemaking_LearnFomulaVital`, `Winemaking_StartWinemakingVital`, and
  `Winemaking_FinishWinemakingVital`).
- The `BuildingCrystal` row's "HEADLESS-DONE" carries the identical nonclaim
  as the `TreasureHunt`/`Gathering`/`Winemaking` rows above, for the same
  reason (`PF_FIELD_VALIDATION.tsv` shows `NOT_OBSERVED`/zero frames for all
  11 of `BuildingCrystal_PurchaseServiceVital`, `OpenCrystalSlotVital`,
  `IncreaseCrystalSlotMaxNutrientVital`, `AddCrystalLusterVital`,
  `SpeedUpBuildCrystalVital`, `InsertCrystalToSlotVital`,
  `ExtractCrystalFailedVital`, `ExtractCrystalFromSlotVital`,
  `ExtractCrystalSucceededVital`, `AddNutrientToCrystalSlotVital`, and
  `DoAbsorbingVital`).
- The `Channel_` chat row's "HEADLESS-DONE" is a wire-shape claim only, and
  unlike the `TreasureHunt`/`Gathering`/`Winemaking`/`BuildingCrystal` rows
  above it is sourced from a byte-exact STATIC disassembly report (grade A,
  reproducible spans/hashes), not from `PF_SERIALIZER_FIELDS.tsv` directly --
  it does not claim any of these ten classes has ever been seen on the wire
  in a live capture (`PF_FIELD_VALIDATION.tsv`: `NOT_OBSERVED`/zero frames
  for all ten, both directions), does not claim which side sends which
  class or what any field means, and does not claim the report's static
  grade extends to the two classes deferred to LANE-CS or the five owned by
  the sibling shared-serializer module (neither touched by this row's code).
- The `Pets` row's "HEADLESS-DONE" carries the identical nonclaim as the
  `TreasureHunt`/`Gathering`/`Winemaking`/`BuildingCrystal`/`Activity` rows
  above, for the same reason (`PF_FIELD_VALIDATION.tsv` shows
  `NOT_OBSERVED`/zero frames for all ten of `Pets_SummonPetVital`,
  `Pets_UnsummonPetVital`, `Pets_UpdatePetPropertyVital`,
  `Pets_RestorePetAmityVital`, `Pets_NotifySailorDeadVital`,
  `Pets_MergePetsVital`, `Pets_MergePetsResultVital`,
  `Pets_ClaimPetsMegringItemVital`, `Pets_LearnPetSkillVital`, and
  `Pets_UpdateSummonPetsTimeOutVital`, both directions). It additionally
  does not claim that `Pets_MergePetsVital`'s `PHI(...)` `field_offset`
  ambiguity has been resolved to a single client-memory address -- only
  that the ambiguity is irrelevant to this module's scope (wire tag/order/
  length, not client-object layout).
- The `Express` row's "HEADLESS-DONE" carries the identical nonclaim as the
  `TreasureHunt`/`Gathering`/`Winemaking`/`BuildingCrystal`/`Activity`/`Pets`
  rows above, for the same reason (`PF_FIELD_VALIDATION.tsv` shows
  `NOT_OBSERVED`/zero frames for all four of `Express_ClientRemoveExpressVital`,
  `Express_ClientSendExpressResultVital`, `Express_ClientcClaimExpressVital`,
  and `Express_ResetExpressCountVital`, both directions). It additionally
  does not claim `Express_ClientRemoveExpressVital` and
  `Express_ClientcClaimExpressVital` are the same action because they share
  a wire shape and a serializer span -- only that their encode/decode byte
  layout happens to be identical; the two vital ids, and the two dataclasses
  representing them, stay distinct. It also does not claim anything about
  the four `Express_GS*`/`Express_GetActorExpressDataFromDBVital` classes
  that have no row in `PF_SERIALIZER_FIELDS.tsv` at all -- their wire shape
  is simply unknown, not excluded for being partially unresolved.
- The `Community` row's "HEADLESS-DONE" carries the identical nonclaim as
  the `TreasureHunt`/`Gathering`/`Winemaking`/`BuildingCrystal`/`Activity`/
  `Pets`/`Express`/`CollectionObj` rows above, for the same reason
  (`PF_FIELD_VALIDATION.tsv` shows `NOT_OBSERVED`/zero frames for all
  sixteen of `ChangeActorCommentVital`, `ChangeActorPenNameVital`,
  `ChangeActorPersonalDataVital`, `CommunityCommandNotAllowVital`,
  `CommunityPropertyChangedVital`, `OpenLetterInABottleVital`,
  `OpenPenpalLetterVital`, `RemoveBlackListVital`,
  `ReplyLetterInABottleVital`, `RequestorConfirmSoulMateMatchVital`,
  `SetReceiveActiveChangeVital`, `TargetConfirmSoulMateMatchVital`,
  `ThrowLetterInABottleVital`, `ThrowPenpalLetterVital`,
  `UseBlankPenpalLetterVital`, and `WriteBlankPenpalLetterVital`, both
  directions). It additionally does not claim that any pair of classes
  sharing an identical field shape (`ChangeActorCommentVital`/
  `ChangeActorPenNameVital`/`RemoveBlackListVital`/
  `TargetConfirmSoulMateMatchVital`/`ThrowLetterInABottleVital` are all
  u64+wstring+u8; `ReplyLetterInABottleVital`/`UseBlankPenpalLetterVital`
  are both u64+u64+u8) are the same action -- only that their encode/decode
  byte layout happens to match; each keeps its own vital id and dataclass.
- The `Appraisal`/`Dyeing`/`Relive` rows' "HEADLESS-DONE" carry the
  identical nonclaim as every sibling row above (`PF_FIELD_VALIDATION.tsv`
  shows `NOT_OBSERVED`/zero frames for `AppraisalVital`,
  `AppraisalStopVital`, `DyeingVitalRes`, `DyeingRemoveVital`,
  `DyeingShipVitalReq`, and `ReliveVital`, both directions). It
  additionally does not claim `DyeingVitalReq`'s field 2 is untagged just
  because `PF_SERIALIZER_FIELDS.tsv` labels it `UNTAGGED_STRING8_LEN32LE`
  -- that specific label has already been shown unreliable on its own
  (`logout_hypothesis.py`'s `ReturnSelectServerVital` erratum, RE-196/
  GT-055); `DyeingVitalReq` is excluded from this batch pending a static-RE
  answer to that exact question, not because its shape is assumed to be
  bad.
- The bridge-side cross-check (`pf_bridge/tools_bridge/pf_wire_fields_xcheck.py`)
  is an INTERNAL-CONSISTENCY check between two descendants of the same static
  image, not independent evidence, and a `PASS` from it has never meant "the
  client sends this frame". As of round `0bzq9y` it also no longer means "every
  function in this module was compared": a function whose body the tool cannot
  read now comes out as a counted `UNRESOLVED` instead of being credited with
  the fields a fallback guessed for it. On 2026-09-07 that moved six functions
  out of the compared set (151 compared, 36 unresolved, where the run before it
  reported 157 compared, 30 unresolved and the same zero findings). Nothing
  about those six functions changed; what changed is that the tool now says it
  cannot see them.

## Who registers an answerer -- the declarative seam (round `ly40b5`)

`ui_dispatch.adopt_answerer(qualified_name, vital_id, module)` landed this
round, in this lane's own file, and it is the route `NOW.md` (COO `1441`
item 1) asks for: a lane may not call `register_answerer()` itself. A lane
that adopts declares two module-level lines --

    ANSWERS_VITAL_ID = 0x37B1
    ANSWERS_WITH = answer_party_invite

-- and registers nothing. `lane_hooks._discover()` is the one function that
imports lane files, so it is the only place a registration can happen
without being an import side effect; chief approved the hookup (letter
`notes_to_chief/20260908_1703_FROM_CHIEF_R404-*`) and has it as queue item 1
of the next chief round that touches `src/`. Until that call exists in
`_discover()`, `adopt_answerer()` has no production caller: the three
answerers still register themselves, and converting them is the round after
the seam lands (converting first would unregister three working buttons).

Every refusal is named on stderr as
`UI_DISPATCH_ADOPT_REFUSED id=... reason=... by=...`, because `_discover()`
does not read the return value: a silent refusal and a lane that declared
nothing would be the same console line, which is chief's one added condition
(letter section 4). The reasons are `not_routed_here`,
`not_a_discoverable_lane`, `name_is_not_that_module`, `no_reviewed_owner`,
`not_the_reviewed_owner`, `id_is_not_the_declared_one`, and
`no_declared_callable`.

**And the sentence chief asked for, written on the day it is still true, not
the day it stops being true (letter section 5).** Until `_discover()` is the
one writing `_ANSWERERS`, the ownership gate in `register_answerer()` closes
only against an impostor that registers under SOMEBODY ELSE'S name: a lane
file that declares `production_allowed = True` and registers an id under its
OWN name is refused only because `_ANSWERER_OWNERS` has no row for it -- a
reviewed row plus a stack the walk cannot see (round `ihf029` D-A -- a table a
lane appends to and an allowed module flushes later; NOT round `1gc6hl`'s D-A,
which is a direct `_ANSWERERS` dict write, and this paragraph named the wrong
round until pf-adversary D6 this round) is still a route in. Reading that code
today and concluding "impostors are closed" would be reading more than it says.

**pf-adversary ran against this seam the round it was written and was NOT
CLEAN; the two findings that mattered are paid in the same round.** D1
(CRITICAL, measured end to end): on the adopt route no lane frame is on the
stack, so `fn.__module__` was the only thing that could add a second name to
the gate -- and a `production_allowed = False` lane that set
`victim.ANSWERS_WITH = evil` and forged `evil.__module__` to the victim's name
put its own bytes on the wire under the reviewed owner's name, its own name in
no token, while discovery printed `SKIPPED_NOT_PRODUCTION_ALLOWED` for it.
That is `ihf029`'s D-A promoted to the sanctioned path -- the hole this seam
was requested to close. The gate now also asks two questions a lane file
cannot rewrite after the fact: which FILE the callable was compiled from
(`__code__.co_filename`, mapped back through a lane's `__file__`) and which
discoverable lanes HOLD the object by identity. Both can only add names, never
remove one. D2 (HIGH): the call sits in `_discover()`, outside the try that
guards a lane's import, so `ANSWERS_VITAL_ID = [id, id]` -- an author wanting
two buttons -- killed the whole boot with `TypeError: unhashable type`. Two
layers now: the declaration's type is checked before it is used, and nothing a
lane writes may raise out of the function at all.

**Still open, recorded rather than fixed:** the gated-incumbent YIELD path has
no test on the adopt route (the refusal path does), and `_install_answerer()`
is a named write primitive whose registrar name and gate are caller-supplied
strings -- no new capability over the `_ANSWERERS[id] = ...` residual that was
already recorded, but it reads like plumbing where the dict write reads like
an attack.
