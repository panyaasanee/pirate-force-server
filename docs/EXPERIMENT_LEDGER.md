# Experiment ledger

| ID | Classification | Result | Evidence ceiling |
|---|---|---|---|
| V140 | Runtime evidence checkpoint | Pass | P86 synthetic-harness interaction transport/UI only; all AGENTS.md nonclaims remain |
| V141 | Legacy characterization | Offline pass | Source/launcher/package consistent; raw capture exists but is not formally audited/reported/promoted |
| FND-001 | Architecture | Pass | Modular lifecycle/persistence only; no protocol hypothesis |
| FND-002 | Character lifecycle | Offline pass | Create/List/Select/StartGame share identity across actor/Avatar/ActorAttr/MovementAttr |
| FND-003 | Restart/loopback | Offline pass | Session-bound position and identity survive DB reopen; exact projected frames round-trip locally |
| FND-004 | Corrective audit | Offline pass | StartGame Teleport restored; malformed/replay/account/lease/migration negatives pass |
| FND-005 | Assisted client reconnect | Runtime pass | Arena01 Create/List/Select/StartGame and scene-1 position survive client exit/relaunch against the same running server; server-process restart is unproven |
| FND-006 | Server-process restart durability / shutdown operational negative | Grade B runtime pass + Grade E shutdown negative | One existing `Arena01` row retained selector 0, identity `0x10010001:0`, name and exact opaque actor/avatar blobs across a stopped Round-A process and fresh Round-B server/client Select/StartGame. The final Round-A checkpoint scene 1/seq 0 `(-9192.125,-2674.037109375,186)`, heading `5.009882926940918`, is byte-exact in Round-B StartGame. The Chief directly observed minimap `-9192,-2674` and name `Arena01` in the client UI (operator observation; no screenshot retained). Round A did not prove graceful shutdown, Round B required Stop-Process after console-control attempts failed, and lease generation 7 remains open. Crash durability, graceful session close, remote/multi-account/name-policy/delete claims remain unproven. |
| FND-007 | Connection-local GAME lease teardown / shutdown operational negative | Grade B runtime pass + Grade E shutdown negative | Exact open session `aeee8c26bef046cfa0a8958579d7f68d`, generation 6 and selected character 1, gained `closed_at=2026-08-16T05:54:24.102133+00:00` immediately after the normal client GAME disconnect while the server remained alive/listening. The persisted scene-1 position and update timestamp were unchanged, so disconnect close did not rewrite movement. Port/PID continuity is an operational observation without a retained netstat sidecar. Ctrl+C still failed and validated Python PID 12228 required force-stop; graceful process shutdown, crash recovery, abrupt client loss, concurrent clients and authenticated multi-account ownership remain unproven. |
| FND-008 | Active-client clean requested server shutdown | Grade B controlled runtime pass | A retained Windows helper result records `attach_console=true` for the console associated with exact target PID 24080 and `ctrl_c_sent=true` while GAME traffic was active; the server then stopped. It closed exact selected session `690593d79d7e4a9bb550900a48f2b21e` generation 7 about 62.6 ms after the helper window began, without rewriting position, then closed GAME/LOGIN logs, emitted one stopped marker, produced empty stderr and exited 0. The Chief directly observed the still-connected client show a disconnect modal after server stop (operator observation; no screenshot retained); its wrapper later exited 0 with empty streams. This proves one helper-driven requested-stop path only, not every console host/signal source, crash/power-loss/transaction-failure recovery, concurrency, remote clients or multi-account ownership. |
| FND-009 | Abrupt server-process loss and restart recovery | Grade B recovery pass + Grade E deliberate-loss trigger | Round A captured exact TargetPos `(-7292.4833984375,-3187.03759765625,186)`, heading `0.18130016326904297`; the committed Position retained those values at scene 1/seq 0 while selected lease generation 8 remained open. A pinned `Stop-Process -Force` removed exact server PID 11412 while the client remained active; the post-kill snapshot retained the committed position and open SID. Round B startup closed that stale SID before fresh login, emitted byte-identical Character List and exact persisted StartGame MovementAttr without CreateActor, opened generation 9, then normal disconnect closed it. The replacement server subsequently emitted one stopped marker and exited 0. This proves one stable committed-checkpoint recovery path, not in-flight/non-empty-WAL recovery, power loss, every crash mechanism, concurrency, remote clients or authenticated multi-account ownership. |
| FND-010 | Abrupt client-process loss and same-server reconnect | Grade B server-reaction/reconnect pass + Grade E deliberate-loss trigger | One validated `Stop-Process -Force` removed active Client A PID 7648; exact selected generation-10 SID `80311b2e090441b68df24a770a64a665` closed about 650 ms after trigger start without a position rewrite while the same server PID 20444/listeners remained live. Client B opened generation 11 and, with zero CreateActor, received byte-identical Character List and StartGame PCs preserving identity/name/opaque blobs/position; its normal exit closed the new SID. The Chief directly observed minimap `-7292,-3187` and `Arena01` after reconnect (operator observation; no screenshot retained). No byte-frozen post-kill DB/WAL/SHM snapshot exists, so the intermediate database claim is logical-row evidence only. This does not prove network partition, power loss, in-flight movement, concurrency, remote clients or authenticated multi-account ownership. |
| OBJECT-POP-002 | Typed authoritative NPC-style scene-actor membership | Grade B controlled runtime pass | The first valid scene-1 TargetPos emitted exact nearest-20 NPCAttr+full-MovementAttr entries and a byte-identical 3-second reapply. Forward refresh retained 19 NPCAttr-only entries, admitted placement 87 with full MovementAttr and omitted 82; reverse admitted 82 and omitted 87, restoring the initial set. All four PCs/frames are byte-identical to typed Foundation and frozen V141. Zero structural DeleteActor entries and 70 post-reverse heartbeats were recorded. The final raw TargetPos XYZ/heading exactly match the durable position; scene/sequence remain separate persisted Position fields. The only pre-to-final logical-row DB deltas are one closed generation-12 session and that position, while identity/name/opaque blobs/migrations/integrity/FK remain fixed. The Chief directly observed populated radar/models plus different forward/reverse scene models (operator observation; no screenshot retained), but no visual identity or despawn is proven. This does not classify monster/remote player, prove authentic policy or persisted membership, or promote combat/faction/other object domains. One stopped marker, empty stderr and no listeners are retained, but no exit-code sidecar exists. |
| ITEM-LIFECYCLE-001 | Exact V111 Backpack merge persistence | Grade B controlled runtime pass | Round A StartGame carried the exact initial four-record Backpack; exact 36-byte V111 request `(op4,value0,id3)` committed identity 1 quantity 2 and removed identity 3 before the byte-exact frozen response. Round B reconnected with zero CreateActor/ItemOperate and received the exact three-record merged Backpack. A/B Character List is byte-identical; replacing only the 159-byte initial Backpack subwire with the 124-byte merged subwire produces the full B StartGame PC, and reframing that PC produces the B frame. Final SQLite adds migration 3, exact merged rows and closed generations 12/13 while account, `Arena01`, selector, identity, opaque blobs, position, older sessions, integrity and FK remain fixed. The Chief observed A UI count `4/40`→`3/40` after drag (operator observation; no screenshot); policy blocked B PIN entry, so no B UI count is claimed. This does not generalize moves/split/drop/equipment/ownership, concurrency, multi-account authorization or crash recovery. No retained server-exit sidecar exists and PTY exit was 1, so no clean-shutdown/exit-0 claim is made. |
| CHARACTER-NAME-001 | Persisted player-name projection | Grade A static boundary + offline implementation pass | Canonical CreateActorDataEx name is persisted losslessly and re-emitted in StartGame ActorAttr mask `0x01000000`/wstring `+0x164`, which exact `NameBoardPlayer` code consumes. BasicAttr, opaque AvatarAttr and frozen faction value remain unchanged. Live visible rendering, remote-player names, rename policy and restart durability are unproven. |
| CHARACTER-NAME-002 | Local-player world name projection | Grade B controlled runtime pass | Exact StartGame raw wire carries actor identity `0x10010001`, unchanged BasicAttr mask `0x070C`, ActorAttr low/high masks `0x01000800/0`, mandatory bool `1`, cash `10000`, and one tag-`0x48`/14-byte UTF-16LE `Arena01`. The Chief directly observed `Arena01` above the selected local player in Port Royal, distinct from the target panel; the client stayed responsive and DB guard was `PASS_UNCHANGED`. No screenshot was retained. Remote-player naming, rename policy, authenticated ownership, and server-process restart durability remain unproven. |
| HYP-LEDGER-001 | Hypothesis governance | Offline pass pending acceptance | Canonical 14-entry inventory strictly pins emitted hypotheses, diagnostics, retired values and synthetic geometry; all are non-production, geometry is non-authentic, no extension approval is currently recorded, and every over-limit entry is frozen or expired against further dependency |
| ARENA-001 | Test Arena P30 object spawn/target | Runtime pass / classification negative | One P30 renders with name and HP and emits TargetVital; stable UI remains green/person/talk, so hostile relation and combat are unproven |
| ARENA-002 | BasicAttr faction-only diagnostic | Runtime transport pass / classification negative | Exact faction-6 transport/TargetVital pass; stable UI stays green/person/talk, while a frame burst proves pink-red/red-outline/non-talk UI only after exit confirmation, so the candidate is retired and teardown is not hostility |
| REL-001 | Relation comparator instrumentation | Runtime trace pass | 1,023 stable comparisons read injected P30 operand 6 against current local/default operand 0; 16 post-exit entries bypass both reads, so teardown UI is not an observed faction mutation; authentic player faction/policy remain unknown |
| SCENE-001 | Scene2 marker2 load-only | Runtime load pass | Existing Arena01 was read-only projected coherently in ActorAttr/MovementAttr/Teleport at `(26905,21185,1680)`; the client loaded and rendered Prison Exile Island and returned runtime-ready traffic; seq0/heading0 remain compositional |
| SCENE-002 | Authentic MOBS34/P60 single population | Runtime render pass / target negative | Fighting Fish soldier rendered with exact name/model at authentic P60 while the test player used a labeled synthetic 100X/50Y offset; DB main/WAL/SHM stayed byte-identical; clicks/Tab produced no TargetVital, so targetability/hostility/combat remain unproven |
| SCENE-003 | MOBS34/P60 HP liveness diagnostic | Runtime target-selection pass | SCENE-002 plus only mask `0x0701`→`0x070D` and canonical current/max HP `3857/3857` caused one click to emit exact P60 TargetVital kind2 + ChooseNPC; HP/liveness controls this selection gate, while 3857 remains a bounded diagnostic rather than proven spawn policy |
| SCENE-004 | P60 hover-refresh interaction characterization | Runtime pass / hostile-relation negative | Explicit pointer off/on refresh produced P60 TargetVital on the first model click; after selection the refreshed talk cursor persisted and the next model click emitted exact 29-byte ChooseNPC for identity `0x203D`. This excludes stale cursor and ground click for the run and proves the present actor follows the NPC-style interaction path; hostile relation/combat remain unproven. DB guard: `PASS_UNCHANGED` |
| REL-002 | FACTION lookup matrix | Static producer/consumer + guarded read-only runtime pass | Client loads `FACTION.n_ID` and `s_ENEMY`; exact lookup `0x4A1D50` maps target 6 against IDs 0-31. Candidate 0 retains the neutral result; 1, 2, 3 and 18 produce the opposite result symmetrically. This selects candidate 1 for a bounded test, not as authentic player faction. |
| SCENE-005 | P60 faction-table-guided relation composition | Runtime hostile-relation pass | StartGame adds only BasicAttr `0x0400`/u32 candidate 1. P60 becomes stably red/outlined, Tab-selectable with red target UI and emits exact TargetVital kind 1 for `0x203D` with no ChooseNPC. The SCENE-004 NPC interaction defect is resolved within this scenario; attack/damage/AI/death/loot and authentic player faction remain unproven. DB guard: `PASS_UNCHANGED` |
| SCENE-006 | P60 target-bound attack command | Static producer/consumer + repeated runtime pass | Guarded producer `0x44D260` and queue `0x5DD800` plus two fresh runtime wires agree that double-clicking selected hostile P60 emits ActionVital `0xEA7D` with target `0x203D`; Tab-only emits no ActionVital and both DB guards pass unchanged. Server response, damage, hit/miss, FightAttr, AI, death and loot remain unproven. |
| SCENE-007 | Port Royal P60 minimum ActionVital response | Static consumer + repeated runtime transport pass | Exact hostile kind-1 target ordering gates one RuntimeRes v4/count-1 ActionVital response per session and omits the request's trailing TargetPos. Within the echoed 64-byte ActionVital body, only performer qword zero changes to the persisted selected identity; target `0x203D`, action `0xEA7D` and pose/scene fields remain exact. Two fresh clients remained responsive with visible HP unchanged and DB main/WAL/SHM unchanged. The V74/P144 placement harness puts player and P60 in one default-camera frame without rotation. Visible attack animation, hit, damage, FightAttr, AI, death, loot, skills and authentic faction remain unproven. |
| SCENE-008 | EA7D inert action-consumer lifecycle | Static consumer + guarded instrumented runtime pass | The exact SCENE-007 ACK reaches the client ActionVital handler, constructs one `0xEA7D`/`0x203D` action with implementation pointer zero and terminal bit `8`, enters the selected actor's `+0x20` queue, and reaches the first common update return with bit `8` unchanged. The clean probe exits while the client remains alive; server heartbeats continue and DB main/WAL/SHM remain unchanged. This proves response consumption and an inert terminal lifecycle only, not animation, hit, damage, FightAttr, AI, death, loot, skills or authentic faction. |
| SCENE-009 | CHitResult consumer provenance and ready-only absence | Exact static consumer proof + guarded instrumentation readiness / operational negative | Client registration/serializer/handler code fixes `CHitResult` at Vital ID `0x16F7` and proves a bounded target-reaction implementation path gated by record bit0+bit3, no bit4, resolved target and non-null factory result, reaching preparation for the target actor `+0x40` queue. Opaque record `+0x08` is not proven damage. The corrected probe emitted only `probe_ready` during one exact hostile TargetVital -> EA7D -> SCENE-007 ACK window; therefore this controlled response did not naturally expose a CHitResult event. No CHitResult packet was sent, and no attack/hit/miss/damage/HP/FightAttr/AI/death/loot/skill or EA7D-to-CHitResult ordering claim is promoted. DB guard: `PASS_UNCHANGED`. |
| SCENE-010 | EA7D BEHAVIOR-registry lookup corrective | Exact static population/callsite proof + guarded instrumented runtime pass | `0x4162A0 -> 0x47BFC0 -> 0x491650` constructs singleton `0x102DAD8`, loads `BEHAVIOR`, stores row `n_ID` at entry `+0x04`, and populates named behavior fields including the `s_HIT_KEYFRAME`/`s_HITBACK` vector at `+0xE4`; `0x702A10` is its dword-key lookup. It received `0xEA7D` twice at producer/control return `0x44E92A` and once at inbound ActionVital-handler return `0x7517B5`; all were null at those exact instants, the last 18 ms after the SCENE-007 ACK. The prior ACHIEVEMENT correction is superseded: `0x705000` belongs to distinct singleton `0x102DB68`. Null does not block generic ActionVital construction/queue. Composing the observed null with factory `0x48D870` predicts a null EA7D CHitResult implementation, but no CHitResult invocation was observed. No exact ScriptB binding, animation, hit/damage/HP, AI, death, loot, skills or authentic faction is proven. Probe exit 0; DB guard `PASS_UNCHANGED`. |
| SCENE-011 | Natural BEHAVIOR entry observation | Exact static layout + guarded instrumented runtime pass | One observe-only run captured key 97 non-null at static caller `0x48D2C8`; keys 278/279 non-null twice each at `0x7555D2`; EA7D null twice at `0x44E92A`; and key 0 null twice at `0x7555D2`. Keys 278/279 have exact source-named `n_AMOUNT_TARGET=1`, `n_RANGE=75`, `n_DAMAGE_AREA=200`, `n_PROFIT=0`, `n_THENDO=278`, `n_CLASS=0` and vector count 1; key 97 has `n_RANGE=10`, vector count 0. These are raw property values, not attack/damage/HP semantics. The paired flow emitted EA7D/target203D without TargetVital, so no strict server ACK and no inbound response/CHitResult occurred. Probe exit 0, no probe error, DB `PASS_UNCHANGED`; two earlier exit-1 runs are superseded timing diagnostics. No ScriptB binding, vector-record content, animation, hit/miss, damage/HP/FightAttr, AI, death, loot, skills or authentic faction is proven. |
| SCENE-012 | EA7D geometric-gate callsite boundary | Exact static proof + guarded instrumented entry / operational negative | Static code proves `0x44EB1D -> 0x4758D0` performs a strict squared-distance gate for EA7D using an unknown live object scalar plus the mode-0 BEHAVIOR selection; accepted natural keys 278/279 provide raw `n_RANGE=75`, but missing live scalar/object/XYZ inputs prevent an offline boolean. The narrowed probe captured exact `gate_enter` at live `0xD0EB1D` and then failed closed on a duplicate gesture; it emitted no `gate_result` and exited 1. The earlier uninstrumented TargetVital kind1 -> EA7D -> one-shot ACK is a separate control, not correlated to the probe. DB `PASS_UNCHANGED`; transport continued until client close. No true/false result, in/out-of-range, units, attack success, animation, hit/miss, damage/HP/FightAttr, AI, death, loot, skills or authentic faction is proven. Runtime-hook expansion on this path is stopped; the next boundary is structured offline original combat-response discovery only. |
| SCENE-013 | Structured combat-corpus capability audit | Exact hash-guarded offline inventory / corpus capability negative | A deterministic anchored-line audit counted 2,621 decoded frames in six unique v74-v76/v81-v83 logical sources. All six are GameClient -> local-emulator receive logs; eligible original server -> client frames are zero. All-direction counts are TargetVital 5 and zero for ActionVital, CHitResult, CFightMsgVital, CKnockdownVital, CShotMissileVital and CMissileHitResult. Because no eligible frame exists, `bounded_target_negative=false`: this proves the curated corpus cannot answer the inbound combat-result question, not that any packet is absent from the real protocol. Raw bytes were not scanned and evidence remained read-only. No synthesis is authorized; the combat-response lane requires a lawful original server capture or exact server producer. |
| SKILL-001 | Trigger/state static boundary and observation readiness | Exact static registration/codec/consumer proof + guarded probe readiness | `TriggerCastSkillVital` is registered with class ID/hash `0x5CD2` and has raw wire fields `(u16,u8,u32)`. With singleton `0x1032EC4` present, its consumer prepares a possibly null candidate and submits only through `0x601880 -> 0x449110`; without the singleton it skips that edge, and all branches return true. No direct UI/hotkey producer or field semantics are proven. The reviewed observe-only probe guards both exact binaries and codec/consumer/submission addresses but has not run live. `CSkillAttr` is an optional ordered container whose records remain `(key u16, opaque u16, opaque u32)`; LearnSkillResult populates it and RevertSkill removes by key. `CStartCooldownVital` instead updates a separate `CCooldownAttr` from raw `(s16,f32)` records with no exact cross-class key binding. StartGame stays unchanged. No job/class, ownership, hotbar, cooldown meaning, successful cast, combat, damage or persistence claim is promoted. |
| JOB-001 | Character-creation preset versus persistent class boundary | Exact static named-table/codec audit / bounded field-identification negative | Exact client code loads `CHARCREATE_CLASS`; the guarded frozen table has keys `{1,2,4,16,32}` and named starter appearance/equipment/skill-preset columns. Captured CreateActor `test01` numerically matches row-1 starter chest, leggings and weapons, but no exact `n_ID` setter/copy edge reaches any CreateActor/AvatarAttr/ActorAttr field. CreateActorDataEx `+0x18=0` while the matching preset key is `1`, so that byte cannot be promoted as class. All transported Avatar bytes remain opaque; no job/class field, progression, entitlement or class persistence is proven and no value may be synthesized. |
| DELETE-003 | DeleteActor nested producer and outer-framing boundary | Grade A exact static producer/submission proof + operational/corpus negative | Exact UI paths construct raw operation values 1 and 2 in `DeleteActorVital` and submit both through generic `0x4011A0 -> 0x5DD800`. No exact edge proves an outer protocol class/version/mask/count, wire envelope, response or state transition. In the isolated `DelTst01` run, the operator observed the confirmation dialog and cancelled before the affirmative action; no screenshot was retained, captured logs contain zero `0x36DB`, and the isolated DB main/WAL/SHM guard is `PASS_UNCHANGED` with `deleted_at=NULL`. This proves no Delete request, response, refresh or mutation. No semantic op labels, outer parser, response or repository delete path are authorized; a future affirmative UI action requires action-time confirmation. |
| HOTBAR-001 | Set-item hotkey protocol structure | Exact static registration/codec/consumer proof / producer and persistence negative | `SetItemOnParticularHotKeyPosVital` registers deterministic class-name hash `0xE0AC`; exact codec `0x5E6DE0` carries raw `(u32,i8,i8)`. Consumer `0x5EFAF0` calls one client-state method with logical arguments `(raw +0x14 u32, sign-extended +0x18, sign-extended +0x19 - 1)`. Generic pool/prototype construction is not a UI producer. No field meaning, item-versus-skill discriminator, direct submission edge, persisted container or reconnect behavior is proven. StartGame is unchanged and no packet/state may be synthesized. |
| COMBAT-BIND-001 | EA7D BEHAVIOR scalar selection versus inbound key | Grade A static selection proof + Grade C SCENE-011 composition / identity-binding negative | Exact mode-0 code retains the first non-null `n_RANGE` and skips later values greater than or equal to it. SCENE-011 order `0,278,279` with equal raw values 75 therefore makes key 278 supply retained scalar 75 and skips 279. Only scalar 75 reaches the geometric gate; candidate identity is discarded. Inbound ActionVital lookup independently consumes object `+0x30`, exact EA7D in the accepted shape. Creation value 2200002 has zero client literal and no dataflow into this lane; no equipment, ownership, weapon, hit/damage/HP or persistence claim is proven. |
| COMBAT-KNOCK-001/BIND | CKnockdown structural consumer and BEHAVIOR binding | Grade A exact static consumer proof / bounded direct-writer negative | `CKnockdownVital` consumer `0x750700` resolves a receiver actor; `0x47CAD0 -> 0x48D270 -> 0x702A10` uses raw `+0x20` as a BEHAVIOR key, stores opaque `+0x24` at inner implementation `+0x50`, and discards `+0x34` in this concrete path. Success builds wrapper vtable `0xF0F7DC` with flags `0x40000005`; wrapper bit `0x40000000` selects the actor `+0x40` lane through `0x4843F0`, while `0x4A0C90` receives a separate queue argument `1`. No direct/static non-framework writer, EA7D/278/279/CHitResult edge, HP/FightAttr/UpdateAttr mutation, visible knockdown/animation or packet authorization is proven. Resume only with original S2C evidence or an exact producer assigning all fields. |
| COMBAT-KNOCK-DATA-001 | Frozen BEHAVIOR data-to-CKnockdown binding | Grade A exact frozen-table/static-path audit + Grade C structural composition / bounded binding negative | The exact expanded BEHAVIOR table has 2,279 rows and 30 columns. Rows 278/279 share the reported raw numeric properties but differ in source animation/keyframe strings; 10 row IDs contain `KNOCK`, 106 rows have non-empty `s_HITBACK`, and zero source strings contain `REACTION`. No row or name is uniquely bound to vital `+0x20`; accepted `vector_count=1` for 278/279 is structural only. The concrete consumer still uses `+0x20` as key, stores `+0x24`, and discards `+0x34`. No direct non-framework writer was found, but indirect/original production remains possible. Stop asset-name mining; require lawful original S2C evidence or an exact producer assigning every field. |

V141 artifacts:

- source SHA-256 `2EB05ED2FDBDD5EE3D91F7FBB8C1D16A4C7A02A843BC97169B16A389E4EA4C22`
- launcher SHA-256 `6D8E410033E91D435BB3D65D3FECD02D93634C922B1D284440DEACF2FC0C5009`
- three-entry package SHA-256 `0572F476437E47302E3CD742239F56EFC96A90D7465F73C08EE77CB1EBAF1F00`

V141 is not described as runtime-passing until its existing raw capture is audited and promoted, or a new verified run is produced.
Foundation is not described as a durable server lifecycle until a later audited run
also proves server-process restart/crash recovery and the remaining state domains.
