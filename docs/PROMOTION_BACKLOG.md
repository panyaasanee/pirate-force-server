# PROMOTION_BACKLOG — proven hypotheses still `production_allowed = false`

> เจ้าของไฟล์: chief (LANE-E) · COO จัดอันดับ · NOW.md `2351`/R362 §7
> Regenerate ทุกรอบที่มี scenario/hypothesis ใหม่ (`production_allowed` เปลี่ยนค่า หรือโมดูลใหม่ตั้งค่า False)
> วิธีสร้างรายการ: `grep -rl "production_allowed" src/pirateforce_foundation/*.py` แล้วอ่านค่าที่ตั้งจริงในแต่ละไฟล์ + เทสคู่ (`tests/test_<module>.py`) ที่ pin ค่า False ไว้
> สำรวจรอบนี้ (chief round `d5igq0`, 2026-09-06): `grep -rl "production_allowed" src/pirateforce_foundation/*.py` เจอ 105 ไฟล์ (นับเฉพาะ top-level ของ `src/pirateforce_foundation/*.py` ตามที่คำสั่งบรรทัดบนพิมพ์จริง — ไม่รวม `lane_hooks/`/`gm/` แม้ 18 แถวด้านล่างมีไฟล์จาก `lane_hooks/` อยู่หนึ่งแถว ตรวจแยกด้วยคำสั่งที่ recursive กว่า) ตั้งค่า `production_allowed = False` เองจริง 18 ไฟล์ (ตรวจซ้ำโดย pf-adversary รอบนี้ ตรงกับ 18 แถวด้านล่างทุกแถว) ที่เหลือ `True` แล้ว = ผู้เล่นเห็นอยู่แล้ว ไม่นับเป็น backlog · ไม่มีโมดูลไหนใช้ค่าคงที่ตัวพิมพ์ใหญ่ `PRODUCTION_ALLOWED` หรือ env var แทน — รูปแบบเดียวในรีโปนี้คือ boolean `production_allowed` ระดับโมดูล
> 🔴 **รอบถัดไปที่ regenerate**: คำนวณจำนวนไฟล์ทั้งหมดใหม่จากคำสั่งจริงที่รันตอนนั้น อย่าคัดลอกตัวเลข 105/18 มาโดยไม่รันซ้ำ (pf-adversary ชี้ว่าตัวเลข "117/99" ของร่างแรกคำนวณผิดพลาดจนทำซ้ำไม่ได้ — แก้เป็น 105 จากการรันจริงแล้ว แต่ตัวเลขรวมนี้เปราะบางทุกครั้งที่มีไฟล์ใหม่ ต้องรันคำสั่งจริงเสมอ ไม่ใช่จำเลขเดิม)

## ตาราง (เรียงตาม milestone/lane คร่าว ๆ — COO จัดอันดับจริงที่ 09:41 ตาม NOW.md)

| module | feature | gate | test | ผู้เล่นจะเห็นอะไรถ้าปลด | สาย |
|---|---|---|---|---|---|
| `npc_hp_link_hypothesis.py` | NPC-HP-LINK-001 (HYP-PF-029) server-driven HP-bar move บน NPC เป้าหมาย | `production_allowed=False` | `test_npc_hp_link_hypothesis.py` | HP bar ของ NPC ที่เลือกไว้จะยุบจริงตามเฟรมโดนตี (100→37→0) แทนที่จะค้าง | B (COMBAT) |
| `damage_hp_link_hypothesis.py` | DAMAGE-HP-LINK-001 (HYP-PF-026) เลขดาเมจลอย + HP ตัวเองลดต่อเนื่องจนตาย | `production_allowed=False` | `test_damage_hp_link_hypothesis.py` (มีเทสกันแก้ flag ทิพย์) | เห็นเลขดาเมจลอยแล้ว HP ตัวเองยุบถึง 0 พร้อมเข้าเงื่อนไข dying/death | B (COMBAT) |
| `damage_model_hypothesis.py` | DAMAGE-ENCODER-001 (ต่อยอด DAMAGE-MODEL-001 รอบ 83 · HYP-PF-024) เลขดาเมจลอยพื้นฐาน | `production_allowed=False` | `test_damage_model_hypothesis.py` | เลขดาเมจลอย ("63","379") ขึ้นเหนือเป้าที่โดนตี ทั้งฝ่ายเราโดนและมอนโดน (ฝั่งเราเอง GT-024 ยืนยันบนจอแล้ว) | B (COMBAT) — M4 |
| `hostile_hp_link_hypothesis.py` | HOSTILE-HP-LINK-001 (HYP-PF-038) HP-link บนมอนตัวจริง (Tornado Eagle tpl 31, HP 3857) | `production_allowed=False` | `test_hostile_hp_link_hypothesis.py` | HP bar ของมอนตัวจริงลดจากค่าฐานจริงของไคลเอนต์ ไม่ใช่แค่ตัวทดสอบสังเคราะห์ 100 แต้ม | B (COMBAT) — M4 |
| `runtimeres_death_hypothesis.py` | RUNTIMERES-DEATH-001 (HYP-PF-023) ลำดับ spawn→ตาย ของ NPC | `production_allowed=False` | `test_runtimeres_death_hypothesis.py` | NPC ตายจริงบนจอ (ท่าตาย `_F_DIE_000`) แทนที่จะมีแค่ฝั่งผู้เล่นตายเอง | B (COMBAT) — M4 |
| `npc_hostile_hypothesis.py` | NPC-HOSTILE-001 (HYP-PF-027) ทำให้ NPC ขึ้นสถานะศัตรู (faction-based) | `production_allowed=False` | `test_npc_hostile_hypothesis.py` | ชื่อ NPC เป้าหมายขึ้นสีชมพู/แดง เส้นขอบแดง แผงเป้าแดง (พิสูจน์แล้วสำหรับ faction 6 vs player faction 1) แทนที่จะเป็นกลาง | B (COMBAT) — ตรง P-2 สีชื่อมอน |
| `ground_loot_hypothesis.py` | GROUND-LOOT-001 (HYP-PF-032) วัตถุของตกพื้นจริงจากบิต RuntimeRes 0x08 | `production_allowed=False` | `test_ground_loot_hypothesis.py` | ไอเทมที่ดร็อปโผล่เป็นวัตถุ 3D บนพื้นพร้อมป้ายชื่อลอย (เคยเห็นจริงบนจอสั้น ๆ ~0.6s มาแล้วครั้งหนึ่ง) | B (COMBAT/loot) |
| `ground_loot_nameprop_hypothesis.py` | GROUND-LOOT-NAMEPROP-001 (HYP-PF-039) ควบคุมสี/ท่านามป้ายของ ของตกพื้น | `production_allowed=False` | `test_ground_loot_nameprop_hypothesis.py` | ป้ายชื่อของตกพื้นที่เห็นอยู่แล้ว ("Red leaves Hammer") เปลี่ยนสี/ท่านามได้ตามไบต์ gate+index แทนที่จะใช้ค่า default ของไคลเอนต์เสมอ | B (COMBAT/loot, UI) |
| `pickup_listener_hypothesis.py` | PICKUP-LISTENER-001 (HYP-PF-036) ตัวถอดรหัสคำขอ PickupTerrainThing | `production_allowed=False` | `test_pickup_listener_hypothesis.py` | ไม่มีผลบนจอโดยตรง (ฟังอย่างเดียว) แต่เซิร์ฟเวอร์จะ "เข้าใจ" คลิกเก็บของพื้นแทนที่จะทิ้งเฟรมไปเฉย ๆ | B (COMBAT/loot) |
| `loot_roll.py` | LOOT-ROLL-001 ตัวสุ่มดร็อปฝั่งเซิร์ฟเวอร์ (Door 2 ของวงจรลูท) | `production_allowed=False`, `LOOT_ROLL_DISPATCH_REACHABLE=False` | `test_loot_roll.py` | มอนที่ตายแล้วดร็อปไอเทมจริงตามตาราง `MOBS.n_DROPS_*` แทนที่จะไม่ดร็อปอะไรเลย | B (COMBAT/loot) — ต่อเนื่อง M5 |
| `remote_player_hypothesis.py` | REMOTE-PLAYER-ENCODER-001 (HYP-PF-025) actor_type 2 (CNetActor) ผู้เล่นอีกคน | `production_allowed=False` | `test_remote_player_hypothesis.py` | เห็นตัวละครผู้เล่นจริงคนที่สอง (รูป+ชื่อ+HP+เดินได้) ในโลกเดียวกัน แทนที่จะเห็นแต่ NPC (actor_type 4) | A (WORLD) — ตรง shared-world/multiplayer |
| `move_authority_hypothesis.py` | MOVE-AUTHORITY-002 (HYP-PF-030) ตรวจ/ปฏิเสธตำแหน่งที่ไคลเอนต์รายงาน | `production_allowed=False` | `test_move_authority_hypothesis.py` | ตำแหน่งที่รายงานผิดปกติ/เป็นไปไม่ได้จะถูกปฏิเสธ (ตำแหน่งเดิมที่บันทึกไว้คงอยู่) แทนที่จะเขียนทับตามไคลเอนต์เสมอ (ตอนนี้ไม่มีการกันวาร์ป/ความเร็วเลย) | A (WORLD, movement) |
| `logout_dialog_open_hypothesis.py` | LOGOUT-DIALOG-OPEN-001 (HYP-PF-040) ส่ง return-select ทันทีที่กล่อง logout เปิด | `production_allowed=False` | `test_logout_dialog_open_hypothesis.py` | ตอนกดออกจากเกม เซิร์ฟเวอร์ส่งคำตอบ return/character-select ของตัวเองทันที แทนที่จะรอ Cancel ของไคลเอนต์เพียงอย่างเดียว | A (WORLD, UI/logout) |
| `lane_hooks/lane_a_choose_npc_scene1.py` | ตัวตอบ ChooseNPC ของฉาก 1 (Port Royal/bg0001) | `production_allowed=False` | `test_lane_a_choose_npc_scene1.py` | คลิก NPC บางตัวใน Port Royal จะได้ trigger คุยเควส Columbus (placement P30) ที่ตอนนี้ไม่ได้อะไร แต่จะ**เสีย** trigger คุยทั่วไปกับ trigger ร้านค้า P91 ไปพร้อมกัน — ปิดไว้เพราะเป็น net regression ที่วัดแล้ว ไม่ใช่แค่ยังไม่ลอง | A (WORLD) |
| `learn_skill_request_hypothesis.py` | LEARN-SKILL-REQUEST-001 (HYP-PF-034) ตัวถอดรหัส CLearnSkillVital 0x36AA | `production_allowed=False` | `test_learn_skill_request_hypothesis.py` | ไม่มีผลบนจอเอง (ถอดรหัสอย่างเดียว ไม่เคยตอบ) แต่เป็นเงื่อนไขให้เซิร์ฟเวอร์เข้าใจคลิก "เรียนสกิล" แทนที่จะทิ้งเฟรมเงียบ | CS (class/skill) |
| `skill_attr_hypothesis.py` | SKILL-ATTR-001 (HYP-PF-035) บล็อก CSkillAttr ให้หน้าต่างสกิลเปิดได้ | `production_allowed=False` | `test_skill_attr_hypothesis.py` | กด K แล้วหน้าต่างสกิล (Skill_Main2) เปิดได้จริง แทนที่จะเปิดไม่ได้อย่างที่ GT-058 วัดไว้ตอนนี้ (attr pointer ที่ต้องใช้ยังไม่มี) | CS (class/skill, UI) |
| `learn_skill_result_hypothesis.py` | LEARN-SKILL-RESULT-001 (HYP-PF-033) ตัวเข้ารหัส CLearnSkillResultVital 0x673C | `production_allowed=False` | `test_learn_skill_result_hypothesis.py` | หลังเรียนสกิล ไคลเอนต์ได้รับเฟรมยืนยัน "เรียนสกิลสำเร็จ" จริง แทนที่จะได้ 0 encoder/0 dispatch ตามที่ STATS-PROG-001 วัดไว้ | CS (class/skill) |
| `item_operate_res_hypothesis.py` | ITEMOP-RES-GREENLINE-001 (HYP-PF-037) ItemOperateVitalRes 0x4C13 ให้ขึ้นแชทเขียว "ได้รับ [item] * count" | `production_allowed=False` | `test_item_operate_res_hypothesis.py` | เก็บ/ได้รับไอเทมแล้วขึ้นข้อความแชทสีเขียว "ได้รับ [item] * count" (client message id 131) แทนที่จะไม่มี feedback เลย | Q (script/quest) หรือ UI (chat) |

## nonclaims
- รายการนี้คือ "โมดูลที่ตั้ง `production_allowed = False` เอง" เท่านั้น — โมดูล/scenario ที่ตั้ง `True` แล้ว (99 ไฟล์จาก 117 ที่ grep เจอ) ผู้เล่นเห็นอยู่แล้วบนบูตปกติ ไม่อยู่ในตารางนี้
- ไม่ได้ตรวจว่าแต่ละอันพร้อม "โปรโมตจริงวันนี้" — บางแถวถูกปิดด้วยเหตุผลที่วัดแล้วว่าปลดแล้วจะเสียของเดิม (เช่น `lane_a_choose_npc_scene1.py` — เสีย trigger คุยทั่วไป + ร้านค้า P91) ไม่ใช่แค่ "ยังไม่มีเวลาทำ"
- ไม่ได้รันชุดเทสจริงของทั้ง 18 ไฟล์ในรอบนี้ — อ้างจากชื่อเทสที่ pin `production_allowed is False` ไว้เป็นค่าคงที่ (ตรวจแล้วว่าไฟล์เทสมีอยู่จริงและ assert ค่านี้ ไม่ได้ตรวจว่าพฤติกรรมข้างในถูกต้อง 100%)
- คอลัมน์ "สาย" เป็นการเดาจากชื่อไฟล์/โดเมน ไม่ใช่ประกาศ ownership อย่างเป็นทางการ — COO ยืนยัน/แก้ตอนจัดอันดับ
